# 项目完整链路说明

> 项目：`codex-switch`
> 根目录：`<repo-root>`
> 初始化日期：`2026-06-23`

本文档说明当前项目从输入、配置、启动、核心处理到产出的完整链路。开发前应先读 `项目文件结构说明.md`，再读本文，最后对照 `项目开发规范（AI协作）.md` 做变更。

<!-- DXM-DOC-RULES:START -->

## DXM 文档维护规则

- 本块由 DXM 管理；`--refresh-blocks` 只刷新本块，保留下方项目事实和人工补充。
- 项目目标、运行模式、配置链路、故障归因等长期事实必须写入下方对应章节，不能只留在聊天或临时文件里。
- 敏感配置和运行态输出只写用途，不回显真实 token、密码、API Key、账号明细、验证码、会话或密钥内容。
- 修改中文内容后必须检查可见乱码、错码和异常替换字符。

<!-- DXM-DOC-RULES:END -->

## 1. 项目目标

`ChatGPT Switch`（仓库与 EXE 兼容名为 `codex-switch`）是本机 ChatGPT 运行态切换与多会话同步工具。

- 解决问题：在当前 ChatGPT 账号登录态与一个 API 中转站调用态之间安全切换，把本地历史会话同步成统一视图，并为用户的 ChatGPT/Codex 环境安装可配置的 Image2/Grok 能力。
- 主要用户：当前 Windows 主机上的 ChatGPT Desktop / Codex CLI 使用者。
- 成功产物：React + Tauri 桌面应用，具备固定双槽位、真实运行态判定、页面内真实切换阶段、受管进程树关闭、共享会话池热同步、会话管理、固定 Skill 安装/配置、DPAPI 凭据、失败补偿、typed receipt 和脱敏操作记录。
- 运行态输入：默认从 `CODEX_HOME` 解析 Codex home；未设置时使用 `%USERPROFILE%\.codex`。核心文件包括 `auth.json`、`config.toml`、`sessions\**\*.jsonl`、`archived_sessions\**\*.jsonl`、`session_index.jsonl`，以及位于 Codex `sqlite_home` 下的 `state_5.sqlite`、`goals_1.sqlite`、`memories_1.sqlite` 和 `logs_2.sqlite`。普通同步只处理活跃 `sessions/`；Full/Sessions 备份与硬删除覆盖 active/archived 两类正文。`sqlite_home` 优先读取 `config.toml`，其次读取 `CODEX_SQLITE_HOME`，最后回落到 Codex home；`sqlite\codex-dev.db` 只作为兼容探测项，不作为当前会话同步来源。
- 运行态输出：工具自身加密运行态、临时回滚检查点、持久 full backup、共享会话池、操作记录、检查点空间状态以及面向 UI 的 dry-run/typed receipt。

## 2. 运行模式

当前已实现 / 已接入的运行模式：

- 桌面正常模式：Tauri 启动 React UI，顶部在“运行态 / 会话管理 / 技能”之间切换。首屏只加载 Codex Home 摘要、固定运行态、当前运行态和最近操作；会话域在首次进入会话页时加载，备份域由用户主动加载。各域保持独立 `loading | ready | error`。窗口关闭门禁在挂载时预注册，未 ready 或注册失败时切换 fail closed；切换后先进入 runtime refresh pending，确认当前运行态前禁用再次切换。session/backup 只标记 stale，不立即重新扫描。
- 固定双槽位模式：只管理 `plus`（ChatGPT 账号，内部兼容 ID）和 `relay`（API 中转站）两个槽位。账号导入必须验证 live `auth_mode = chatgpt`；覆盖使用页面内确认并归档旧的加密槽位文件。UI 分开显示已保存、当前精确匹配/模式匹配和最近验证。
- Dry-run 模式：只比较 current ↔ shared 双向未归档会话，返回新增与重复数量，不写入任何 Codex 文件；页面内确认后才进入同步 mutation。
- 切换模式：点击后在大会话扫描前先通过 Tauri `Channel` 发送真实 `planningSessions`，再解析计划、检查受管 roots 不重叠并按窄状态写集预检容量；relay 目标还要验证 `/models` 中存在配置的精确 model ID。这些失败均发生在关闭 ChatGPT 前。随后用 ToolHelp 识别并关闭受管 `ChatGPT.exe` / `OpenAI.Codex.exe` 进程树；若检测到独立 `codex.exe` CLI 则提示用户自行关闭并 fail closed，绝不结束 CLI。确认 ChatGPT 完全退出后校验 `process_manager/chat_processes.json`，仅在无法解析为 JSON 数组时原子重建为空数组并复核。changed switch 始终创建 current `RuntimeState` + shared `StateOnly`，不再把 GiB 级会话正文复制到检查点；双向证明 JSONL/index/provider 都无需写入时冻结 `SessionFileWritePolicy::Deny`，否则使用零 in-place 的 `Allow`。Create 原子 no-clobber 发布；允许替换时，严格扩展和分叉来源都按稳定 source SHA-256 生成完整 imported JSONL，旧目标 bytes/hash/mtime 不变。关闭态从 shared 写回 current 时还会核对 SQLite 与 JSONL 首条 `session_meta.payload.model_provider`；不一致或 legacy imported 文件名不被 Remote 枚举时，发布 provider 已归一、文件名符合 `rollout-<timestamp>-<thread-id>.jsonl` 的完整副本并优先作为活动路径，相同内容/provider 幂等复用。备份后做最后写前复检，再把 current 会话写入 shared，原子应用 auth/config overlay，从 shared 写回 current，最后要求 `confidence = exact`。失败补偿恢复 config/SQLite，最多留下未被引用的完整新 JSONL，不宣称 bit-exact 文件回滚。成功、完整回滚或 typed `Failed + Backup` 写入前失败只有在终态日志落盘和强 hash 复检后才进入 `cleaningCheckpoints` 释放本次一根/两根临时点；Apply/回滚/日志失败或复核异常时保留。只有认证、模型、provider 与 Relay Base URL 均 exact 时才 no-op；mode-only 必须重新应用。
- API 中转站模式：用户只配置一个中转站，保存 base URL、model 和 API key；URL 必须是 HTTP(S)，不得内嵌凭据、query 或 fragment，未写 scheme 时补 `https://`，远程地址必须使用 HTTPS。首次保存必须有 Key；后续只有 origin 不变时空 Key 才保留旧密文，改变 scheme/host/port 必须输入新 Key。配置使用页面内可访问 `<section>`，打开后聚焦标题，保存成功或取消后销毁 Key 并恢复触发点焦点，保存失败保留本次输入。连接验证用 rustls 客户端携带 Bearer 请求 `<base>/models`，10 秒超时、禁止重定向并限制响应大小；模型列表必须非空且包含配置的精确 ID，错误不回显 Key 或响应正文。
- 会话同步模式：单独点击“立即同步”允许 ChatGPT 正在运行；async Tauri command 把容量扫描、备份和合并整体放入 `spawn_blocking`。后端按 current/shared 双 `StateOnly` source 预检容量并创建窄临时点，不复制 `sessions/` payload。同步采用 JSONL-first：只同步存在有效正文 JSONL 的未归档会话，跳过 DB-only 孤儿和 `archived_sessions/`；SQLite 目标使用 2 秒 busy timeout、事务和 `PRAGMA quick_check`。source 在执行期校验完整尾行、session ID、长度和 SHA-256 前后稳定，只做有界重试。JSONL 生产路径零 in-place：Create 原子 no-clobber；允许替换时的严格扩展与 Divergent 都发布按稳定 source hash 命名的完整 imported JSONL，旧目标不改。current→shared 使用 `SelectMostComplete`；只有 shared→live current 使用 `PreserveExisting`：一旦 target 已有该 thread，就在 `existing_thread_rollout_path` 和 `copy_rollout_file` 前直接 duplicate+continue，target candidate 不读取、不写入、不发布，`copied_session_files = 0`，原 SQLite `rollout_path`、provider、title 与旧 writer 可见性保持不变，也不会创建 imported orphan。重复热同步因此不会为既有会话增加 C 盘文件占用或无效文件 I/O。新 thread 仍在事务中插入，沿用 source row 的可用字段并把 provider 归一为 current provider。live current index 明确 `Skip`；工具独占的 shared index 以完整 merged bytes 同目录 `atomic_write`。`Deny` 零写，`Unchanged` 返回前复检。同步成功或 typed `Failed + Backup` 写入前失败在终态落盘后释放相应一根/两根；Apply/回滚/日志失败保留。Apply 失败只补偿工具内部 shared SQLite，不用旧状态覆盖 live current；status 仍为 Failed。同步不替代用户主动创建的 full backup。
- 会话管理模式：默认展示 current + shared 合并视图，current 的显示元数据优先；支持搜索、排序、每页 50 条和跨页选择。所有硬删除都使用页面内确认，确认区打开后聚焦标题，取消或成功后恢复触发点；后端先按真实 Full source 预检容量，再关闭并复检 ChatGPT，并对独立 CLI fail closed。hard delete 会在两根创建覆盖四个受管数据库、`sessions/`、`archived_sessions/` 与索引的 Full 快照，最后写前再次复检进程，再用 Immediate 事务删除 `threads` 与相关边表并原子更新 active/archived JSONL/index；跨根部分失败恢复两份快照。`恢复可见` 创建单根 `StateOnly`，更新 current 的 `archived = 0` / `archived_at = NULL`；成功终态落盘后强复检并自动释放该临时点。shared-only 选择不执行无效恢复。
- Skill 模式：固定管理 `image2` 与 `grokSearch` 两个 wire ID，分别落到 `CODEX_HOME\skills\newapi-image2-client` 与 `CODEX_HOME\skills\grok-search`。Image2 来源 ZIP 的审查 SHA-256 固定为 `648C192C2414BBFD9DBA36E264C01932BDCF7E2057A8BA2DA7006B40A94B332B`；Grok 包移除本机路径、endpoint 和配置。包体通过 `include_bytes!` 进入 EXE，不在运行时下载或复制管理员目录。安装/更新由后端关闭并复检 ChatGPT，同时持有 mutation guard；本地漂移/未知目录需页面内确认，旧目录先移动到同卷备份再原子激活 stage；原子 journal 让下次安装能处理进程中断留下的 stage/backup/live 组合。URL/Key 配置由用户输入，Key 仅保存 DPAPI 密文；首次必填、后续空值保留；离开技能页会立即卸载确认/配置区并销毁局部 Key，取消或成功后恢复触发点焦点。
- 更新模式：每次应用启动只在当前进程内发起一次非阻塞检查，手动入口可再次触发；不注册后台服务、不在运行中轮询。Rust 后端固定请求本仓库最新正式 GitHub Release，8 秒超时、禁止重定向、限制 256 KiB、拒绝 draft/prerelease/非法 SemVer。发现新版后用户点击一次“立即更新”：更新先取得全局 mutation guard，UI 双向禁用其他写操作；后端只接受唯一固定名称的 Windows x64 `codex-switch.exe`，校验资产 URL、大小和 GitHub SHA-256 digest。v0.2.1 下载连接超时为 30 秒、总下载超时为 10 分钟；helper readiness 超时执行 kill + wait，staging 清理使用有界重试。staging 名使用 Windows CSPRNG，目录按进程是否 elevated 应用受限 DACL并持有目录句柄；helper 持有 canonical target lease，并以阶段 journal、旧/新 hash 和 Tauri `RunEvent::Ready` ACK 决定完成或回滚。已发布 v0.2.0 的首跳下载器仍是 120 秒总超时，`v0.2.0 -> v0.2.1` 只能由真实一键更新 smoke 证明；10 分钟合同从 v0.2.1 后续更新开始生效。
- 完整备份、检查点空间与恢复模式：用户可从运行态页创建 current/shared 两根 Full；后端在 blocking worker 内持有 mutation guard，容量预检后关闭并复检 ChatGPT、拒绝独立 CLI，并返回两份已验证快照。Full 覆盖四个受管数据库、`sessions/`、`archived_sessions/` 和索引；Dashboard 只在用户请求时按时间从新到旧验证候选，最多返回 256 个有效恢复点供逐份恢复或删除。列表和删除共用 managed-full 强校验；未声明 extra file、payload 大小/hash drift、路径或 manifest 异常的目录不会显示为 verified，也不会被自动删除。页面内“安全释放”严格解析操作日志：v3 接受成功/完整回滚双根、typed `Failed + Backup` 写入前失败的一根/两根，以及恢复可见成功的单根 `StateOnly`；legacy v2 只兼容旧成功/完整回滚双根。operation ID、时间窗、reason、canonical source root、scope 和目录引用均须精确；cleanup plan 与 execute 都重新强校验 manifest、文件集合、大小与完整 payload SHA-256，计划后任何漂移都 fail closed，不按年龄、数量、mtime 或容量阈值猜测。Summary、Receipt 和新日志都记录 `attemptedCount` / `failedCount`：只有进入 plan 后执行期 revalidate/remove 失败才是 partial/Failed；Full、孤儿、unclassified 等保留 warning 只作说明。真正恢复时再次强校验并创建 Full safety snapshot，按备份内 config 恢复 active/archived 文件与数据库。手工 Full、hard delete/restore safety 不自动删除；已验证 Full/legacy v2 可通过页面内“删除恢复点”显式确认，命令再次强校验、回报释放字节并写 `deleteBackup` 审计。Apply/回滚/日志失败、孤儿和无证据目录保持不动。

## 3. 配置加载链路

配置来源：

- Codex home 默认路径：优先 `CODEX_HOME`，否则 `%USERPROFILE%\.codex`，最后兜底 `HOME/.codex`；后续实现应允许高级设置覆盖。
- Codex SQLite home：优先读取 live `config.toml` 的 `sqlite_home`，其次读取环境变量 `CODEX_SQLITE_HOME`，最后回落到 Codex home。共享会话池内部固定使用自身目录，不继承用户侧 `sqlite_home`，避免共享池写到真实用户目录。
- 登录态：`auth.json`，含敏感 token，只能加密保存和脱敏展示。
- Codex 配置：`config.toml`，切换登录态或 API provider 时默认保护全局配置，只 patch 账号 / provider 绑定项。
- 工具配置：放在 `%APPDATA%\codex-switch`，保存运行态、备份、共享会话池、Skill 非敏感 config、Skill DPAPI credential 和 `logs\operations.jsonl`。

敏感字段不得在日志、UI、文档或错误报告中明文输出。

## 4. 核心流程

所有 mutation 使用同一语义：`preflight -> plan/dry-run -> backup -> apply -> verify -> persist terminal -> receipt/cleanup`；关闭态跨文件/跨根 mutation 发生失败时进入 `rollback -> verify rollback -> persist terminal -> receipt/cleanup`。临时点只有成功、完整回滚、typed `Failed + Backup` 写入前失败或恢复可见成功，且终态日志落盘、cleanup plan 与 execute 两阶段完整 SHA 复检通过时才释放；Apply/回滚/日志失败和证据不足时保留。切换补偿 current/shared config/SQLite；热同步只补偿 shared SQLite，不用旧状态覆盖 live current。实际进入 Create/Import 的路径先发布完整新文件，closed/current→shared 才可在发布后推进既有 SQLite 引用；hot shared→current 的 existing + `PreserveExisting` 在任何 target candidate 文件处理前直接跳过，既不发布文件也不产生 orphan。跨 SQLite/JSONL/index 仍不是单一 durable transaction，也仍不能宣称 bit-exact 回到操作前。

1. 启动与扫描：`loadRuntimeDashboard` 只并发加载 Codex Home 摘要、固定运行态、当前运行态和最近操作，并用 `Promise.allSettled` 保留各域真实错误；进入会话页时调用 `loadSessionDashboard`，备份由用户触发 `loadBackupDashboard`。独立启动 effect 同时读取应用版本、上次更新重启状态并异步检查更新，不阻塞首屏。备份域按需执行与删除相同的 managed-full 强校验，最多返回 256 项，再读取操作记录并调用持 mutation guard 的 `inspect_checkpoint_storage`；extra/hash/path/manifest drift 候选不进入 verified 列表。空间扫描只量取目录并核对 manifest/终态，不把可恢复列表的 payload 强校验扩散到首屏。运行态切换和普通会话同步都把容量/relay 校验、scoped 快照、SQLite/文件同步与终态处理作为阻塞工作单元放入 Tauri blocking worker，避免大型会话池占用异步 IPC/UI 事件线程；后端 `Channel`、mutation guard、日志和终态保持同一操作顺序。所有数据 mutation 与一键更新安装先获取进程内 try-lock，再持有 Windows 独占 `%APPDATA%\codex-switch\mutation.lock` 文件句柄。
2. 计划：所有会创建快照的 mutation 都把每个 `home + CodexPaths + BackupScope` 作为显式 source，在第一份新快照前校验 roots，累加实际 payload、DPAPI/manifest 开销，使用最大的 SQLite workspace，并保留至少 2 GiB 或 15% reserve；overflow、路径冲突和容量查询失败均 fail closed。changed switch 固定规划 `RuntimeState + StateOnly`，普通同步固定规划双 `StateOnly`；Full/Sessions 纳入 `archived_sessions/`，runtime/state scope 不扩大。relay 验证和关闭态容量检查在关闭 ChatGPT 前完成。
3. 备份：`backup` 生成 manifest v3，并显式记录 `scope` 与 `trackedDatabases`。changed switch 与普通同步的窄临时点不包含 active/archived JSONL；hard delete、手工 Full 和 restore safety 覆盖 `state_5.sqlite`、`goals_1.sqlite`、`memories_1.sqlite`、`logs_2.sqlite` 四库以及 active/archived 会话文件。纳入 scope 的 SQLite 均通过 Online Backup，所有 payload 均用当前 Windows 用户 DPAPI 加密并记录大小/SHA-256。创建过程中失败时会移除 partial 目录；若该清理也失败，返回错误必须同时指出残留，不能吞错。v2 Full 仍可恢复和显式删除，legacy 自动回收只兼容严格旧成功/完整回滚双根。
4. 应用：需要整文件替换的配置、备份和可写 index 使用同目录完整临时文件、flush/sync 和原子替换；Windows 在调用 `MoveFileExW` write-through 前，先把已存在的父目录规范化为绝对 verbatim 路径并解析目录联接，以支持超过传统 `MAX_PATH` 的深层备份临时路径。会话 source 在执行期验证完整尾行、session ID、长度和 SHA-256 前后稳定，只允许有界重试；Create/Import/关闭态 provider rollout 在临时文件完成 provider 归一和同步，再用 hard-link no-clobber 发布。允许替换时的严格扩展和 Divergent 都发布完整 hash-named imported JSONL，旧目标不改；provider rollout 使用 Remote 可识别的官方文件名形态并幂等复用。closed/current→shared 的 `SelectMostComplete` 可在发布后切换 SQLite；hot shared→current 的 existing + `PreserveExisting` 在 `existing_thread_rollout_path` / `copy_rollout_file` 前直接跳过，零 target candidate I/O、零 orphan、`copied_session_files = 0`，仅 hot 新 thread 进入发布与事务插入。hot current index `Skip`，closed/app-owned shared index 原子整文件替换，`Deny` 零写且 `Unchanged` 终前复检。Skill 包先写同卷 stage，逐文件验 hash；覆盖时把旧目录移动到当前 Codex Home 的 `.codex-switch\skill-backups`，再 rename stage。
5. 验证：备份/恢复检查大小和 SHA-256，并对 manifest `trackedDatabases` 执行适用的 `PRAGMA quick_check`；full 范围覆盖四个受管 SQLite，局部范围不得声称完整可恢复。运行态切换要求 exact；Skill 安装重新扫描 package manifest、固定 allowlist 和文件 SHA-256；关闭态高风险 mutation 使用 ToolHelp 在入口与最后写前重新枚举，确认受管 ChatGPT 进程树和独立 Codex CLI 均已关闭，且只允许结束受管树。
6. 回执、操作记录与检查点终结：成功结果返回 typed receipt（operation ID、备份、计数、rolledBack、warnings、checkpoint cleanup）；失败返回脱敏错误，各类终态先写入本地 `operations.jsonl`。即时与显式清理都先强复检 manifest/path/file set/size/hash；v3 支持成功/完整回滚双根、typed `Failed + Backup` 写入前失败的一根/两根和恢复可见成功单根，legacy v2 只支持旧成功/完整回滚双根。cleanup Summary/Receipt/日志统一记录 `attemptedCount` 与 `failedCount`；仅执行期 revalidate/remove 失败使 `failedCount > 0`、页面 partial、日志 Failed，纯保留 warning 仍是 Succeeded/Complete。持久 Full 删除使用独立 `deleteBackup` action、页面内显式确认和重新强校验。切换通过 `Channel` 发送 `planningSessions`、`cleaningCheckpoints` 等真实阶段；手工 cleanup 只显示单一诚实运行态。前端在挂载时预注册关闭门禁，切换后 runtime refresh pending 完成前保持切换禁用；mutation、刷新和 unknown 终态使用分离但不重复的页面错误面，Relay 验证失败只由 mutation 管道呈现一次。备份刷新进行中若收到 mutation 后请求，会排队补跑最新扫描。

## 5. 模块与边界

当前模块边界：

- `codex_paths`：统一解析 Codex home、`sqlite_home`、会话正文目录和 `session_index.jsonl`，封装用户真实路径与共享池本地路径的差异。
- `codex_home`：路径发现、文件探测、运行态检测。
- `runtime_store`：固定账号/relay 双槽位、损坏 metadata fail-closed、DPAPI 认证存储、无碰撞覆盖历史和三文件失败回滚验证；不保存无关 live 全局配置。
- `crypto`：Windows DPAPI protect / unprotect。
- `process_control`：ToolHelp PID/PPID 进程树检测；只管理 `ChatGPT.exe` / `OpenAI.Codex.exe` 受管根，独立识别但绝不结束 standalone `codex.exe` CLI；受管树先温和关闭，并在超时后对重新验证身份的残留进程强制兜底。
- `chat_process_state`：只在运行态切换已确认 ChatGPT 完全退出后校验其瞬态进程记录；合法数组保持原字节，缺失文件不创建，损坏或错误结构通过同目录原子替换重建为 `[]` 并复核。
- `runtime_switcher`：冻结双向会话文件写策略，changed switch 固定选择 `RuntimeState + StateOnly` 双根窄检查点；关闭态双向使用 `SelectMostComplete`，普通热同步 current→shared 也使用该策略，只有 hot shared→current 使用 `PreserveExisting`；同步使用 `Deny` 或零 in-place 的 `Allow`，并负责 auth/config overlay 原子应用、exact 后置校验和 config/SQLite 失败补偿。
- `config_patch`：TOML 解析、保护字段、账号覆盖、diff。
- `session_scan`：按解析后的 `sqlite_home` 做 SQLite 只读扫描、dry-run 去重，并扫描 Codex home 下的 JSONL 正文。
- `session_sync`：JSONL-first allowlist、SQLite 事务/quick_check、数据库活动 rollout 优先级和语义化增长/分叉处理；source 在执行期校验完整尾行/session ID/len/hash 稳定并有界重试。JSONL 生产路径零 in-place：Create/Import/provider rollout 使用预归一的 atomic hard-link no-clobber，旧文件 bytes/hash/mtime 不变。关闭态 provider rollout 同步 SQLite 与首条 JSONL provider，并使用 Remote 可识别的官方文件名形态；相同内容/provider 幂等复用。`SelectMostComplete` 用于 current→shared 与关闭态；`PreserveExisting` 只用于 hot shared→current，existing thread 在 rollout path 查询/复制前直接 duplicate+continue，零 target candidate I/O、零 orphan、`copied_session_files = 0`，原 rollout/provider/title 和旧 writer 可见性保留；新 row 正常插入。hot current index `Skip`，closed/app-owned shared index 以完整 merged bytes 原子替换；`Deny` 零写，`Unchanged` 终前复检。为切换提供 `Deny/Allow`，不承担 bit-exact 会话文件回滚。
- `session_manager`：current/shared 合并视图、active/archived 会话发现、所有硬删除确认、Immediate 事务、双根 Full 补偿和 current-only 可见恢复；硬删除同时处理两根的 `sessions/` 与 `archived_sessions/`。
- `backup`：manifest v3 scoped payload、SQLite Online Backup、容量/root 冲突预检、active/archived Full/Sessions 范围、强校验/精确恢复和 legacy plaintext auth 迁移；负责临时点即时/显式释放与持久 Full 显式删除。临时清理支持严格 v3 成功/完整回滚、typed prewrite 一根/两根与恢复可见单根，legacy v2 只兼容旧成功双根；删除前复核完整 payload hash，不做年龄/数量/mtime猜测。cleanup 计划数、执行失败数、回收数/字节和保留数分开统计，保留 warning 不进入失败数；partial 创建清理失败必须上报。
- `file_ops`：同目录临时文件、同步、平台原子替换，以及 hard-link no-clobber `atomic_create` 的统一实现。
- `relay_verify`：relay `/models` 脱敏验证；仅 HTTPS/loopback HTTP，响应最多 4 MiB，列表必须非空且包含配置的精确 model ID。
- `operation_log`：追加式结构化终态记录；普通历史读取容忍最后一条截断，检查点回收使用全文件严格解析，并记录 `cleanupCheckpoints` / `deleteBackup` 结果。新 cleanup 记录包含 `attemptedCount` / `failedCount`，只在计划项执行期失败时 status 为 Failed；旧候选曾因 warning 误记的历史 Failed 记录原样保留。
- `skill_manager`：固定 Skill 包、manifest/hash/漂移状态、路径/reparse 校验、同卷 stage/备份/原子激活、URL 归一化和 DPAPI 配置存储。
- `update_check`：固定 GitHub latest-release 读取、stable SemVer 验证、响应大小/超时/重定向边界、并发检查 guard 和限长说明。
- `update_install`：固定 Release EXE 资产选择、GitHub digest/大小/URL 校验、30 秒连接/10 分钟总下载超时、CSPRNG staging、受限 DACL、目录句柄、canonical target lease、阶段 journal、`RunEvent::Ready` ACK、helper kill + wait、cleanup retry、失败回滚和 elevated 无参数恢复发现 fail-closed。
- `commands`：Tauri command 薄边界，负责固定路径/槽位、进程门禁、恢复选择限制、typed receipt、终态日志后临时点清理、检查点 inspect/cleanup，以及按需最多 256 项的已验证 Full 列表与 `delete_backup`；列表/删除共用 managed-full 强校验，不允许前端传任意 Codex 路径。cleanup terminal 只由 `failedCount` 决定，warnings 不再被当作失败代理。

## 6. 输出文件与状态流

工具自身输出目录：`%APPDATA%\codex-switch`。

- `runtimes/plus/runtime.json`：ChatGPT 账号态元数据，不含敏感明文；目录名 `plus` 是内部兼容 ID。
- `runtimes/plus/auth.enc`：加密后的 ChatGPT 账号态 `auth.json`。
- `runtimes/plus/config.toml`：账号态 runtime overlay，只含切换需要的模型/provider 绑定。
- `runtimes/relay/runtime.json`：单个 API 中转站元数据，不含 API Key。
- `runtimes/relay/auth.enc`：加密后的中转站 API Key 登录态。
- `runtimes/relay/config.toml`：中转站 runtime overlay，包含 `model_provider = "openai_custom"` 与 provider 连接参数，不含无关 live 全局配置、明文 API Key、`env_key` 或 `goal`。
- `runtimes/<slot>/history/<timestamp>[-<sequence>]/`：覆盖前的完整槽位三件套；同毫秒归档使用递增后缀避免碰撞。
- `shared-sessions/state_5.sqlite`、`shared-sessions/sessions/`、`shared-sessions/archived_sessions/` 与 `shared-sessions/session_index.jsonl`：ChatGPT 账号态与中转站共用的会话池；普通同步只处理活跃正文，管理/Full 路径覆盖两类目录。
- `backups/<timestamp>-<pid>-<counter>-<reason>/manifest.json`：v3 的 scope、`trackedDatabases`、来源、大小与 SHA-256 元数据；同目录 `.enc` 文件是 DPAPI 加密 payload。switch/sync/restore-visible 局部目录是终态前临时点，Full 和失败/无证据目录持续保留；只有通过直接子目录、manifest、精确文件集合、大小与 hash 强校验的 Full/legacy v2 才进入最多 256 项的按需列表并可显式删除，extra/hash drift 目录保留但不标 verified。
- `logs/operations.jsonl`：追加式脱敏操作记录；不保存凭据、请求/响应正文或自由文本错误。检查点空间清理严格解析完整文件，以 action/status/phase 和目录引用形成证据；持久恢复点删除记录独立 `deleteBackup` action。
- `skills/image2/config.json`、`skills/grok-search/config.json`：位于 `%APPDATA%\codex-switch` 下的非敏感 URL、固定模型和 credential 路径；不含 Key。
- `skills/<skill>/credential.enc`：当前 Windows 用户的 DPAPI 密文，经 Base64 供安装后的 PowerShell helper 解密；UI 只返回配置布尔状态。
- 当前 `CODEX_HOME\skills\newapi-image2-client` / `grok-search`：受管 Skill 文件和 `.codex-switch-package.json`；不含用户配置或 Key。
- 当前 `CODEX_HOME\.codex-switch\skill-backups\<operation-id>`：覆盖前的完整旧 Skill 目录；当前不自动清理。
- 当前 `CODEX_HOME\.codex-switch\skill-transactions\<skill>.json`：目录 swap 的原子恢复 journal；成功后删除，异常残留由下一次该 Skill 安装先恢复。

## 7. 常见故障归因

- 登录态切换后不生效：先查页面任务执行器停在哪个真实阶段、ChatGPT 受管进程树是否仍在运行、容量/root 预检、relay `/models` 验证、操作 ID/终态、`auth.json` 修改时间，以及运行态检测是 exact、mode 还是 unknown；mode-only 需要重新应用。
- 会话列表缺失：先查活跃库是否真在 `sqlite_home/state_5.sqlite`，再查 `threads.rollout_path` 指向的 JSONL 是否存在、`session_index.jsonl` 是否包含对应会话，最后查 `threads.model_provider` 是否与当前 `config.toml` 的 `model_provider` 一致；ChatGPT App 会按 provider / service 过滤，会话正文 JSONL 中也需要同步 `session_meta.payload.model_provider`。
- 归档会话没有同步回来：这是预期行为；归档会话只在会话管理页可见并可主动删除或恢复可见，不会被自动同步或清理。
- 同步重复或历史突然变短：查共享会话池、`threads.id`、SQLite 当前 `rollout_path`、同 ID 的全部 JSONL 及其严格前缀/分叉关系。hot shared→current 对既有 thread 的预期结果是文件处理前直接跳过、`copied_session_files = 0`、DB 继续指向旧 active 且旧 writer 后续尾部可见；如果此分支后观察到 target candidate 文件，应从其他 Create/Import 操作或历史残留继续归因。只有 current→shared/关闭态 `SelectMostComplete` 才会在发布后推进活动路径；关闭态单纯 provider 不同会发布 provider rollout，但它不是正文冲突副本，并应在后续相同内容/provider 切换时复用。
- SQLite 写入失败：查 `sqlite_home` 严格解析结果、schema、2 秒 busy timeout、事务/quick_check、操作回执和两根备份。
- 配置损坏：无效 live TOML 会阻断路径解析/写操作；查解析错误、操作记录和最近已验证备份，不允许静默回落到默认数据库。
- Windows 原子替换报 `os error 3`：不一定是 live 目录丢失。完整加密备份会在深层 payload 目录生成更长的同目录临时文件；旧实现把超过传统 260 字符限制的普通路径直接传给 `MoveFileExW` 时也会报“找不到路径”。先核对备份目标与临时路径长度、应用版本和父目录是否可解析，不要直接重建或删除 Codex Home；修复后的实现使用规范化 verbatim 父路径并兼容目录联接。
- 操作成功但页面提示错误：mutation、runtime 刷新和 unknown 终态使用不同页面状态。成功后“正在确认当前运行态”期间再次切换会被禁用；刷新失败不改写 mutation 成功。未收到可信终态时先不要重新打开 ChatGPT，按 operation ID 与日志确认，不要重复点击。
- C 盘备份目录持续增长：先在运行态页加载备份域，核对“目录占用 / 可证明回收 / 安全保留”和警告。页面内“安全释放”只处理严格终态证明的临时点；按需列表最多展示 256 个通过 list/delete 同一强校验的 Full/legacy v2，可逐项“删除恢复点”。extra/hash drift、失败、孤儿或无证据目录不会显示 verified，也不能手工按日期、mtime 或目录数量批量删除。2026-07-26 受控 UI 实证为 `21` 目录/`6,327,089,609` bytes → `17` 目录/`2,693,977,957` bytes，计划项 `4/4` 删除、回收 `3,633,111,652` bytes，紧邻测得 C 盘空闲增加 `3,637,547,008` bytes；首次旧候选 UI 的 warning→partial/Failed 历史保留不改，当前语义按 `failedCount` 判定，剩余 17 项不自动删除。
- Skill 已安装但不可用：先检查页面是否显示“API Key 已加密配置”，再确认重启过 ChatGPT / Codex CLI；Image2 URL 必须以 `/v1` 结尾，Grok URL 在保存时会移除末尾 `/v1` 并由脚本调用 `/v1/responses`。不要读取 credential 文件排查明文。
- Skill 显示“本地已修改 / 非受管目录 / 安装异常”：工具不会静默覆盖；确认目录确实可被接管后使用覆盖安装，并保留回执中的旧目录备份。

## 8. 测试与验证

当前真实可用验证命令：

- `npm test -- --run`：Vitest 前端 Dashboard、会话与技能页测试。
- `npm run typecheck`：TypeScript 静态检查。
- `npm run build`：Vite 前端生产构建。
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`：Rust 格式门禁。
- `cargo clippy --locked --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`：Rust lint 门禁。
- `cargo test --locked --manifest-path src-tauri/Cargo.toml`：Rust 后端单元测试。
- `cargo test --manifest-path src-tauri/Cargo.toml --test skill_manager_contract`：临时 `CODEX_HOME` Skill 安装、漂移、备份、URL、DPAPI 与 Windows PowerShell 兼容合同。
- `cargo test --manifest-path src-tauri/Cargo.toml --test synthetic_large_home`：隔离的多文件 provider 切换回归，验证原 JSONL 的 bytes/SHA-256/mtime 不变、首次关闭态切换发布 Remote 可识别 provider rollout、SQLite/JSONL provider 一致以及重复切换零新增；ignored benchmark 可用 `CODEX_SWITCH_SYNTHETIC_SESSION_COUNT` / `CODEX_SWITCH_SYNTHETIC_SESSION_KIB` 放大。
- `npm run tauri -- build`：完整 Tauri 桌面应用 release 编译，当前输出 `src-tauri\target\release\codex-switch.exe`。
- `npm run check:release`：校验五处版本、tag（如适用）、PE 格式/大小及构建机路径与常见 token marker。
- `.\scripts\pack-windows-release.ps1 -UpxPath "<verified-upx.exe>"`：固定验证官方 UPX 5.2.0 与 `upx.exe` SHA-256 `F4C0CC7ACA0F1FF0D0B750E966B44139F2FA1A2DB7281F48FC52194400712E1D`，冻结并验证 raw hash/contract，只复制到 staging 后执行 `--best --lzma`，再执行 `upx -t`、packed contract、PE32+ x64/双版本与 3,000,000 bytes 硬门禁；raw 不原地修改。

Cargo release profile 固定 `opt-level = "z"`、`lto = true`、`codegen-units = 1`、`panic = "abort"`、`strip = "symbols"`。`.github/workflows/ci.yml` 在 `windows-latest` / Node 22 / 固定 Rust 1.94.1 上执行前端/Rust 门禁并构建 raw Tauri EXE；随后从官方固定 URL 下载 UPX 5.2.0 ZIP，验证 ZIP SHA-256 `B471EBF1B7F20F4A89150264ED9A008A2A5BFD247F3C6D1184A75BB59CA08F5D` 与 EXE SHA-256 `F4C0CC7ACA0F1FF0D0B750E966B44139F2FA1A2DB7281F48FC52194400712E1D`，调用统一脚本，并只上传 packed 的 `release/codex-switch.exe`。本地临时候选 raw `5,955,584` bytes → packed `2,228,224` bytes（SHA-256 `4DDC…CED7A`）只证明本地链路，不能当作最终 tag-CI Release 证据。

## 9. Release 发布链路

当前公开 release 采用 GitHub Releases + Windows 单文件可执行产物：

1. 同步版本号：`package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json`。
2. 同步公开文档：`README.md` release badge / 功能说明、`CHANGELOG.md`、根目录长期文档。
3. 运行完整验证：`npm test -- --run`、`npm run typecheck`、`npm run build`、Rust fmt/clippy/test、必要的临时 `CODEX_HOME` E2E/故障注入/恢复演练、敏感扫描、乱码检查和 `npm run tauri -- build`；对 raw EXE 运行 release contract。
4. 使用固定官方 UPX 5.2.0 调用 `scripts/pack-windows-release.ps1`，证明 raw hash 未变、`upx -t` 成功、packed 仍为 PE32+ x64、双版本一致、release contract 通过且不超过 3,000,000 bytes。
5. 提交并推送版本提交。
6. 创建与 manifest 一致的版本 tag，例如 `v0.2.1`，并推送 tag。
7. 从对应 tag commit 的成功 CI run 下载唯一 packed artifact，用 `gh release create` 或 `gh release edit` 发布；不得上传 raw EXE，也不得另行上传未经相同门禁验证的本地产物。
8. 用 `gh release view <tag>` 和 `git ls-remote --tags origin <tag>` 验证远端 release/tag，重下载唯一资产并复核大小、SHA-256、PE32+ x64 与双版本，再做真实旧版一键更新。

应用内检查与一键更新依赖该链路的正式 release/tag/资产版本一致性。公开 UI/窗口标题使用 ChatGPT Switch，但 Release 必须包含唯一 packed `codex-switch.exe` 资产和 GitHub 生成的 SHA-256 digest；v0.1.9 updater 固定校验该名称，不能改为 `chatgpt-switch.exe`。不得只推 tag 不建 Release，不得上传 raw/packed 两份资产，也不得让 Release tag、Rust crate、Tauri config 和 npm 版本漂移。

## 10. 发布前剩余门禁

- 必须从干净工作区实际运行本节列出的前端/Rust门禁；不能只依赖历史结果或 CI 配置。
- 临时 `CODEX_HOME` E2E 至少覆盖账号态 → relay → 账号态、双向同步幂等和完整备份恢复；不得用真实 `.codex` 作为首轮写入目标。
- 故障注入至少覆盖运行态文件写入后失败、跨 current/shared 删除部分失败和恢复失败的安全快照补偿。
- 敏感扫描必须覆盖 UI 构建产物、操作记录、备份 manifest 和错误文本；不得出现 token/API Key/响应正文。
- 中文乱码检查必须覆盖本轮修改的 Markdown、TSX 和 Rust 文案。
- 已知产品残余：临时点已有终态驱动清理，已验证 Full 可逐个显式删除，但持久恢复点仍没有自动 retention/pin 分组；失败、孤儿和无证据目录也不会自动删除。容量预检与“可证明回收”不能被误述为所有旧备份都安全。
- 已知更新安全残余：helper 已有受限 staging、目录句柄和持久化阶段 journal，但 GitHub digest 与 Release 元数据仍来自同一控制面，尚无独立固定公钥/Authenticode 信任根。引入发布签名基础设施前不得宣称能抵抗仓库发布权限失陷。
- 已知一致性残余：热同步仍不是跨 SQLite/JSONL/index 的单一 durable transaction。source 稳定验证和完整 JSONL atomic no-clobber 发布保证实际 Create/Import 不会留下半尾；hot shared→current 对既有 thread 在 target path 查询/复制前直接跳过，不创建 candidate/orphan，并保留旧 writer 可见性。closed/current→shared 才由 `SelectMostComplete` 在完整发布后推进引用；SQLite 与文件仍没有外部 ChatGPT 共同尊重的跨介质事务。
- 已知首跳更新残余：v0.2.1 已把下载连接/总超时扩展到 30 秒/10 分钟，但已发布 v0.2.0 的下载器仍是 120 秒；`v0.2.0 -> v0.2.1` 在发布前必须做真实一键更新 smoke，不能由 v0.2.1 单元测试替代。
- 已知发布候选残余：当前 `5,955,584` / `2,228,224` bytes 与 `4DDC…CED7A` 仅是本地临时 raw/packed 证据；最终 tag-CI hash/尺寸、Release 重下载合同和真实 v0.2.0 首跳仍待发布后确认。
- 已知 updater 多实例残余：target-keyed OS lease 已覆盖 helper 的替换、ACK 与回滚窗口，并由启动清理再次取得；应用本身仍未实现 singleton，因此多个普通实例仍可同时打开，但同一路径的更新 helper 会 fail closed，不能并发替换同一 EXE。

<!-- DXM-TRELLIS:START -->

## DXM 大开发工作流链路

中大型开发、新模块、跨多文件重构、需求不清楚的任务，默认走：

1. 先用 `project-grill` 问清楚；有代码/文档时用 `grill-with-docs`，空项目用 `new-project-grill`，小脚本/demo 用 `lightweight-grill`。
2. 提问前先从代码和文档自行判断，能确定的是否题、偏好题、低风险取舍题作为推荐假设推进。
3. 把结论写入 `.trellis/tasks/<task>/prd.md`，不能只停留在聊天上下文里。
4. 用 `.trellis/scripts/task.py start <task>` 进入 Trellis active task。
5. 按 Trellis 的 implement/check/update-spec/finish 节奏开发、验证、沉淀规范。
6. 任务完成后同步 DXM 长期文档；不能只更新 `.trellis/` 内部状态。

<!-- DXM-TRELLIS:END -->

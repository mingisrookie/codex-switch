# 项目完整链路说明

> 项目：`codex-switch`
> 根目录：`<repo-root>`
> 初始化日期：`2026-06-23`

本文档说明当前项目从输入、配置、启动、核心处理到产出的完整链路。开发前应先读 `项目文件结构说明.md`，再读本文，最后对照 `项目开发规范（AI协作）.md` 做变更。

<!-- DXM-DOC-RULES:START -->

## DXM 文档维护规则

- 本块由 DXM 管理；`--refresh-blocks` 只刷新本块，保留下方项目事实和人工补充。
- 项目目标、运行模式、配置链路、故障归因等长期事实必须写入下方对应章节，不能只留在聊天或临时文件里。
- 敏感配置和运行态输出只写用途，不回显真实 token、密码、API Key、账号明细、验证码、会话或密钥内容。
- 修改中文内容后必须检查可见乱码、错码和异常替换字符。

<!-- DXM-DOC-RULES:END -->

## 1. 项目目标

`ChatGPT Switch`（仓库与 EXE 兼容名为 `codex-switch`）是本机 ChatGPT 运行态切换与多会话同步工具。

- 解决问题：在当前 ChatGPT 账号登录态与一个 API 中转站调用态之间安全切换，把本地历史会话同步成统一视图，并为用户的 ChatGPT/Codex 环境安装可配置的 Image2/Grok 能力。
- 主要用户：当前 Windows 主机上的 ChatGPT Desktop / Codex CLI 使用者。
- 成功产物：React + Tauri 桌面应用，具备固定双槽位、真实运行态判定、页面内真实切换阶段、受管进程树关闭、共享会话池热同步、会话管理、固定 Skill 安装/配置、DPAPI 凭据、失败补偿、typed receipt 和脱敏操作记录。
- 运行态输入：默认从 `CODEX_HOME` 解析 Codex home；未设置时使用 `%USERPROFILE%\.codex`。核心文件包括 `auth.json`、`config.toml`、`sessions\**\*.jsonl`、`session_index.jsonl`，以及位于 Codex `sqlite_home` 下的 `state_5.sqlite`、`goals_1.sqlite`、`memories_1.sqlite` 和 `logs_2.sqlite`。`sqlite_home` 优先读取 `config.toml`，其次读取 `CODEX_SQLITE_HOME`，最后回落到 Codex home；`sqlite\codex-dev.db` 只作为兼容探测项，不作为当前会话同步来源。
- 运行态输出：工具自身加密运行态、加密备份快照、共享会话池、操作记录以及面向 UI 的 dry-run/typed receipt。

## 2. 运行模式

当前已实现 / 已接入的运行模式：

- 桌面正常模式：Tauri 启动 React UI，顶部在“运行态 / 会话管理 / 技能”之间切换。首屏只加载 Codex Home 摘要、固定运行态、当前运行态和最近操作；会话域在首次进入会话页时加载，备份域由用户主动加载。各域保持独立 `loading | ready | error`，切换完成后昂贵域只标记 stale，不立即重新扫描。
- 固定双槽位模式：只管理 `plus`（ChatGPT 账号，内部兼容 ID）和 `relay`（API 中转站）两个槽位。账号导入必须验证 live `auth_mode = chatgpt`；覆盖使用页面内确认并归档旧的加密槽位文件。UI 分开显示已保存、当前精确匹配/模式匹配和最近验证。
- Dry-run 模式：只比较 current ↔ shared 双向未归档会话，返回新增与重复数量，不写入任何 Codex 文件；页面内确认后才进入同步 mutation。
- 切换模式：先解析计划、检查受管 roots 不重叠并按实际写集预检备份容量；relay 目标还要验证 `/models`。这些失败均发生在关闭 ChatGPT 前。随后用 ToolHelp 识别并关闭受管 `ChatGPT.exe` / `OpenAI.Codex.exe` 进程树，为 current/shared 创建已验证的 runtime / sessions scoped 快照，把 current 会话写入 shared，原子应用 auth/config overlay，再从 shared 写回 current，最后要求 `confidence = exact`。Tauri `Channel` 将真实阶段发送给页面任务执行器；失败区分写入前失败、已回滚和回滚失败。只有目标已 exact 时才 no-op；mode-only 必须重新应用。
- API 中转站模式：用户只配置一个中转站，保存 base URL、model 和 API key；URL 必须是 HTTP(S)，不得内嵌凭据、query 或 fragment，未写 scheme 时补 `https://`，远程地址必须使用 HTTPS。首次保存必须有 Key；后续只有 origin 不变时空 Key 才保留旧密文，改变 scheme/host/port 必须输入新 Key。配置使用页面内可访问 `<section>`，保存失败保留本次输入，保存成功或取消后销毁 Key。连接验证用 rustls 客户端携带 Bearer 请求 `<base>/models`，10 秒超时、禁止重定向并限制响应大小，错误不回显 Key 或响应正文。
- 会话同步模式：单独点击“立即同步”允许 ChatGPT 正在运行；后端先为 current/shared 创建 sessions-scope 快照，再双向同步。同步采用 JSONL-first：只同步存在有效正文 JSONL 的未归档会话，跳过 DB-only 孤儿和归档会话；SQLite 目标使用 2 秒 busy timeout、事务和 `PRAGMA quick_check`。同一会话 ID 存在多份 JSONL 时优先选择 SQLite 当前 `rollout_path`，仅沿严格追加关系推进到更完整版本；较短或分叉来源不得劫持目标活动路径。内容等价的既有 JSONL 不因 provider 变化而重写，bytes/mtime 保持不变，provider 可只更新 SQLite；仅复制、新建或增长替换的 JSONL 才归一元数据。热同步失败时只恢复 shared-sessions，live current Home 保持不动，并在错误中给出 current scoped 安全快照。
- 会话管理模式：默认展示 current + shared 合并视图，current 的显示元数据优先；支持搜索、排序、每页 50 条和跨页选择。所有硬删除都使用页面内确认，后端关闭并复检 ChatGPT；hard delete 会在两根创建覆盖四个受管数据库的 full 快照，再用 Immediate 事务删除 `threads` 与相关边表并原子更新 JSONL/index，跨根部分失败恢复两份快照。`恢复可见` 只创建覆盖其实际写集的 state-only 快照，再更新 current 的 `archived = 0` / `archived_at = NULL`；shared-only 选择不执行无效恢复。两类局部快照都不进入 UI 可恢复列表。
- Skill 模式：固定管理 `image2` 与 `grokSearch` 两个 wire ID，分别落到 `CODEX_HOME\skills\newapi-image2-client` 与 `CODEX_HOME\skills\grok-search`。Image2 来源 ZIP 的审查 SHA-256 固定为 `648C192C2414BBFD9DBA36E264C01932BDCF7E2057A8BA2DA7006B40A94B332B`；Grok 包移除本机路径、endpoint 和配置。包体通过 `include_bytes!` 进入 EXE，不在运行时下载或复制管理员目录。安装/更新由后端关闭并复检 ChatGPT，同时持有 mutation guard；本地漂移/未知目录需页面内确认，旧目录先移动到同卷备份再原子激活 stage；原子 journal 让下次安装能处理进程中断留下的 stage/backup/live 组合。URL/Key 配置由用户输入，Key 仅保存 DPAPI 密文；首次必填、后续空值保留。
- 更新模式：每次应用启动只在当前进程内发起一次非阻塞检查，手动入口可再次触发；不注册后台服务、不在运行中轮询。Rust 后端固定请求本仓库最新正式 GitHub Release，8 秒超时、禁止重定向、限制 256 KiB、拒绝 draft/prerelease/非法 SemVer。发现新版后用户点击一次“立即更新”：更新先取得全局 mutation guard，UI 双向禁用其他写操作；后端只接受唯一固定名称的 Windows x64 `codex-switch.exe`，校验资产 URL、大小和 GitHub SHA-256 digest。staging 名使用 Windows CSPRNG，目录按进程是否 elevated 应用受限 DACL并持有目录句柄；helper 持有按 canonical target EXE 派生的跨进程 lease，并通过持久化阶段 journal、旧/新 hash 和 Tauri `RunEvent::Ready` ACK 决定继续、完成或回滚。早退、超时、中断或状态不一致时恢复旧版本。
- 完整恢复模式：用户可创建 full backup；Dashboard 读取 manifest 后先排除 runtime / sessions / state-only 局部快照，再按 `createdAtMs` 对最近 5 个完整候选做 payload 大小/SHA-256 强校验。UI 只展示通过校验、可完整恢复的候选。真正恢复时后端再次完整校验，并只接受受管 backup root 的直接子目录，按 `sourceRoot` 恢复 current 或 shared；恢复前创建覆盖四个受管数据库的 full safety snapshot，失败时用该快照补偿。恢复按备份内 config 解析 `sqlite_home`、移除快照外文件，并按 `trackedDatabases` 执行 SQLite `quick_check`。当前没有自动 retention/prune，旧备份仍会累积。

## 3. 配置加载链路

配置来源：

- Codex home 默认路径：优先 `CODEX_HOME`，否则 `%USERPROFILE%\.codex`，最后兜底 `HOME/.codex`；后续实现应允许高级设置覆盖。
- Codex SQLite home：优先读取 live `config.toml` 的 `sqlite_home`，其次读取环境变量 `CODEX_SQLITE_HOME`，最后回落到 Codex home。共享会话池内部固定使用自身目录，不继承用户侧 `sqlite_home`，避免共享池写到真实用户目录。
- 登录态：`auth.json`，含敏感 token，只能加密保存和脱敏展示。
- Codex 配置：`config.toml`，切换登录态或 API provider 时默认保护全局配置，只 patch 账号 / provider 绑定项。
- 工具配置：放在 `%APPDATA%\codex-switch`，保存运行态、备份、共享会话池、Skill 非敏感 config、Skill DPAPI credential 和 `logs\operations.jsonl`。

敏感字段不得在日志、UI、文档或错误报告中明文输出。

## 4. 核心流程

所有 mutation 使用同一语义：`preflight -> plan/dry-run -> backup -> apply -> verify -> receipt`；关闭态跨文件/跨根 mutation 发生失败时进入 `rollback -> verify rollback -> terminal receipt/error`。热同步是例外：恢复内部 shared 根但不把旧快照写回正在变化的 live current Home。恢复可见是单条 SQLite 更新，写前仍备份，SQL 自身保持原子性。

1. 启动与扫描：`loadRuntimeDashboard` 只并发加载 Codex Home 摘要、固定运行态、当前运行态和最近操作，并用 `Promise.allSettled` 保留各域真实错误；进入会话页时调用 `loadSessionDashboard`，备份由用户触发 `loadBackupDashboard`。独立启动 effect 同时读取应用版本、上次更新重启状态并异步检查更新，不阻塞首屏。备份列表读取 manifest 后先排除局部 scope，再强校验最近 5 个完整候选。运行态切换把 relay 验证、scoped 快照、SQLite/文件同步和后置校验作为一个阻塞工作单元放入 Tauri blocking worker，避免大型会话池占用 UI 事件线程；后端 `Channel`、mutation guard、日志和终态保持同一操作顺序。所有数据 mutation 与一键更新安装先获取进程内 try-lock，再持有 Windows 独占 `%APPDATA%\codex-switch\mutation.lock` 文件句柄。
2. 计划：运行态切换解析 auth/overlay，校验 current/shared/backup/外置 SQLite roots 互不重叠，并按本次 scope 的实际 payload、SQLite workspace、加密/manifest 开销和至少 2 GiB 或 15% reserve 预检容量；relay 验证和这些 fail-closed 检查均在关闭 ChatGPT 前完成。会话同步输出 dry-run；完整恢复校验受管来源；Skill 安装只根据固定 ID 选择编译期包和后端推导的固定目标。
3. 备份：`backup` 生成 manifest v3，并显式记录 `scope` 与 `trackedDatabases`。runtime / sessions / state-only 快照只包含本次实际写集；hard delete、手工 full backup 和 restore safety 才覆盖 `state_5.sqlite`、`goals_1.sqlite`、`memories_1.sqlite`、`logs_2.sqlite` 四库。纳入 scope 的 SQLite 均通过 Online Backup，所有 payload 均用当前 Windows 用户 DPAPI 加密并记录大小/SHA-256。v2 快照仍可恢复；共享池显式使用 local paths，不继承用户 `sqlite_home`。
4. 应用：文件修改统一通过同目录临时文件、flush/sync 和原子替换；Windows 在调用 `MoveFileExW` write-through 前，先把已存在的父目录规范化为绝对 verbatim 路径并解析目录联接，以支持超过传统 `MAX_PATH` 的深层备份临时路径。Skill 包先写同卷 stage，逐文件验 hash；覆盖时把旧目录移动到当前 Codex Home 的 `.codex-switch\skill-backups`，再 rename stage。Skill Key 经 DPAPI 后 Base64 写入工具配置目录。
5. 验证：备份/恢复检查大小和 SHA-256，并对 manifest `trackedDatabases` 执行适用的 `PRAGMA quick_check`；full 范围覆盖四个受管 SQLite，局部范围不得声称完整可恢复。运行态切换要求 exact；Skill 安装重新扫描 package manifest、固定 allowlist 和文件 SHA-256；高风险 mutation 使用 ToolHelp 重新枚举并确认受管 ChatGPT 进程树已关闭。
6. 回执与操作记录：成功结果返回 typed receipt（operation ID、备份、计数、rolledBack、warnings）；失败返回脱敏错误，回滚/回滚失败终态写入本地 `operations.jsonl`。切换中的阶段通过 `Channel<RuntimeSwitchProgress>` 发送，页面显示真实阶段、耗时和 typed 终态。命令结束后立即释放 UI busy；切换只刷新 runtime 域并把 session/backup 标记 stale，昂贵域在用户进入对应页面或主动加载时刷新。刷新失败与 mutation 失败分开表达。

## 5. 模块与边界

当前模块边界：

- `codex_paths`：统一解析 Codex home、`sqlite_home`、会话正文目录和 `session_index.jsonl`，封装用户真实路径与共享池本地路径的差异。
- `codex_home`：路径发现、文件探测、运行态检测。
- `runtime_store`：固定账号/relay 双槽位、损坏 metadata fail-closed、DPAPI 认证存储、无碰撞覆盖历史和三文件失败回滚验证；不保存无关 live 全局配置。
- `crypto`：Windows DPAPI protect / unprotect。
- `process_control`：ToolHelp PID/PPID 进程树检测；只管理 `ChatGPT.exe` / `OpenAI.Codex.exe` 受管根，保留独立 `codex.exe` CLI，先温和关闭并在超时后对重新验证身份的残留进程强制兜底。
- `runtime_switcher`：双根快照、共享会话池同步、auth/config overlay 原子应用、exact 后置校验和双根失败补偿。
- `config_patch`：TOML 解析、保护字段、账号覆盖、diff。
- `session_scan`：按解析后的 `sqlite_home` 做 SQLite 只读扫描、dry-run 去重，并扫描 Codex home 下的 JSONL 正文。
- `session_sync`：JSONL-first allowlist、SQLite 事务/quick_check、数据库活动 rollout 优先级、语义化增长/分叉处理和 append-only session index；内容等价的既有 JSONL 不重写，bytes/mtime 保持不变，provider 可只更新 SQLite。
- `session_manager`：current/shared 合并视图、所有硬删除确认、Immediate 事务、文件原子更新、双根补偿和 current-only 可见恢复。
- `backup`：manifest v3 scoped payload、`scope` / `trackedDatabases`、SQLite Online Backup、容量/root 冲突预检、完整候选过滤、校验/精确恢复和 legacy plaintext auth 迁移；hard delete、手工 full backup、restore safety 覆盖四库，局部快照不进入 UI 列表；兼容恢复 v2，不实现 retention/prune。
- `file_ops`：同目录临时文件、同步和平台原子替换的唯一实现。
- `relay_verify`：relay `/models` 脱敏连通性验证；仅 HTTPS/loopback HTTP，响应最多 4 MiB。
- `operation_log`：结构化终态操作记录。
- `skill_manager`：固定 Skill 包、manifest/hash/漂移状态、路径/reparse 校验、同卷 stage/备份/原子激活、URL 归一化和 DPAPI 配置存储。
- `update_check`：固定 GitHub latest-release 读取、stable SemVer 验证、响应大小/超时/重定向边界、并发检查 guard 和限长说明。
- `update_install`：固定 Release EXE 资产选择、GitHub digest/大小/URL 校验、CSPRNG staging、elevated-aware 受限 DACL、目录句柄、canonical target 跨进程 lease、持久化阶段 journal、`RunEvent::Ready` ACK、失败回滚和受控清理。
- `commands`：Tauri command 薄边界，负责固定路径/槽位、进程门禁、恢复选择限制、typed receipt 组装和终态记录；不允许前端传任意 Codex 路径。

## 6. 输出文件与状态流

工具自身输出目录：`%APPDATA%\codex-switch`。

- `runtimes/plus/runtime.json`：ChatGPT 账号态元数据，不含敏感明文；目录名 `plus` 是内部兼容 ID。
- `runtimes/plus/auth.enc`：加密后的 ChatGPT 账号态 `auth.json`。
- `runtimes/plus/config.toml`：账号态 runtime overlay，只含切换需要的模型/provider 绑定。
- `runtimes/relay/runtime.json`：单个 API 中转站元数据，不含 API Key。
- `runtimes/relay/auth.enc`：加密后的中转站 API Key 登录态。
- `runtimes/relay/config.toml`：中转站 runtime overlay，包含 `model_provider = "openai_custom"` 与 provider 连接参数，不含无关 live 全局配置、明文 API Key、`env_key` 或 `goal`。
- `runtimes/<slot>/history/<timestamp>[-<sequence>]/`：覆盖前的完整槽位三件套；同毫秒归档使用递增后缀避免碰撞。
- `shared-sessions/state_5.sqlite`、`shared-sessions/sessions/` 与 `shared-sessions/session_index.jsonl`：ChatGPT 账号态与中转站共用的会话池。
- `backups/<timestamp>-<pid>-<counter>-<reason>/manifest.json`：v3 的 scope、`trackedDatabases`、来源、大小与 SHA-256 元数据；同目录 `.enc` 文件是 DPAPI 加密 payload。v2 目录仍可恢复，局部 v3 快照只用于对应操作补偿。
- `logs/operations.jsonl`：追加式脱敏操作记录；不保存凭据、请求/响应正文或自由文本错误。
- `skills/image2/config.json`、`skills/grok-search/config.json`：位于 `%APPDATA%\codex-switch` 下的非敏感 URL、固定模型和 credential 路径；不含 Key。
- `skills/<skill>/credential.enc`：当前 Windows 用户的 DPAPI 密文，经 Base64 供安装后的 PowerShell helper 解密；UI 只返回配置布尔状态。
- 当前 `CODEX_HOME\skills\newapi-image2-client` / `grok-search`：受管 Skill 文件和 `.codex-switch-package.json`；不含用户配置或 Key。
- 当前 `CODEX_HOME\.codex-switch\skill-backups\<operation-id>`：覆盖前的完整旧 Skill 目录；当前不自动清理。
- 当前 `CODEX_HOME\.codex-switch\skill-transactions\<skill>.json`：目录 swap 的原子恢复 journal；成功后删除，异常残留由下一次该 Skill 安装先恢复。

## 7. 常见故障归因

- 登录态切换后不生效：先查页面任务执行器停在哪个真实阶段、ChatGPT 受管进程树是否仍在运行、容量/root 预检、relay `/models` 验证、操作 ID/终态、`auth.json` 修改时间，以及运行态检测是 exact、mode 还是 unknown；mode-only 需要重新应用。
- 会话列表缺失：先查活跃库是否真在 `sqlite_home/state_5.sqlite`，再查 `threads.rollout_path` 指向的 JSONL 是否存在、`session_index.jsonl` 是否包含对应会话，最后查 `threads.model_provider` 是否与当前 `config.toml` 的 `model_provider` 一致；ChatGPT App 会按 provider / service 过滤，会话正文 JSONL 中也需要同步 `session_meta.payload.model_provider`。
- 归档会话没有同步回来：这是预期行为；归档会话只在会话管理页可见并可主动删除或恢复可见，不会被自动同步或清理。
- 同步重复或历史突然变短：查共享会话池、`threads.id`、SQLite 当前 `rollout_path`、同 ID 的全部 JSONL 及其严格追加/分叉关系；单纯 `model_provider` 不同不应生成冲突副本，较短来源也不应改写目标活动路径。
- SQLite 写入失败：查 `sqlite_home` 严格解析结果、schema、2 秒 busy timeout、事务/quick_check、操作回执和两根备份。
- 配置损坏：无效 live TOML 会阻断路径解析/写操作；查解析错误、操作记录和最近已验证备份，不允许静默回落到默认数据库。
- Windows 原子替换报 `os error 3`：不一定是 live 目录丢失。完整加密备份会在深层 payload 目录生成更长的同目录临时文件；旧实现把超过传统 260 字符限制的普通路径直接传给 `MoveFileExW` 时也会报“找不到路径”。先核对备份目标与临时路径长度、应用版本和父目录是否可解析，不要直接重建或删除 Codex Home；修复后的实现使用规范化 verbatim 父路径并兼容目录联接。
- 操作成功但页面提示错误：若切换已显示成功终态，随后提示的是 runtime 状态刷新失败，mutation 已成功；session/backup stale 不是错误，会在打开会话页或主动加载备份时刷新。按操作 ID 和备份记录排查，不要重复执行。
- Skill 已安装但不可用：先检查页面是否显示“API Key 已加密配置”，再确认重启过 ChatGPT / Codex CLI；Image2 URL 必须以 `/v1` 结尾，Grok URL 在保存时会移除末尾 `/v1` 并由脚本调用 `/v1/responses`。不要读取 credential 文件排查明文。
- Skill 显示“本地已修改 / 非受管目录 / 安装异常”：工具不会静默覆盖；确认目录确实可被接管后使用覆盖安装，并保留回执中的旧目录备份。

## 8. 测试与验证

当前真实可用验证命令：

- `npm test -- --run`：Vitest 前端 Dashboard、会话与技能页测试。
- `npm run typecheck`：TypeScript 静态检查。
- `npm run build`：Vite 前端生产构建。
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`：Rust 格式门禁。
- `cargo clippy --locked --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`：Rust lint 门禁。
- `cargo test --locked --manifest-path src-tauri/Cargo.toml`：Rust 后端单元测试。
- `cargo test --manifest-path src-tauri/Cargo.toml --test skill_manager_contract`：临时 `CODEX_HOME` Skill 安装、漂移、备份、URL、DPAPI 与 Windows PowerShell 兼容合同。
- `npm run tauri -- build`：完整 Tauri 桌面应用 release 编译，当前输出 `src-tauri\target\release\codex-switch.exe`。
- `npm run check:release`：校验五处版本、tag（如适用）、PE 格式/大小及构建机路径与常见 token marker。

`.github/workflows/ci.yml` 在 `windows-latest` / Node 22 / 固定 Rust 1.94.1 上执行前端测试、类型检查、构建、Rust fmt/clippy/test，并完整构建 Tauri release EXE；随后校验版本/PE/路径与 token marker、上传该 commit 的已验证 artifact。临时 `CODEX_HOME` E2E、updater 成功/回滚演练、签名验证和中文乱码检查仍需发布时给出独立证据。

## 9. Release 发布链路

当前公开 release 采用 GitHub Releases + Windows 单文件可执行产物：

1. 同步版本号：`package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json`。
2. 同步公开文档：`README.md` release badge / 功能说明、`CHANGELOG.md`、根目录长期文档。
3. 运行完整验证：`npm test -- --run`、`npm run typecheck`、`npm run build`、Rust fmt/clippy/test、必要的临时 `CODEX_HOME` E2E/故障注入/恢复演练、敏感扫描、乱码检查和 `npm run tauri -- build`。
4. 提交并推送版本提交。
5. 创建版本 tag，例如 `v0.2.0`，并推送 tag。
6. 从对应 commit 的成功 CI run 下载 verified artifact，用 `gh release create` 或 `gh release edit` 发布；不得另行上传未经相同门禁验证的本地产物。
7. 用 `gh release view <tag>` 和 `git ls-remote --tags origin <tag>` 验证远端 release/tag 真实存在。

应用内检查与一键更新依赖该链路的正式 release/tag/资产版本一致性。公开 UI/窗口标题使用 ChatGPT Switch，但 Release 必须包含唯一 `codex-switch.exe` 资产和 GitHub 生成的 SHA-256 digest；v0.1.9 updater 固定校验该名称，不能改为 `chatgpt-switch.exe`。不得只推 tag 不建 Release，也不得让 Release tag、Rust crate、Tauri config 和 npm 版本漂移。

## 10. 发布前剩余门禁

- 必须从干净工作区实际运行本节列出的前端/Rust门禁；不能只依赖历史结果或 CI 配置。
- 临时 `CODEX_HOME` E2E 至少覆盖账号态 → relay → 账号态、双向同步幂等和完整备份恢复；不得用真实 `.codex` 作为首轮写入目标。
- 故障注入至少覆盖运行态文件写入后失败、跨 current/shared 删除部分失败和恢复失败的安全快照补偿。
- 敏感扫描必须覆盖 UI 构建产物、操作记录、备份 manifest 和错误文本；不得出现 token/API Key/响应正文。
- 中文乱码检查必须覆盖本轮修改的 Markdown、TSX 和 Rust 文案。
- 已知产品残余：尚无 backup retention/prune 或安全清理入口；容量预检会阻止新切换在空间不足时继续，但不会自动治理已累积备份，发布说明不得暗示旧备份会被删除。
- 已知更新安全残余：helper 已有受限 staging、目录句柄和持久化阶段 journal，但 GitHub digest 与 Release 元数据仍来自同一控制面，尚无独立固定公钥/Authenticode 信任根。引入发布签名基础设施前不得宣称能抵抗仓库发布权限失陷。
- 已知一致性残余：热同步仍不是跨 SQLite/JSONL/index 的单一 durable transaction；关闭态 mutation 虽在备份后、写入前复检 ChatGPT 受管进程，但无法获得 ChatGPT 自身尊重的持续独占锁。后续应使用 delta journal/分阶段提交，并在检测到 ChatGPT 重启时避免用旧 current 快照覆盖并发变化。
- 已知 updater 多实例残余：target-keyed OS lease 已覆盖 helper 的替换、ACK 与回滚窗口，并由启动清理再次取得；应用本身仍未实现 singleton，因此多个普通实例仍可同时打开，但同一路径的更新 helper 会 fail closed，不能并发替换同一 EXE。

<!-- DXM-TRELLIS:START -->

## DXM 大开发工作流链路

中大型开发、新模块、跨多文件重构、需求不清楚的任务，默认走：

1. 先用 `project-grill` 问清楚；有代码/文档时用 `grill-with-docs`，空项目用 `new-project-grill`，小脚本/demo 用 `lightweight-grill`。
2. 提问前先从代码和文档自行判断，能确定的是否题、偏好题、低风险取舍题作为推荐假设推进。
3. 把结论写入 `.trellis/tasks/<task>/prd.md`，不能只停留在聊天上下文里。
4. 用 `.trellis/scripts/task.py start <task>` 进入 Trellis active task。
5. 按 Trellis 的 implement/check/update-spec/finish 节奏开发、验证、沉淀规范。
6. 任务完成后同步 DXM 长期文档；不能只更新 `.trellis/` 内部状态。

<!-- DXM-TRELLIS:END -->

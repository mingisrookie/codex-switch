# 项目完整链路说明

> 项目：`codex-switch`
> 根目录：`<repo-root>`
> 初始化日期：`2026-06-23`

本文档说明当前项目从输入、配置、启动、核心处理到产出的完整链路。开发前应先读 `项目文件结构说明.md`，再读本文，最后对照 `项目开发规范（AI协作）.md` 做变更。

<!-- DXM-DOC-RULES:START -->

<!-- DXM-CONTRACT:1 -->

## DXM 文档维护规则

- 本块由 DXM 管理；`--refresh-blocks` 只刷新本块，保留下方项目事实和人工补充。
- 项目目标、运行模式、配置链路、故障归因等长期事实必须写入下方对应章节，不能只留在聊天或临时文件里。
- 敏感配置和运行态输出只写用途，不回显真实 token、密码、API Key、账号明细、验证码、会话或密钥内容。
- 修改中文内容后必须检查可见乱码、错码和异常替换字符。

<!-- DXM-DOC-RULES:END -->

## 1. 项目目标

`ChatGPT Switch`（仓库与 EXE 兼容名为 `codex-switch`）是本机 ChatGPT 运行态切换与多会话同步工具。

- 解决问题：在当前 ChatGPT 账号登录态与一个 API 中转站调用态之间安全切换，把本地历史会话同步成统一视图，并为用户的 ChatGPT/Codex 环境安装可配置的 Image2/Grok 能力。
- 主要用户：当前 Windows 主机上的 ChatGPT Desktop / Codex CLI 使用者。
- 成功产物：React + Tauri 桌面应用，具备固定双槽位、官方登录态保留、真实请求路由判定、Relay 可选验证、单一页面内 task overlay、受管进程树关闭/原生重启、cutover 后手机连续性、旧会话单条/完全同步、Remote-compatible provider successor、会话管理、固定 Skill 安装/配置、DPAPI 凭据、失败补偿、typed receipt、durable 脱敏操作记录，以及可按 operation/attempt 关联并导出的独立脱敏诊断事件。
- 运行态输入：默认从 `CODEX_HOME` 解析 Codex home；未设置时使用 `%USERPROFILE%\.codex`。核心文件包括 `auth.json`、`config.toml`、`sessions\**\*.jsonl`、`archived_sessions\**\*.jsonl`、`session_index.jsonl`，以及位于 Codex `sqlite_home` 下的 `state_5.sqlite`、`goals_1.sqlite`、`memories_1.sqlite` 和 `logs_2.sqlite`。自动增量与手动完全同步只处理活跃 `sessions/`；Full/Sessions 备份与硬删除覆盖 active/archived 两类正文。`sqlite_home` 优先读取 `config.toml`，其次读取 `CODEX_SQLITE_HOME`，最后回落到 Codex home；`sqlite\codex-dev.db` 只作为兼容探测项，不作为当前会话同步来源。
- 运行态输出：工具自身加密运行态、临时回滚检查点、持久 full backup、共享会话池、增量同步基线、durable 操作记录、最近 14 天读取窗口且同根总量不超过 10 MiB（容量可缩短实际窗口）的诊断事件、用户主动生成的五文件诊断 ZIP、检查点空间状态以及面向 UI 的 typed receipt。

## 2. 运行模式

当前已实现 / 已接入的运行模式：

- 桌面正常模式：Tauri 启动 React UI，顶部在“运行态 / 会话管理 / 技能”之间切换。首屏只加载 Codex Home 摘要、固定运行态、当前运行态和最近操作；会话域在首次进入会话页时加载，备份域由用户主动加载。各域保持独立 `loading | ready | error`。窗口关闭监听在挂载时预注册，所有 close event 先由前端阻止，再调用后端 `request_app_exit`；后端取得同一 mutation lock 后调度 `app.exit(0)`，若退出被阻止或停滞，2 秒后释放 reservation，前端 2.5 秒后重试，不把后续 mutation 永久锁死。mutation 期间的关闭请求显示为排队状态，不会静默丢失或强拆窗口。监听未 ready 或注册失败时切换 fail closed；切换后先进入 runtime refresh pending，确认当前运行态前禁用再次切换。增量状态为 `applied` 时会话域标记 stale；增量 apply 失败/延期可能留下安全检查点时备份域标记 stale。
- 固定双槽位模式：只管理 `plus`（ChatGPT 账号，内部兼容 ID）和 `relay`（API 中转站）两个槽位。账号导入必须验证 live `auth_mode = chatgpt`；覆盖使用页面内确认并归档旧的加密槽位文件。UI 分开显示已保存、当前精确匹配/模式匹配和最近验证。
- 请求端切换模式：点击后只打开一个页面内 task overlay，并通过 Tauri `Channel` 保留运行态读取、官方登录态校验、Relay 验证选择、ChatGPT 检测/关闭、最小配置生成、进程状态检查、会话视图准备、请求端原子切换、登录态/路由复核、终态记录与启动的真实 phase/timestamp；UI 聚合为最多 7 个可见步骤。preflight 首先要求 live `auth.json` 为官方 `auth_mode = chatgpt`。需要变更时从受管 `ChatGPT.exe` / `OpenAI.Codex.exe` 根捕获唯一 AppUserModelID，用 ToolHelp 关闭进程树；独立 `codex.exe` CLI 只阻断不结束。关闭后重新冻结 auth/config，按目标生成最小 route patch：Relay 注入 `model_providers.openai_custom.experimental_bearer_token`、`requires_openai_auth = true`、`supports_websockets = true` 和受管 `sqlite_home`；Account 删除同一受管 provider 表并恢复原 SQLite home。写后要求 auth bytes 与关闭后快照完全一致、路由 exact；失败只回滚原始 `config.toml`。
- API 中转站模式：用户只配置一个中转站，保存 base URL、model 和 API key；URL 必须是 HTTP(S)，不得内嵌凭据、query 或 fragment，未写 scheme 时补 `https://`，远程地址必须使用 HTTPS。首次保存必须有 Key；后续只有 origin 不变时空 Key 才保留旧密文，改变 scheme/host/port 必须输入新 Key。首次切换或地址/凭据变化后，页面询问“验证连接后切换”或“直接切换”并保存选择。验证用 rustls + Bearer 请求 `<base>/models`，10 秒超时、禁止重定向，只以 2xx 判断地址、网络与鉴权，不解析或判断模型列表；直接切换路径零网络验证。
- 会话视图/手机连续性模式：切入 Relay 前用 SQLite Online Backup 把 Account 四库刷新到 `%APPDATA%\codex-switch\relay-sqlite`，只在副本中把 thread provider 归一为 `openai_custom`；Account 原库和共享 `sessions/` 正文保持不变。首次启用连续性时只读取 SQLite thread 行并把当前全部 ID 记为 cutover，不扫描/上传既有 JSONL。以后仅把 cutover 后新建、未归档且 provider 为 `openai_custom` 的 thread 加入 `%APPDATA%\codex-switch\mobile-continuity-v1.json`。切回 Account 的配置写入前最多执行 4 个发布批次，每批最多 8 个/8 MiB，总预算 30 秒；发布使用 immutable/no-clobber `openai` provider successor 与 SQLite transaction/`PRAGMA quick_check`，并复核受管 canonical path、官方 Remote 文件名、SQLite provider 和首条 JSONL provider。若仍有 deferred 项则 fail closed、保留 Relay route，不退化为全量。旧会话及其追加、删除和归档不自动处理。
- 内容与冲突模式：兼容事件保持原样；`local_file`、`file_path`、`attachment` 值只在 Remote successor 中递归替换为“部分内容仅本机”，原始 Relay JSONL bytes 不变，状态为 `partial`。严格前缀关系可复用/扩展同一 thread；分叉时分配新的 no-clobber provider slot，保留各分支并返回 `conflict`，不执行 last-write-wins。
- 手动完全同步模式：单独点击“完全同步”后，由 async Tauri command 在 blocking worker 内关闭并复核 ChatGPT，取消旧的前端 dry-run 重复全扫描。后端按 current/shared 双 `StateOnly` source 预检容量、创建窄临时点，再执行 current→shared 与 shared→current 的完整活跃会话对账；已归档 SQLite 行与 `archived_sessions/` 不参与、不复活。同步采用 JSONL-first、`SelectMostComplete`、source 稳定复检、immutable/no-clobber successor、SQLite transaction/`PRAGMA quick_check`；目标 current 最终归一为手机 Remote 兼容的 `openai` provider。若目标 schema 有 source 缺失但带 default 的新必填列，INSERT 省略该列让 SQLite 执行 default。成功后保存增量基线、持久化终态、强校验释放自动点并受控重启 ChatGPT；Apply 失败尝试恢复两边数据库，回滚失败保持 ChatGPT 关闭并保留检查点。同步不替代用户主动创建的 full backup。
- 会话管理模式：默认展示 current + shared 合并视图，current 的显示元数据优先；支持搜索、排序、每页 50 条和跨页选择。所有硬删除都使用页面内确认，确认区打开后聚焦标题，取消或成功后恢复触发点；后端先按真实 Full source 预检容量，再关闭并复检 ChatGPT，并对独立 CLI fail closed。hard delete 会在两根创建覆盖四个受管数据库、`sessions/`、`archived_sessions/` 与索引的 Full 快照，最后写前再次复检进程，再用 Immediate 事务删除 `threads` 与相关边表并原子更新 active/archived JSONL/index；跨根部分失败恢复两份快照。`恢复可见` 创建单根 `StateOnly`，更新 current 的 `archived = 0` / `archived_at = NULL`；成功终态落盘后强复检并自动释放该临时点。shared-only 选择不执行无效恢复。
- Skill 模式：固定管理 `image2` 与 `grokSearch` 两个 wire ID，分别落到 `CODEX_HOME\skills\newapi-image2-client` 与 `CODEX_HOME\skills\grok-search`。Image2 来源 ZIP 的审查 SHA-256 固定为 `648C192C2414BBFD9DBA36E264C01932BDCF7E2057A8BA2DA7006B40A94B332B`；Grok 包移除本机路径、endpoint 和配置。包体通过 `include_bytes!` 进入 EXE，不在运行时下载或复制管理员目录。安装/更新由后端关闭并复检 ChatGPT，同时持有 mutation guard；本地漂移/未知目录需页面内确认，旧目录先移动到同卷备份再原子激活 stage；原子 journal 让下次安装能处理进程中断留下的 stage/backup/live 组合。URL/Key 配置由用户输入，Key 仅保存 DPAPI 密文；首次必填、后续空值保留；离开技能页会立即卸载确认/配置区并销毁局部 Key，取消或成功后恢复触发点焦点。
- 更新模式：每次应用启动只在当前进程内发起一次非阻塞检查，手动入口可再次触发；不注册后台服务、不在运行中轮询。Rust 后端固定请求本仓库最新正式 GitHub Release，8 秒超时、禁止重定向、限制 256 KiB、拒绝 draft/prerelease/非法 SemVer。发现新版后用户点击一次“立即更新”：更新先取得全局 mutation guard，UI 双向禁用其他写操作；后端只接受唯一固定名称的 Windows x64 `codex-switch.exe`，校验资产 URL、大小和 GitHub SHA-256 digest。v0.2.1 下载连接超时为 30 秒、总下载超时为 10 分钟；helper readiness 超时执行 kill + wait，staging 清理使用有界重试。staging 名使用 Windows CSPRNG，目录按进程是否 elevated 应用受限 DACL并持有目录句柄；helper 持有 canonical target lease，并以阶段 journal、旧/新 hash 和 Tauri `RunEvent::Ready` ACK 决定完成或回滚。已发布 v0.2.0 的首跳下载器仍是 120 秒总超时，但正式 `v0.2.0 -> v0.2.1` 已通过隔离 UIA 真实一键更新证明；10 分钟合同从 v0.2.1 后续更新开始生效。
- 完整备份、检查点空间与恢复模式：用户可从运行态页创建 current/shared 两根 Full；后端在 blocking worker 内持有 mutation guard，容量预检后关闭并复检 ChatGPT、拒绝独立 CLI，并返回两份已验证快照。Full 覆盖四个受管数据库、`sessions/`、`archived_sessions/` 和索引；Dashboard 只在用户请求时按时间从新到旧验证候选，最多返回 256 个有效恢复点供逐份恢复或删除。页面内“安全释放”严格解析操作日志，只接受带非空 `operationId + role` 且与唯一 terminal record 在 action/status/phase、目录、reason/scope、时间窗和 payload hash 全部匹配的 bound BackupManifest v4。未绑定 v2/v3 自动点 fail closed 保留；经强校验的 v2 legacy Full、v3 Full、v4 Full 仍可显式列出、恢复和确认删除，但不参与自动 cleanup。
- 诊断与支持模式：应用在读取 updater 启动参数前尽早从绝对 `APPDATA` 初始化随机 session、bounded store 与 panic hook；`sessionStarted` 带 PID/timestamp unit 和 best-effort process creation stamp，Tauri Ready/正常 Exit 分别记录 `appReady` / `sessionEnded`。下次启动在 liveness 字段可用时会用 PID + creation stamp 排除仍存活实例和 PID reuse 后再决定是否记录 `previousSessionUnclean`。所有用户主动 mutation 从 command boundary 记录完整 start/真实 phase/关键 branch/typed terminal；顶部工具栏“诊断”打开当前页面内面板，展示可用性、事件数、占用与 14 天/10 MiB 配置上限，并提供“导出最近诊断”“打开日志目录”和只清除 event segments 的确认。失败面拿到真实 operation/attempt ID 时导出该操作与开始前 10 分钟上下文，否则导出 retention window。默认导出失败后，用户可在同一页面重试 Downloads 或主动改存固定应用诊断目录；两个目标共用唯一 exporter，不显示原始事件，也不接受任意路径。

## 3. 配置加载链路

配置来源：

- Codex home 默认路径：优先 `CODEX_HOME`，否则 `%USERPROFILE%\.codex`，最后兜底 `HOME/.codex`；后续实现应允许高级设置覆盖。
- Codex SQLite home：优先读取 live `config.toml` 的 `sqlite_home`，其次读取环境变量 `CODEX_SQLITE_HOME`，最后回落到 Codex home。共享会话池内部固定使用自身目录，不继承用户侧 `sqlite_home`，避免共享池写到真实用户目录。
- 官方登录态：live `auth.json`，含敏感 token；请求端切换把它视为只读权威状态，只做结构与 byte-exact 校验，不写入、不恢复。
- Codex 配置：live `config.toml`。请求端切换保护全局配置，只 patch 模型/service tier/受管 provider 绑定；Relay 激活期间 bearer token 以明文存在受管 provider 表，并固定写入 `supports_websockets = true`；Account 激活时删除该表。
- 工具配置：放在 `%APPDATA%\codex-switch`，保存运行态、备份、共享会话池、Skill 非敏感 config、Skill DPAPI credential、严格 durable 的 `logs\operations.jsonl`，以及分库的 `logs\diagnostics\events-*.jsonl`。后者只保存已结构化/限长/脱敏的支持事件，不参与前者的终态或检查点证明。

敏感字段不得在日志、UI、文档或错误报告中明文输出。

## 4. 核心流程

会创建快照的 mutation 使用：`preflight -> plan -> capacity -> backup -> apply -> verify -> persist terminal -> cleanup -> typed receipt`，失败时按各自 scope 补偿。请求路由是独立窄合同：`validate official auth -> optional relay access verification -> close app -> rebuild route/session-view plan -> repair known process-state corruption -> prepare target session view -> atomic config apply -> verify auth bytes + route -> persist route terminal`；它不创建 checkpoint。Relay 目标准备隔离 SQLite 副本；Account 目标先 `claim cutover queue -> publish selected no-clobber successors -> verify local Remote -> persist continuity state`，若无法在预算内收口则不写 route config、继续保持 Relay。手动完全同步关闭 ChatGPT并对两根数据库补偿；跨 SQLite/JSONL/index 仍不是单一 durable transaction，也不能宣称 bit-exact 回到操作前。

1. 启动与扫描：`loadRuntimeDashboard` 只并发加载 Codex Home 摘要、固定运行态、当前运行态和最近操作，并用 `Promise.allSettled` 保留各域真实错误；另行初始化手机连续性 cutover/status，但不扫描会话正文。进入会话页时调用 `loadSessionDashboard`，备份由用户触发 `loadBackupDashboard`。请求端切换、单会话发布与手动完全同步都在 Tauri blocking worker 中串行执行；任何全量扫描仍只由用户显式点击“完全同步”触发。所有数据 mutation 与一键更新安装先获取进程内 try-lock，再持有 Windows 独占 `%APPDATA%\codex-switch\mutation.lock` 文件句柄。
2. 请求路由计划：switch preflight 读取目标槽位、live `auth.json` 和 `config.toml`，先证明官方 `chatgpt` 登录态。Relay Key 只在内存中从 DPAPI 槽位解密。需要关闭 ChatGPT 时，关闭后重新读取 auth/config 并生成最小 patch；Account 恢复或权威删除模型/service tier 并移除受管 provider，Relay 只接受 `openai_custom`、复制安全 provider 字段、注入 bearer，并把 `requires_openai_auth` 与 `supports_websockets` 权威设为 `true`。任何缺失、格式错误、非官方 auth 或路由不完整都在 config 写入前 fail closed。
3. 会话/备份计划：请求路由 mutation 不创建 `RuntimeState`/`StateOnly`。Relay 目标仅对四个受管 SQLite 做 Online Backup 并在副本中归一 provider；Account 目标仅枚举持久 cutover 队列候选并做有界发布。手动完全同步固定双 `StateOnly` 并完整扫描活跃会话；hard delete、手工 Full 和 restore safety 覆盖四库以及 active/archived 会话文件。
4. 应用：请求端切换先准备目标 SQLite 会话视图，再使用同目录完整临时文件、flush/sync 和原子替换写 live `config.toml`。Relay 路由写入受管 `experimental_bearer_token`、`requires_openai_auth = true`、`supports_websockets = true` 和 `sqlite_home`；Account 路由删除整个受管 `openai_custom` 表并恢复原 SQLite home。手机发布与完全同步使用既有 SQLite transaction、no-clobber JSONL 和原子 index 合同。
5. 验证：request switch 写后重新读取 `auth.json` 并与关闭后快照 byte-exact 比较，再通过 `detect_active_runtime` 验证官方 auth + 目标路由 exact。配置写后的失败只在再次证明 ChatGPT/Codex 未恢复活动后回滚原始 `config.toml`，并再次验证 auth 未变。备份/恢复、Skill 与会话操作继续执行各自 hash、SQLite 和路径验证。
6. 回执与终态：`RuntimeSwitchResult` 只保留 route changed、目标 runtime、warning、typed incremental receipt、进程状态修复与 ChatGPT launch，不再携带恒空 backup/toShared/fromShared/checkpoint 字段。请求路由与增量分别写 `SwitchRuntime`、`IncrementalSync` 脱敏终态；增量状态明确区分 skipped/unchanged/applied/needsFullSync/deferred/failed。成功切换只在路由 terminal 和增量收口后受控激活 ChatGPT，启动失败形成 typed warning，可在同一 overlay 重试。自动点只有与唯一 durable terminal 精确绑定的 v4 点才可清理。
7. 诊断旁路：每个用户主动 mutation 都在 lock/spawn/可能早退之前从 command boundary 创建随机 attempt；worker/join/早退和后置步骤写真实 phase/安全 branch，durable ledger 发布成功时只绑定 operation ID，cleanup/mobile/launch/block/rollback 完成后才按 typed 语义写唯一 terminal。失败 Tauri rejection 使用固定、严格限长的信封携带 durable ID 或本次 attempt ID，前端统一解包且兼容裸字符串，因此失败导出不猜 latest。任何诊断失败都 fail-open，不改变原业务 `Result`。event 先经 allowlist、限长、路径/秘密 sanitizer，再 append + `sync_data` 到独立 segment；command/lifecycle recorder 与 panic 拿不到 root named mutex 都零等待放弃。年龄 prune 逐事件验证，只删 clean 且全部过期的段；容量仍可淘汰 clean/valid 最旧段，dirty tail、未知 schema、内部损坏和祖先 reparse fail closed。exporter 严格只读 operation ledger/health，在受控内存先完成固定五文件脱敏、hash 与 ZIP 自检，再解析目标并同目录 staging/no-clobber 发布；只有 destination failure 返回短期 opaque retry ID，Downloads 重试和用户主动 APPDATA fallback 复用同一 prepared bytes/hash/selection，不重新采集。

## 5. 模块与边界

当前模块边界：

- `codex_paths`：统一解析 Codex home、`sqlite_home`、会话正文目录和 `session_index.jsonl`，封装用户真实路径与共享池本地路径的差异。
- `codex_home`：路径发现、文件探测、运行态检测。
- `runtime_store`：固定账号/relay 双槽位、损坏 metadata fail-closed、DPAPI 认证存储、无碰撞覆盖历史和三文件失败回滚验证；不保存无关 live 全局配置。
- `crypto`：Windows DPAPI protect / unprotect。
- `process_control`：ToolHelp PID/PPID 进程树检测；只管理 `ChatGPT.exe` / `OpenAI.Codex.exe` 受管根，独立识别但绝不结束 standalone `codex.exe` CLI；关闭前从受管根读取并验证唯一 AppUserModelID，切换后通过 `IApplicationActivationManager` 原生激活并重新验证同一身份，目标缺失/冲突/超时返回 typed failure。
- `request_route_switcher`：冻结官方 auth 与 live config，联合请求路由和会话视图 plan；目标视图准备成功后才原子写 `config.toml`，验证 auth byte-exact 与 route exact；配置写后失败只补偿配置。
- `runtime_session_view`：持久化 Account 原始/有效 SQLite home；切入 Relay 时在线备份四库并只在副本归一 provider，切回 Account 时在 30 秒预算内驱动最多 4 批手机连续性发布，仍 deferred 则保持 Relay。
- `runtime_switcher`：保留切换 typed phase/receipt/launch 类型与手动活跃会话完全同步编排；不再负责 auth/config 切换。
- `config_patch`：TOML 解析、保护字段、账号覆盖、diff。
- `session_scan`：按解析后的 `sqlite_home` 做 SQLite 只读扫描、dry-run 去重，并扫描 Codex home 下的 JSONL 正文。
- `session_incremental`：持久化 v1 增量基线，按 thread ID 记录受管 rollout 路径、provider、归档、文件大小与 mtime；执行有界 metadata inventory、变化/冲突判定、容量/数量门槛和 typed plan，缺失或异常索引 fail closed 到手动完全同步。
- `session_sync`：JSONL-first allowlist、SQLite 事务/quick_check、数据库活动 rollout 优先级、存储 planning 和语义化增长/分叉处理。首个完整 `session_meta` 是 rollout 的权威身份；后续继承的父任务 `session_meta` 不参与 ID 判定，provider 归一也只改写权威首条并逐字保留后续历史元数据。`SelectMostComplete` 先决定活动历史；provider 归一随后复用现有完整文件，或发布严格 UUID、Remote 文件名、canonical containment 的 immutable successor 与 provenance marker；SQLite provider/path 与 JSONL 首条 provider 同步提交。终态后 GC 在删除旧工具槽位前重新验证 marker、完整版本、successor 包含关系与 current/shared 两库引用，任何漂移都保留。`PreserveExisting` 只用于 hot shared→current，existing thread 零 target candidate I/O、零复制并保留原 SQLite。planning 返回 rollout/marker/index/SQLite workspace 输出 bytes，供运行中初检与关闭后 exact 复检。
- `session_manager`：current/shared 合并视图、active/archived 会话发现、所有硬删除确认、Immediate 事务、双根 Full 补偿和 current-only 可见恢复；硬删除同时处理两根的 `sessions/` 与 `archived_sessions/`。
- `backup`：BackupManifest v4 scoped payload、自动点 `operationId + role` binding、RuntimeState process-state、SQLite Online Backup、按卷组合容量、active/archived Full/Sessions 范围及强校验/精确恢复。临时清理仅接受与唯一 terminal 全量匹配的 bound v4；未绑定 v2/v3 自动点及失败、孤儿、drift、unclassified 保留。v2/v3/v4 Full 仍可显式管理。
- `file_ops`：同目录临时文件、同步、平台原子替换，以及 hard-link no-clobber `atomic_create` 的统一实现。
- `relay_verify`：relay `/models` 脱敏 access 验证；仅 HTTPS/loopback HTTP，禁止重定向，只以 2xx 判断 URL/TLS/HTTP/鉴权，不读取模型列表。
- `operation_log`：业务语义 append-only、物理整文件原子发布的结构化终态记录；普通历史读取容忍旧的最后一条截断，严格 cleanup reader 与新 terminal 发布拒绝损坏日志/重复 ID，发布失败保持旧文件 byte-exact。
- `diagnostics`：独立支持诊断域。`event` 定义 schema、Unix epoch 毫秒时间、OS CSPRNG/`CoCreateGuid` fail-open 的 session-attempt ID 与 typed terminal；`mod` 管理 operation correlation 和 terminal 后封闭状态机；`sanitize` 在落盘前限制字段、深度、数组和字符串并脱敏 secrets/URL/path；`store` 用 Windows root named mutex、segment 创建时间轮转、event timestamp 14 天读取窗口、10 MiB/512 KiB 容量、dirty-tail 封存、schema/reparse/internal-corruption 防护；`lifecycle` 负责 PID+creation-stamp start/ready/exit/previous-unclean/panic best-effort；`export` 负责固定五文件、更严格二次脱敏、hash/ZIP 自检和同目录 no-clobber 发布；`platform` 负责 Known Folder Downloads、本地时区、路径 containment 与 Explorer 打开边界。
- `diagnostic_commands`：诊断 Tauri 薄边界；返回 store status，按 retained window 或从 durable bind 反查真实 attempt 的完整事件自动选择，严格只读并脱敏 operation 子集，先注册有界 prepared context，再生成 typed receipt/failure。只有 destination failure 携带 retry ID，成功后一次性消费；只接受 opaque export ID 打开刚生成的包。health best-effort 汇总应用/平台、存储、route/auth mode、受管/standalone 进程计数和四个受管 SQLite 的 present/readable/bytes/只读 `schema_version`，失败逐项 unavailable。
- `skill_manager`：固定 Skill 包、manifest/hash/漂移状态、路径/reparse 校验、同卷 stage/备份/原子激活、URL 归一化和 DPAPI 配置存储。
- `update_check`：固定 GitHub latest-release 读取、stable SemVer 验证、响应大小/超时/重定向边界、并发检查 guard 和限长说明。
- `update_install`：固定 Release EXE 资产选择、GitHub digest/大小/URL 校验、30 秒连接/10 分钟总下载超时、CSPRNG staging、受限 DACL、目录句柄、canonical target lease、阶段 journal、`RunEvent::Ready` ACK、helper kill + wait、cleanup retry、失败回滚和 elevated 无参数恢复发现 fail-closed。
- `commands`：Tauri command 薄边界，负责固定路径/槽位、Relay 验证选择、会话视图准备、旧会话单条发布、手动完全同步、storage/capacity plan、进程门禁、AUMID capture/launch、细分 progress、typed receipt、终态日志和检查点/Full 管理。`request_app_exit` 复用 mutation guard，调度退出后若 2 秒仍存活则释放 reservation；`launch_chatgpt` 只消费受控缓存身份，不接受前端路径/命令。

## 6. 输出文件与状态流

工具自身输出目录：`%APPDATA%\codex-switch`。

- `runtimes/plus/runtime.json`：ChatGPT 账号态元数据，不含敏感明文；目录名 `plus` 是内部兼容 ID。
- `runtimes/plus/auth.enc`：保存账号配置时产生的 ChatGPT 官方 `auth.json` 加密快照；请求端切换不读取它作为 live 写回源。
- `runtimes/plus/config.toml`：账号态 runtime overlay，只含切换需要的模型/provider 绑定。
- `runtimes/relay/runtime.json`：单个 API 中转站元数据，不含 API Key。
- `runtimes/relay/auth.enc`：加密后的中转站 API Key 登录态。
- `runtimes/relay/config.toml`：中转站 runtime overlay，包含 `model_provider = "openai_custom"`、`supports_websockets = true` 与 provider 连接参数，不含无关 live 全局配置、明文 API Key、`env_key` 或 `goal`；激活时才把解密 Key 投影到 live config 的 `experimental_bearer_token`。旧槽位中的缺失值或 `false` 会在下一次 Relay 切换时被权威纠正为 `true`。
- `runtimes/<slot>/history/<timestamp>[-<sequence>]/`：覆盖前的完整槽位三件套；同毫秒归档使用递增后缀避免碰撞。
- `shared-sessions/state_5.sqlite`、`shared-sessions/sessions/`、`shared-sessions/archived_sessions/` 与 `shared-sessions/session_index.jsonl`：ChatGPT 账号态与中转站共用的会话池；自动增量与手动完全同步只处理活跃正文，管理/Full 路径覆盖两类目录。
- `mobile-continuity-v1.json`：手机连续性 cutover 与持久队列；保存 thread ID、source fingerprint、typed 状态、尝试次数/下次重试和脱敏失败分类，不保存正文或凭据。损坏/版本漂移 fail closed，不自动重建并上传历史。
- `session-sync-state-v1.json`：既有 current/shared 活跃会话对账基线；由手动完全同步维护，不进入普通请求端切换。
- 当前 Codex Home `sessions/YYYY/MM/DD/rollout-YYYY-MM-DDTHH-MM-SS-<uuid>.jsonl`：Remote-compatible 活动 provider 槽位。原用户/legacy/既有工具 rollout 都不原地改写；等价或更完整槽位直接复用，需要新 provider 或增长时创建 immutable successor。
- 当前 Codex Home `sessions/**/.<rollout-name>.codex-switch-slot-v1.json`：与工具新建 JSONL 相邻的严格 provenance marker，记录 thread/provider、最初 origin、`createdBytes` 与 created-prefix 完整 SHA-256。它不是会话正文或备份；只有 marker 和 JSONL 都可验证时工具才拥有回收权限。
- `backups/<timestamp>-<pid>-<counter>-<reason>/manifest.json`：当前 v4 记录 scope、tracked databases、自动点 binding、来源、大小与 SHA-256；current RuntimeState 可额外跟踪 process-state。未绑定 v2/v3 自动点保留；经强校验的 v2/v3/v4 Full 可进入最多 256 项的按需列表并显式管理。
- `logs/operations.jsonl`：脱敏终态记录；逻辑追加、物理同目录临时文件同步后原子整文件发布，不保存凭据、请求/响应正文或自由文本错误。
- `logs/diagnostics/events-<timestamp>-<pid>-<session>-<sequence>.jsonl`：独立的脱敏诊断 segment；每次启动使用不可稳定关联的随机 session ID，单段默认 512 KiB，同一 Windows 诊断根以 named mutex 跨进程维持 10 MiB 容量。segment 文件名创建时间负责轮转/prune，读取再按 event `timestamp` 精确过滤最近 14 天，容量可能使窗口更短；每段唯一未完成尾记录可丢弃且该 dirty 段不再追加，内部损坏/未知 schema 明确报错。自由文本业务 UUID 和未知绝对路径在落盘前脱敏；结构化诊断关联 ID 保持。清除入口只删除这类受管 regular/non-reparse segment，不触碰 `operations.jsonl` 或业务数据。
- Windows Known Folder Downloads 下的 `ChatGPT-Switch-Diagnostics-<YYYYMMDD-HHMMSS-mmm>[-N].zip`：用户主动导出的支持包，固定包含 `README.txt`、`manifest.json`、`diagnostics.jsonl`、`operations.jsonl`、`health.json`；manifest 以 `timestampUnit = unixEpochMilliseconds` 声明 event `timestamp` 的 Unix epoch 毫秒单位。retained window 使用实际事件 timestamp 的 min/max，相关 operation 以规范化时间区间求交，因果排序与墙钟回拨不会让 manifest 窗口漏掉已打包事件。同名使用数字后缀且不覆盖；ZIP 不由 APPDATA retention 自动删除。
- `%APPDATA%\codex-switch\diagnostic-exports\ChatGPT-Switch-Diagnostics-<YYYYMMDD-HHMMSS-mmm>[-N].zip`：Downloads 导出失败后由用户主动选择的固定备用支持包；使用相同五文件、同目录 staging/self-check/no-clobber 合同，不属于 event retention，也不会被“清除诊断日志”删除。
- `skills/image2/config.json`、`skills/grok-search/config.json`：位于 `%APPDATA%\codex-switch` 下的非敏感 URL、固定模型和 credential 路径；不含 Key。
- `skills/<skill>/credential.enc`：当前 Windows 用户的 DPAPI 密文，经 Base64 供安装后的 PowerShell helper 解密；UI 只返回配置布尔状态。
- 当前 `CODEX_HOME\skills\newapi-image2-client` / `grok-search`：受管 Skill 文件和 `.codex-switch-package.json`；不含用户配置或 Key。
- 当前 `CODEX_HOME\.codex-switch\skill-backups\<operation-id>`：覆盖前的完整旧 Skill 目录；当前不自动清理。
- 当前 `CODEX_HOME\.codex-switch\skill-transactions\<skill>.json`：目录 swap 的原子恢复 journal；成功后删除，异常残留由下一次该 Skill 安装先恢复。

## 7. 常见故障归因

- 请求端切换后不生效：先查任务执行器停在哪个真实阶段、Relay `/models` 验证、ChatGPT 受管进程树、操作 ID/终态、live `config.toml` 目标 provider 下的 `supports_websockets = true` 等受管 route 字段，以及 `auth.json` hash/修改时间是否保持不变。缺失或非 `chatgpt` auth 会在写入前 fail closed；mode-only 需要重新应用。
- 手机连续性提示“需手动处理”：查看 `mobile-continuity-v1.json` 对应 item 的 typed status 与脱敏 failure category，再看 `IncrementalSync` 操作记录。旧会话、归档/删除、source 超过 8 MiB、路径/metadata 不一致、状态损坏或无法证明安全的分支按设计保留；不得因此重跑请求端切换或自动执行全量同步。
- 会话列表缺失：先查活跃库是否真在 `sqlite_home/state_5.sqlite`，再查 `threads.rollout_path` 是否指向受管 sessions 根内、Remote 文件名合法且 ID 一致的 JSONL，最后核对 SQLite `threads.model_provider`、live `config.toml` provider 与活动 JSONL 首条 `session_meta.payload.model_provider` 三者一致。若 provider 槽位未生成，继续查 strict UUID/containment、容量预检和 source 稳定性错误，不要直接改写原 JSONL。
- 归档会话没有同步回来：这是预期行为；归档会话只在会话管理页可见并可主动删除或恢复可见，不会被自动同步或清理。
- 同步重复或历史突然变短：查共享会话池、`threads.id`、SQLite 当前 `rollout_path`、同 ID 的全部 JSONL 及其严格前缀/分叉关系。hot shared→current 对既有 thread 的预期结果是文件处理前直接跳过、`copied_session_files = 0`、DB 继续指向旧 active 且旧 writer 后续尾部可见；如果此分支后观察到 target candidate 文件，应从其他 Create/Import 操作或历史残留继续归因，不能把它解释为 `PreserveExisting` 本轮发布。只有 current→shared/关闭态 `SelectMostComplete` 才会在发布后推进活动路径。单纯 `model_provider` 不同不应生成冲突副本。
- SQLite 写入失败：查 `sqlite_home` 严格解析结果、source/target `PRAGMA table_info(threads)`、2 秒 busy timeout、事务/quick_check、操作回执和两根备份。若出现 `NOT NULL constraint failed: threads.<column>`，先核对该目标列是否是 source 尚无但带 default 的 schema 新列；同步应省略缺失列让 SQLite 执行 default，而不是显式绑定 `NULL`。
- 配置损坏：无效 live TOML 会阻断路径解析/写操作；查解析错误、操作记录和最近已验证备份，不允许静默回落到默认数据库。
- Windows 原子替换报 `os error 3`：不一定是 live 目录丢失。完整加密备份会在深层 payload 目录生成更长的同目录临时文件；旧实现把超过传统 260 字符限制的普通路径直接传给 `MoveFileExW` 时也会报“找不到路径”。先核对备份目标与临时路径长度、应用版本和父目录是否可解析，不要直接重建或删除 Codex Home；修复后的实现使用规范化 verbatim 父路径并兼容目录联接。
- 操作成功但页面提示错误：mutation、runtime 刷新和 unknown 终态使用不同页面状态。成功后“正在确认当前运行态”期间再次切换会被禁用；刷新失败不改写 mutation 成功。未收到可信终态时先不要重新打开 ChatGPT，按 operation ID 与日志确认，不要重复点击。
- 诊断导出失败：先看诊断面板的可用性、Known Folder Downloads 是否可解析/可写、诊断目录是否为 regular directory，以及指定 operation/attempt 是否真有事件。页面不会静默改存：可重试 Downloads，或由用户主动点击“改存应用诊断目录”写入固定 `%APPDATA%\codex-switch\diagnostic-exports`；两处都失败时保留页面内错误，不允许指定任意目录。导出失败不会反转原业务结果，也不会递归记录自身错误形成日志风暴。
- 上一会话显示 `previousSessionUnclean`：它只说明最近一次已记录 `sessionStarted` 的随机诊断 session 缺少 `sessionEnded`，可能来自 panic、强制结束、断电、硬崩溃或诊断写入失败，不能单靠该事件确定根因。继续结合最后已持久 phase、typed terminal 和 durable operation record；硬卡死、访问冲突、断电、Windows WER 等可能完全没有诊断事件。
- C 盘备份目录持续增长：先确认使用版本。v0.2.0 的 changed switch 每次永久创建 current `Runtime` + shared `Sessions` 全会话 checkpoint，是按次下降的直接原因；当前请求端切换不创建任何 checkpoint，也不写会话/provider rollout。2026-07-26 受控 UI 清理实证为 `21` 目录/`6,327,089,609` bytes → `17` 目录/`2,693,977,957` bytes，剩余项是 Full/孤儿/失败/hash drift/无法分类等安全保留项。若空间继续增长，应归因到用户单独执行的同步、备份、恢复、管理、日志或 updater staging，不能归因到 request switch。
- 切换成功但 ChatGPT 未打开：运行态成功不应被回滚。先看 task overlay 的 `ChatGptLaunchResult`；`failed` 表示关闭前未能捕获唯一 AUMID、原生激活失败或启动后未出现可验证的同身份受管根。使用 overlay 的“重新打开 ChatGPT”重试，不应从 PATH 手工执行不明同名程序。
- Skill 已安装但不可用：先检查页面是否显示“API Key 已加密配置”，再确认重启过 ChatGPT / Codex CLI；Image2 URL 必须以 `/v1` 结尾，Grok URL 在保存时会移除末尾 `/v1` 并由脚本调用 `/v1/responses`。不要读取 credential 文件排查明文。
- Skill 显示“本地已修改 / 非受管目录 / 安装异常”：工具不会静默覆盖；确认目录确实可被接管后使用覆盖安装，并保留回执中的旧目录备份。

## 8. 测试与验证

当前真实可用验证命令：

- `npm test -- --run`：Vitest 前端 Dashboard、会话与技能页测试。
- `npm run typecheck`：TypeScript 静态检查。
- `npm run build`：Vite 前端生产构建。
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`：Rust 格式门禁。
- `cargo clippy --locked --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`：Rust lint 门禁。
- `cargo test --locked --manifest-path src-tauri/Cargo.toml`：Rust 后端单元测试。
- `cargo test --manifest-path src-tauri/Cargo.toml --test skill_manager_contract`：临时 `CODEX_HOME` Skill 安装、漂移、备份、URL、DPAPI 与 Windows PowerShell 兼容合同。
- `cargo test --manifest-path src-tauri/Cargo.toml --test synthetic_large_home`：隔离的多文件 provider 切换回归，验证内容等价 JSONL 的文件数、bytes、SHA-256 与 mtime 不变；ignored benchmark 可用 `CODEX_SWITCH_SYNTHETIC_SESSION_COUNT` / `CODEX_SWITCH_SYNTHETIC_SESSION_KIB` 放大。
- diagnostics Rust/前端回归属于 `cargo test --locked --manifest-path src-tauri/Cargo.toml` 与 `npm test -- --run` 的发布门禁；必须覆盖所有用户主动 mutation 的 begin-before-lock/spawn/早退、真实 phase/安全 branch、attempt→durable 只绑定、worker join/后置步骤、唯一 typed terminal、无 durable record lifecycle、后台只记录失败和 diagnostics fail-open；还要覆盖 schema/sanitizer、路径 tokenization、每段 partial tail/dirty-tail 封存/internal corruption、segment 创建时间轮转、event timestamp 14 天精确过滤、10 MiB/512 KiB、Windows root named mutex 并发/panic 零等待/clear/post-prune、operation ledger byte-exact 分离、operation terminal 封闭、lifecycle PID+creation stamp、health 只读字段/逐项 unavailable、固定五文件/hash/ZIP self-check、Known Folder/用户显式备用目录/no-clobber/冲突/失败、staging 正常清理与硬崩溃残余边界、顶部面板/失败导出/焦点与 390px 可达。
- `npm run tauri -- build --no-bundle`：不生成安装包的正式桌面 UI/E2E 候选；仍会执行 `beforeBuildCommand`、加载 `frontendDist` 并使用生产 custom protocol。裸 `cargo build --release` 只证明 Rust 编译，可能继续请求 `devUrl`，不得用它代替桌面 UI 实测。
- `npm run tauri -- build`：完整 Tauri 桌面应用 release 编译与安装包构建，当前输出 `src-tauri\target\release\codex-switch.exe`。
- `npm run check:release`：校验五处版本、tag（如适用）、PE 格式/大小及构建机路径与常见 token marker。
- `.\scripts\pack-windows-release.ps1 -UpxPath "<verified-upx.exe>"`：固定验证官方 UPX 5.2.0 与 `upx.exe` SHA-256 `F4C0CC7ACA0F1FF0D0B750E966B44139F2FA1A2DB7281F48FC52194400712E1D`，冻结并验证 raw hash/contract，只复制到 staging 后执行 `--best --lzma`，再执行 `upx -t`、packed contract、PE32+ x64/双版本与 3,000,000 bytes 硬门禁；raw 不原地修改。

Cargo release profile 固定 `opt-level = "z"`、`lto = true`、`codegen-units = 1`、`panic = "abort"`、`strip = "symbols"`。`.github/workflows/ci.yml` 在 `windows-latest` / Node 22 / 固定 Rust 1.94.1 上执行前端/Rust 门禁并构建 raw Tauri EXE；随后从官方固定 URL 下载 UPX 5.2.0 ZIP，验证 ZIP SHA-256 `B471EBF1B7F20F4A89150264ED9A008A2A5BFD247F3C6D1184A75BB59CA08F5D` 与 EXE SHA-256 `F4C0CC7ACA0F1FF0D0B750E966B44139F2FA1A2DB7281F48FC52194400712E1D`，调用统一脚本，并只上传 packed 的 `release/codex-switch.exe`。本地临时候选 raw `5,955,584` bytes → packed `2,228,224` bytes（SHA-256 `4DDC…CED7A`）仅作为历史候选；v0.2.1 tag-CI/Release 的正式资产为 `2,214,400` bytes、SHA-256 `8F6EA219A53BB3395F039327A3CD3827B53EE67B8DAF4B130E60235940A3020C`。

## 9. Release 发布链路

当前公开 release 采用 GitHub Releases + Windows 单文件可执行产物：

1. 同步版本号：`package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json`。
2. 同步公开文档：`README.md` release badge / 功能说明、`CHANGELOG.md`、根目录长期文档。
3. 运行完整验证：`npm test -- --run`、`npm run typecheck`、`npm run build`、Rust fmt/clippy/test、必要的临时 `CODEX_HOME` E2E/故障注入/恢复演练、敏感扫描、乱码检查和 `npm run tauri -- build`；对 raw EXE 运行 release contract。
4. 使用固定官方 UPX 5.2.0 调用 `scripts/pack-windows-release.ps1`，证明 raw hash 未变、`upx -t` 成功、packed 仍为 PE32+ x64、双版本一致、release contract 通过且不超过 3,000,000 bytes。
5. 提交并推送版本提交。
6. 创建与 manifest 一致的版本 tag，例如 `v0.2.1`，并推送 tag。
7. 从对应 tag commit 的成功 CI run 下载唯一 packed artifact，用 `gh release create` 或 `gh release edit` 发布；不得上传 raw EXE，也不得另行上传未经相同门禁验证的本地产物。
8. 用 `gh release view <tag>` 和 `git ls-remote --tags origin <tag>` 验证远端 release/tag，重下载唯一资产并复核大小、SHA-256、PE32+ x64 与双版本，再做真实旧版一键更新。

应用内检查与一键更新依赖该链路的正式 release/tag/资产版本一致性。公开 UI/窗口标题使用 ChatGPT Switch，但 Release 必须包含唯一 packed `codex-switch.exe` 资产和 GitHub 生成的 SHA-256 digest；v0.1.9 updater 固定校验该名称，不能改为 `chatgpt-switch.exe`。不得只推 tag 不建 Release，不得上传 raw/packed 两份资产，也不得让 Release tag、Rust crate、Tauri config 和 npm 版本漂移。

v0.2.1 已完成整条发布链：PR #5 从工作提交 `702dc37` 合并为 `3b4f440`，PR CI `30194264349` / `30194276794`、main CI `30194772843` 与 tag CI `30195207004` 均通过；annotated tag `v0.2.1` 指向 `3b4f440`。[正式 Release](https://github.com/mingisrookie/codex-switch/releases/tag/v0.2.1) 为 latest stable、非 draft、单资产；tag artifact 与 Release 回下载的 `codex-switch.exe` 均为 `2,214,400` bytes、上述同一 SHA-256、PE32+ x64、`FileVersion/ProductVersion = 0.2.1`，并通过 `upx -t`。两个 ignored live GitHub 合同测试各 `1 passed`。

正式 v0.2.0（SHA-256 `42012…A65A`）已在隔离 UIA 中真实点击“立即更新”：约 `2.086s` 出现并点击，旧进程约 `89.103s` 后 exit `0`，`105.1s` 内自动替换并重启 ChatGPT Switch；目标文件与正式 v0.2.1 Release 的 bytes/hash/version 一致，staging/install leftovers 均为 `0`，没有手工替换。本机 Microsoft Defender Product/Feature disabled，相关命令无法形成扫描通过证据，不能用上述合同或更新闭环替代 Defender 结论。

当前源码目标为 `v0.2.7`；`v0.2.6` annotated tag 在正式 Release 前被最终独立隐私审查拦截，保持未发布且不移动。本段不把工作区实现或本地候选当作正式发布证据。诊断专项隔离故障注入/ZIP 外部敏感扫描、真实 packed EXE 导出 UI/E2E、独立 reviewer PASS、PR/main/tag CI、annotated tag、唯一 tag-CI packed 资产、GitHub Release/Latest、公开回下载 bytes/hash/PE/双版本/UPX 合同和正式 `v0.2.5 -> v0.2.7` updater smoke，均待主任务按真实输出最终闭环后补录。

## 10. 发布门禁与剩余边界

- 必须从干净工作区实际运行本节列出的前端/Rust门禁；不能只依赖历史结果或 CI 配置。
- 临时 `CODEX_HOME` E2E 至少覆盖账号态 → relay → 账号态、双向同步幂等和完整备份恢复；不得用真实 `.codex` 作为首轮写入目标。
- 故障注入至少覆盖运行态文件写入后失败、跨 current/shared 删除部分失败和恢复失败的安全快照补偿。
- 敏感扫描必须覆盖 UI 构建产物、操作记录、备份 manifest 和错误文本；不得出现 token/API Key/响应正文。
- 中文乱码检查必须覆盖本轮修改的 Markdown、TSX 和 Rust 文案。
- 已知产品残余：临时点已有终态驱动清理，已验证 Full 可逐个显式删除，但持久恢复点仍没有自动 retention/pin 分组；失败、孤儿和无证据目录也不会自动删除。容量预检与“可证明回收”不能被误述为所有旧备份都安全。
- 已知更新安全残余：helper 已有受限 staging、目录句柄和持久化阶段 journal，但 GitHub digest 与 Release 元数据仍来自同一控制面，尚无独立固定公钥/Authenticode 信任根。引入发布签名基础设施前不得宣称能抵抗仓库发布权限失陷。
- 已知一致性残余：增量与完全同步仍不是跨 SQLite/JSONL/index 的单一 durable transaction。source 稳定验证和完整 JSONL atomic no-clobber 发布保证实际 Create/Import 不会留下半尾；失败可恢复两根 SQLite，但已经原子发布且尚未引用的完整 JSONL 可能保留，交由下一次完全同步对账。SQLite 与文件仍没有外部 ChatGPT 共同尊重的跨介质事务。
- 已知 provider 槽位残余：首次切换到一个新 provider 可能新增一份完整活动 JSONL；后续增长创建 successor，只有 durable terminal 后且新旧 marker/version、内容包含关系和两根数据库引用都可重新证明时才回收 predecessor。崩溃、日志失败、marker/文件漂移、数据库仍引用或删除失败会安全保留旧槽位，因此不能承诺绝对零残留。普通 provider 归一只处理 active `sessions/`，不扫描 `archived_sessions/`。
- 已知 source TOCTOU 残余：执行期对 source ID、完整尾行、长度和 SHA-256 做有界复检，并在 no-clobber 发布后再次比较；它能 fail closed，但不能让外部 ChatGPT 与本工具共享同一跨介质事务。失败时可能保留完整、未引用的工具文件供下次识别，只有 provenance 和终态证据充分时才能清理，不能逆向猜测删除。
- 首跳更新证据：v0.2.1 已把下载连接/总超时扩展到 30 秒/10 分钟；已发布 v0.2.0 的下载器虽仍是 120 秒，但真实 `v0.2.0 -> v0.2.1` 一键更新已在 105.1 秒内自动替换并重启，无 staging/install 残留。
- 发布资产证据：`5,955,584` / `2,228,224` bytes 与 `4DDC…CED7A` 只保留为本地历史候选；最终 tag-CI 与 Release 回下载均为 `2,214,400` bytes、SHA-256 `8F6EA219A53BB3395F039327A3CD3827B53EE67B8DAF4B130E60235940A3020C`，合同闭环已完成。
- 已知 updater 多实例残余：target-keyed OS lease 已覆盖 helper 的替换、ACK 与回滚窗口，并由启动清理再次取得；应用本身仍未实现 singleton，因此多个普通实例仍可同时打开，但同一路径的更新 helper 会 fail closed，不能并发替换同一 EXE。
- 已知诊断残余：diagnostics 是 fail-open 支持旁路，不是业务事务或完整崩溃取证系统；panic hook 在 `panic = "abort"` 前只尽力写最小事件，硬卡死、访问冲突、强制结束、断电和 Windows WER 不保证捕获。Windows named mutex 在同一 `Local\` 登录会话、同一词法 root 标识内协调；跨登录会话或同目录不同路径别名不保证共享锁，14 天窗口也会受系统墙钟大幅回拨影响。exporter 正常 unwind 会尽力删除同目录 staging，但最终发布前硬崩溃仍可能留下 `.chatgpt-switch-diagnostics.<pid>.<sequence>.tmp`，clear 不处理它。health 只提供 best-effort 结构化摘要与计数，不包含完整 route/config/auth、进程命令行、SQLite `quick_check`、磁盘自由空间或 WER；任一 collector 失败会逐项 unavailable。备用导出只允许用户显式选择固定应用诊断目录，不是任意位置选择；全部 PRD/组合工作树/真实 EXE/发布验收仍须用最终检查证据确认。

<!-- DXM-TRELLIS:START -->

## DXM 大开发工作流链路

中大型开发、新模块、跨多文件重构、需求不清楚的任务，默认走：

1. 先用 `project-grill` 问清楚；有代码/文档时用 `grill-with-docs`，空项目用 `new-project-grill`，小脚本/demo 用 `lightweight-grill`。
2. 提问前先从代码和文档自行判断，能确定的是否题、偏好题、低风险取舍题作为推荐假设推进。
3. 把结论写入 `.trellis/tasks/<task>/prd.md`，不能只停留在聊天上下文里。
4. 用 `.trellis/scripts/task.py start <task>` 进入 Trellis active task。
5. 按 Trellis 的 implement/check/update-spec/finish 节奏开发、验证、沉淀规范。
6. 任务完成后同步 DXM 长期文档；不能只更新 `.trellis/` 内部状态。

<!-- DXM-TRELLIS:END -->

<!-- DXM-PROJECT-BASELINE:START -->

## DXM 项目基线

- Schema version: `1`
- Project root: local canonical path stored in `.dxm/project.json`; not embedded in shared Markdown.
- Goal: Maintain a safe Windows desktop control surface for ChatGPT official-account and Relay request routing, explicit session management, encrypted recovery, Skills, and self-update.

### Primary users / callers

- Windows users who operate ChatGPT/Codex locally and need auditable request-route switching and recovery

### Deliverables

- A Tauri desktop application with a typed React and Rust command boundary
- Official-login-preserving Account and Relay request-route switching
- Explicit session synchronization, encrypted backup and restore, Skill management, and verified self-update
- Executable tests, release gates, and synchronized long-term project documentation

### Non-goals

- Replacing the ChatGPT official login flow
- Sharing user credentials or publishing a hosted relay service
- Silently rewriting existing conversation files or terminating unrelated standalone Codex CLI processes

### Runtime entry points

- npm run tauri -- dev
- npm run tauri -- build --no-bundle
- src-tauri\target\release\codex-switch.exe

### Runtime facts

- The desktop UI is React/TypeScript and the native backend is Rust/Tauri on Windows.
- The live Codex root resolves from CODEX_HOME and otherwise defaults to the current user .codex directory.
- Official auth.json is sensitive and request-route switching treats it as read-only authority.
- Relay credentials are DPAPI encrypted at rest and projected only into the active managed config.toml provider.
- Request-route switching reaches a durable config-only terminal before optional bounded indexed session closure; manual complete sync remains an explicit closed-state mutation.

### Acceptance criteria

| ID | Criterion | Required evidence |
| --- | --- | --- |
| desktop-native-flow | The Tauri production artifact renders the dashboard and native window actions complete without a development server. | runtime, screenshot, test |
| auth-preserving-routing | Account and Relay routing preserve official login state, apply only the managed request configuration, and fail closed before mutation on invalid prerequisites. | runtime, test, source |
| storage-and-recovery-safety | Session, backup, restore, and update mutations remain bounded, encrypted where required, compensating, and independently testable. | test, source |
| quality-and-release-gates | Frontend and Rust tests, type checks, lint, production builds, release contracts, encoding checks, and secret checks are explicit and reproducible. | command, artifact |
| documentation-governance | Architecture, file ownership, development workflow, Git boundaries, and observable behavior stay synchronized in the governed project documents. | audit, source |

### Validation commands

- `npm test -- --run`
- `npm run typecheck`
- `npm run build`
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check`
- `cargo clippy --manifest-path src-tauri/Cargo.toml --locked --all-targets -- -D warnings`
- `cargo test --manifest-path src-tauri/Cargo.toml --locked --all-targets`
- `npm run tauri -- build --no-bundle`
- `npm run check:release`
- `git diff --check`

### Assumptions

- The maintained target is Windows and current-user DPAPI is available.
- ChatGPT owns official login refreshes; this application does not mint or replace official credentials.
- Git stage, commit, push, PR, tag, and release remain explicit user-authorized actions.

<!-- DXM-PROJECT-BASELINE:END -->

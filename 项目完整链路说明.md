# 项目完整链路说明

> 项目：`codex-switch`
> 根目录：`<repo-root>`
> 初始化日期：`2026-06-23`

本文档说明当前项目从输入、配置、启动、核心处理到产出的完整链路。开发前应先读 `项目文件结构说明.md`，再读本文，最后对照 `项目开发规范（AI协作）.md` 做变更。

<!-- DXM-DOC-RULES:START -->

<!-- DXM-CONTRACT:2 -->

## DXM 文档维护规则

- 本块由 DXM 管理；`--refresh-blocks` 只刷新本块，保留下方项目事实和人工补充。
- 项目目标、运行模式、配置链路、故障归因等长期事实必须写入下方对应章节，不能只留在聊天或临时文件里。
- 敏感配置和运行态输出只写用途，不回显真实 token、密码、API Key、账号明细、验证码、会话或密钥内容。
- 修改中文内容后必须检查可见乱码、错码和异常替换字符。

<!-- DXM-DOC-RULES:END -->

## 1. 项目目标

`ChatGPT Switch`（仓库与 EXE 兼容名为 `codex-switch`）是本机 ChatGPT 请求路由切换与 canonical 会话存储工具。

- 解决问题：在当前 ChatGPT 账号登录态与一个 API 中转站调用态之间安全切换，把无冲突的逻辑会话收敛为 OpenAI 主目录中的一份 canonical 正文，由 Account/Relay/Shared 数据库视图共同引用，并为 ChatGPT/Codex 环境安装可配置的 Image2/Grok 能力。
- 主要用户：当前 Windows 主机上的 ChatGPT Desktop / Codex CLI 使用者。
- 成功产物：React + Tauri 桌面应用，具备固定双槽位、官方登录态保留、真实请求路由判定、canonical/数据库视图、全局引用图、一次性前台迁移、真实恢复验证、冲突与待恢复处理、离线 GC、显式 v0.2.x 降级、会话管理、固定 Skill 安装/配置、DPAPI 凭据、失败补偿、typed receipt、durable 脱敏操作记录，以及可按 operation/attempt 关联并导出的独立脱敏诊断事件。
- 运行态输入：默认从 `CODEX_HOME` 解析 OpenAI 主目录；未设置时使用 `%USERPROFILE%\.codex`。核心文件包括 `auth.json`、`config.toml`、`sessions\**\*.jsonl`、`archived_sessions\**\*.jsonl`、`session_index.jsonl`，以及当前实际 runtime 指向的 `state_5.sqlite`、`goals_1.sqlite`、`memories_1.sqlite` 和 `logs_2.sqlite`。全局引用图另发现 Account、Relay、Shared、legacy/relocated 与备份库存数据库；`sqlite\codex-dev.db` 只作为兼容探测项，不作为正文身份来源。
- 运行态输出：工具自身加密运行态、route-specific SQLite 视图、`session-storage-v1` Shadow/cache/provenance/control/operation ledger、用户选择位置的未加密迁移备份、7 天冲突/恢复包、显式降级包、durable 操作记录、诊断事件/ZIP、回收统计以及面向 UI 的 typed receipt。正文只存在于 canonical、真实冲突、限期恢复、显式降级或操作期临时适配边界。

## 2. 运行模式

当前已实现 / 已接入的运行模式：

- 桌面正常模式：Tauri 启动 React UI，顶部在“运行态 / 会话 / 存储 / 技能”之间切换。首屏只加载 Codex Home 摘要、固定运行态、当前运行态、最近操作和最近一次 Shadow 报告，不触发全盘扫描；会话、存储 control state 与备份域按页/按动作加载。各域保持独立 `loading | ready | error`。窗口关闭监听在挂载时预注册，所有 close event 先由前端阻止，再调用后端 `request_app_exit`；mutation 期间的关闭请求显示为排队状态，迁移提交/验证期间不允许强拆窗口。
- 固定双槽位模式：只管理 `plus`（ChatGPT 账号，内部兼容 ID）和 `relay`（API 中转站）两个槽位。账号导入必须验证 live `auth_mode = chatgpt`；覆盖使用页面内确认并归档旧的加密槽位文件。UI 分开显示已保存和当前精确匹配/模式匹配。
- 请求端切换模式：点击后只打开一个页面内 task overlay，并通过 Tauri `Channel` 保留运行态读取、目标感知认证校验、ChatGPT 检测/关闭、最小配置生成、进程状态检查、会话视图准备、请求端原子切换、认证状态/路由复核、route provenance、终态记录与启动的真实 phase/timestamp；UI 聚合为最多 6 个可见步骤。Account preflight 必须证明 live `auth.json` 为官方 `auth_mode = chatgpt`；Relay preflight 允许 auth/config 缺失，也接受可解析非官方 auth 并保持原字节，损坏 auth 仍 fail closed。需要变更时从受管 `ChatGPT.exe` / `OpenAI.Codex.exe` 根捕获唯一 AppUserModelID，用 ToolHelp 关闭进程树；独立 `codex.exe` CLI 只阻断不结束。可信 AUMID 会 best-effort 持久化，供后续双应用均注册且无运行根时精确选择；记录损坏/篡改/失效时不得降级猜测 EXE/PATH，持久根不可用时也只有注册目标唯一才可使用。关闭后重新冻结可选 auth/config，按目标生成最小 route patch：Relay 注入 `model_providers.openai_custom.experimental_bearer_token`、`supports_websockets = true`、受管 `sqlite_home`，并按 auth 快照把 `requires_openai_auth` 写为 `true` 或 `false`；Account 删除同一受管 provider 表并恢复原 SQLite home。写后要求 auth 原字节或缺失状态与关闭后快照完全一致、路由 exact；随后把 provider、实际配置模型和不含凭据的 runtime generation 写入 append-only route epoch。账本写入/复核失败时 route 结果仍可诊断，但 ChatGPT 保持关闭，不能产生新的无来源回合。
- API 中转站模式：用户只配置一个中转站，保存 base URL、model 和 API key；URL 必须是 HTTP(S)，不得内嵌凭据、query 或 fragment，未写 scheme 时补 `https://`，远程地址必须使用 HTTPS。首次保存必须有 Key；后续只有 origin 不变时空 Key 才保留旧密文，改变 scheme/host/port 必须输入新 Key。Relay 可在完全未登录、live auth/config 均不存在的 Codex Home 中保存和切换，且绝不创建或覆盖 `auth.json`。生产切换不发送 `/models` 或其他网络探测，也不显示 Validate/Direct 选择；实际连通性由切换后的 ChatGPT 请求反馈，本地仍 fail closed 校验 URL、凭据、可选认证状态和受管 route 字段。Account 保存/切换仍要求官方登录态。
- 会话视图/手机连续性模式：provider/account 切换始终复用 OpenAI 主目录唯一 `sessions/`，不改写首行 provider，也不创建永久 provider JSONL。Relay sibling SQLite view 位于 Account effective SQLite home 下的 `.codex-switch-session-views/relay-v2`：已有 `state_5.sqlite` 时做 Online Backup 和 `threads.model_provider` 归一；三个全局库先 checkpoint WAL，再以相同 file identity hard link 共享。完全新建 Home 则只建立受管空视图目录和 `lastCommonStateSha256 = null` 的 v2 状态，不伪造数据库；Relay 自然创建 state DB 后，返回 Account 时再通过 Online Backup/provider normalization 物化。目录污染、独立副本、hard-link 不支持、WAL busy、digest drift 或 canonical root 变化均在 route write 前 fail closed；bootstrap/route 失败删除本次创建的状态和空目录。手机连续性只保留为用户显式单会话兼容入口。
- 内容与冲突模式：语义关系按有效消息、顺序、工具调用/结果和 hash 判定。Equal、EqualExceptProvider 与严格 Prefix 可收敛；Divergent/Unknown 保留所有分支并进入冲突。冲突默认 defer；只有用户显式选择且时间可靠时才切换主版本，旧主版本进入 7 天回收，禁止 last-write-wins。
- v0.3 Shadow 会话存储模式：Dashboard 首屏只读取 `%APPDATA%\codex-switch\session-storage-v1\latest-shadow-report.json` 的最近脱敏报告。显式扫描或切换成功后的后台任务使用 single-flight/coalescing 与跨进程 scan lease，逐库 Online Backup + `quick_check` 后建立 Account/Relay/Shared/legacy/backup 全局引用图，并从 `turn_context` + route epoch 解析逐轮 provider/model/account 来源。跨库快照非原子，因此 online 固定 `deletionEnabled = false`；cache/report 不持久化正文、threadId、完整路径或 credential，hash cache 永远不能成为删除证据。
- 会话合并与修复模式：旧“完全同步”语义和 `sync_all_sessions` wire 已删除。升级但尚未完成 v0.3 一次性迁移时，`merge_and_repair_sessions` 只返回 typed blocked 终态，不关闭应用、不扫描全库、不物化 provider 正文。迁移完成后的实现只允许处理 missing/equal/provider-only/prefix 和数据库视图修复；Divergent/Unknown 必须进入冲突或“交给 Codex 排查”。本地合并与 Relay 发布是两个独立流程，旧双 current/shared provider 物化代码仅保留在 test-only 迁移/降级兼容 fixture，不能从日常命令到达。
- 一次性迁移模式：用户选择备份目录并关闭全部 writer 后，依次执行 `Preflight -> Backup -> BackupVerified -> PlanReady -> Applying -> Validating -> Committed`。预检建立稳定引用图、检查 SQLite/WAL、空间、活动句柄与变化文件；备份恢复到隔离目录并由真实 Codex 验证列表/读取/恢复；数据库先在副本中修改，canonical 校验前不删除历史副本。Applying/Validating 失败进入 `RollingBack -> RolledBack/Failed`，启动恢复读取 durable ledger，不后台无限重试。
- 离线 GC/恢复/降级模式：在线只扫描；成功切换、迁移、冲突处理、恢复导入和会话合并会合并请求新 Shadow，但 route 成功本身不直接请求 provider GC。自动清理开启时，后台只有在迁移已提交、scan identity 新鲜、零未完成操作且实时确认全部 writer 关闭后，才进入与手工入口相同的离线执行器。自动 GC、未完成 GC recovery 和隐私 retention 都先检查 generation/writer，再尝试 mutation guard，并在取得 guard 后复检；绝不持有全局 mutation lock 等待本机 Codex/ChatGPT 退出。窗口未形成时零删除、释放或不取得 guard，保留 coalesced 最终校准并等后续安全触发复检。writer-closed 离线 GC 在每个候选删除前重新证明 Switch marker、确定 canonical peer、Equal/Prefix、两次稳定、全局 runtime 零引用、无句柄/写入；安全检查失败则停止并报告，冲突、恢复包、降级包和未知来源永不作为 provider GC 候选。关闭自动清理只停止 provider 副本删除，Shadow 扫描/报告与诊断、operation/迁移审计、恢复包的 7 天隐私 TTL 继续执行。旧备份先实际验证，再把主库缺失会话提取到待恢复区、延续允许显式恢复、分叉进入冲突。v0.2.0–v0.2.7 通过自描述隔离包降级，canonical 不动；包内数据库/索引保存绝对 rollout path，因此必须直接生成在最终目录且不可移动，换盘时从 canonical 重新生成；再次升级按 Equal/Prefix/Conflict 导入。
- 会话管理模式：默认展示 current + shared 合并视图，current 的显示元数据优先；支持搜索、排序、每页 50 条和跨页选择，并可将 current 归档会话“恢复可见”。v0.3 production command registry 不含会话 hard-delete，UI 也没有永久删除入口；旧 v0.2.x hard-delete/双根 Full 补偿实现仅作为 test fixture 保留。恢复可见创建单根 `StateOnly`，更新 current 的 `archived = 0` / `archived_at = NULL`；成功终态落盘后强复检并自动释放该临时点。shared-only 选择不执行无效恢复。
- Skill 模式：固定管理 `image2` 与 `grokSearch` 两个 wire ID，分别落到 `CODEX_HOME\skills\newapi-image2-client` 与 `CODEX_HOME\skills\grok-search`。Image2 来源 ZIP 的审查 SHA-256 固定为 `648C192C2414BBFD9DBA36E264C01932BDCF7E2057A8BA2DA7006B40A94B332B`；Grok 包移除本机路径、endpoint 和配置。包体通过 `include_bytes!` 进入 EXE，不在运行时下载或复制管理员目录。安装/更新由后端关闭并复检 ChatGPT，同时持有 mutation guard；本地漂移/未知目录需页面内确认，旧目录先移动到同卷备份再原子激活 stage；原子 journal 让下次安装能处理进程中断留下的 stage/backup/live 组合。URL/Key 配置由用户输入，Key 仅保存 DPAPI 密文；首次必填、后续空值保留；离开技能页会立即卸载确认/配置区并销毁局部 Key，取消或成功后恢复触发点焦点。
- 更新模式：每次应用启动只在当前进程内发起一次非阻塞检查，手动入口可再次触发；不注册后台服务、不在运行中轮询。Rust 后端固定请求本仓库最新正式 GitHub Release，8 秒超时、禁止重定向、限制 256 KiB、拒绝 draft/prerelease/非法 SemVer。发现新版后用户点击一次“立即更新”：更新先取得全局 mutation guard，UI 双向禁用其他写操作；后端只接受唯一固定名称的 Windows x64 `codex-switch.exe`，校验资产 URL、大小和 GitHub SHA-256 digest。v0.2.1 下载连接超时为 30 秒、总下载超时为 10 分钟；helper readiness 超时执行 kill + wait，staging 清理使用有界重试。staging 名使用 Windows CSPRNG，目录按进程是否 elevated 应用受限 DACL并持有目录句柄；helper 持有 canonical target lease，并以阶段 journal、旧/新 hash 和 Tauri `RunEvent::Ready` ACK 决定完成或回滚。已发布 v0.2.0 的首跳下载器仍是 120 秒总超时，但正式 `v0.2.0 -> v0.2.1` 已通过隔离 UIA 真实一键更新证明；10 分钟合同从 v0.2.1 后续更新开始生效。
- 完整备份、检查点空间与恢复模式：用户可从运行态页创建 current/shared 两根 Full；后端在 blocking worker 内持有 mutation guard，容量预检后关闭并复检 ChatGPT、拒绝独立 CLI，并返回两份已验证快照。Full 覆盖四个受管数据库、`sessions/`、`archived_sessions/` 和索引；Dashboard 只在用户请求时按时间从新到旧验证候选，最多返回 256 个有效恢复点供逐份恢复或删除。页面内“安全释放”严格解析操作日志，只接受带非空 `operationId + role` 且与唯一 terminal record 在 action/status/phase、目录、reason/scope、时间窗和 payload hash 全部匹配的 bound BackupManifest v4。未绑定 v2/v3 自动点 fail closed 保留；经强校验的 v2 legacy Full、v3 Full、v4 Full 仍可显式列出、恢复和确认删除，但不参与自动 cleanup。
- 诊断与支持模式：应用在读取 updater 启动参数前尽早从绝对 `APPDATA` 初始化随机 session、bounded store 与 panic hook；`sessionStarted` 带 PID/timestamp unit 和 best-effort process creation stamp，Tauri Ready/正常 Exit 分别记录 `appReady` / `sessionEnded`。下次启动在 liveness 字段可用时会用 PID + creation stamp 排除仍存活实例和 PID reuse 后再决定是否记录 `previousSessionUnclean`。所有用户主动 mutation 从 command boundary 记录完整 start/真实 phase/关键 branch/typed terminal；顶部工具栏“诊断”打开当前页面内面板，展示可用性、事件数、占用与 7 天/10 MiB 配置上限，并提供“导出最近诊断”“打开日志目录”和只清除 event segments 的确认。失败面拿到真实 operation/attempt ID 时导出该操作与开始前 10 分钟上下文，否则导出 retention window。默认导出失败后，用户可在同一页面重试 Downloads 或主动改存固定应用诊断目录；两个目标共用唯一 exporter，不显示原始事件，也不接受任意路径。

## 3. 配置加载链路

配置来源：

- Codex home 默认路径：优先 `CODEX_HOME`，否则 `%USERPROFILE%\.codex`，最后兜底 `HOME/.codex`；后续实现应允许高级设置覆盖。
- Codex SQLite home：优先读取 live `config.toml` 的 `sqlite_home`，其次读取环境变量 `CODEX_SQLITE_HOME`，最后回落到 Codex home。共享会话池内部固定使用自身目录，不继承用户侧 `sqlite_home`，避免共享池写到真实用户目录。
- 登录状态：live `auth.json`，含敏感 token。Account 目标要求官方 `chatgpt` 状态；Relay 目标允许文件缺失或可解析非官方状态。请求端切换始终把该文件/缺失状态视为只读权威快照，只做结构与 byte-exact/absence 校验，不写入、不恢复、不创建。
- Codex 配置：live `config.toml`，允许完全新建 Home 初始缺失。请求端切换保护全局配置，只 patch 模型/service tier/受管 provider 绑定；Relay 激活期间 bearer token 以明文存在受管 provider 表，固定写入 `supports_websockets = true`，并按 auth 快照写入目标感知 `requires_openai_auth`；Account 激活时删除该表。初始缺失的 config 在失败回滚后恢复为缺失。
- 工具配置：放在 `%APPDATA%\codex-switch`，保存运行态、legacy Shared 库、`session-storage-v1` 引用图/cache/provenance/control/ledger/限期恢复包、受管 launch target、既有备份、Skill 非敏感 config/DPAPI credential、严格 durable 的 `logs\operations.jsonl`，以及分库的 `logs\diagnostics\events-*.jsonl`。诊断只保存已结构化/限长/脱敏的支持事件，不参与业务终态或删除证明。

敏感字段不得在日志、UI、文档或错误报告中明文输出。

## 4. 核心流程

会创建快照的 mutation 使用：`preflight -> plan -> capacity -> backup -> apply -> verify -> persist terminal -> cleanup -> typed receipt`，失败时按各自 scope 补偿。请求路由是独立窄合同：`validate target-specific auth/config state + local Relay config -> close app -> rebuild route/session-view plan -> checkpoint existing shared global DB -> verify inactive state digest or bootstrap absence -> prepare target state view -> atomic config apply -> verify auth absence/bytes + route -> persist route epoch -> persist route terminal -> controlled launch`；它不执行 Relay 链接状态或 `/models` 网络探测，不创建 session checkpoint、不扫描或复制 JSONL。配置补偿前再次证明 live config 仍是本次原子写入；若被外部替换则保留外部文件、回滚本次受管会话视图并返回 `rollbackFailed`。bootstrap 尚无共同 digest 时同时出现独立 Account/Relay 数据库也 fail closed。route epoch 或 durable terminal 不可靠时不启动 ChatGPT。高风险前台迁移/合并/离线 GC 另走 ledger + verified full backup，不得把普通切换扩大成迁移。

1. 启动与扫描：`loadRuntimeDashboard` 只并发加载 Codex Home 摘要、固定运行态、当前运行态和最近操作，并用 `Promise.allSettled` 保留各域真实错误；另行初始化手机连续性 status，但不扫描会话正文。进入会话页时调用 `loadSessionDashboard`，备份由用户触发 `loadBackupDashboard`。请求端切换和单会话发布在 Tauri blocking worker 中串行执行；全局 Shadow 只由显式扫描或成功切换后的 coalesced background 请求触发。未迁移时“会话合并与修复”直接 blocked，不再运行旧完全同步。所有数据 mutation 与一键更新安装先获取进程内 try-lock，再持有 Windows 独占 `%APPDATA%\codex-switch\mutation.lock` 文件句柄。
2. 请求路由计划：switch preflight 读取目标槽位和可选 live `auth.json` / `config.toml`。Account 先证明官方 `chatgpt` 登录态；Relay 接受 auth 缺失或可解析非官方状态，损坏 auth 拒绝。Relay Key 只在内存中从 DPAPI 槽位解密。需要关闭 ChatGPT 时，关闭后重新读取可选 auth/config 并生成最小 patch；Account 恢复或权威删除模型/service tier 并移除受管 provider，Relay 只接受 `openai_custom`、复制安全 provider 字段、注入 bearer，固定 `supports_websockets = true`，并按 auth 快照设置 `requires_openai_auth`。Relay 缺失 auth/config 不是错误；目标配置损坏、Account 非官方 auth 或路由不完整都在 config 写入前 fail closed。
3. 会话/备份计划：请求路由 mutation 不创建 `RuntimeState`/`StateOnly`，也不读取会话正文。已有数据库时，Relay/Account 目标只 snapshot 小型 state DB、验证 inactive logical digest，并确认三个全局 SQLite 为同 file identity hard-link view；完全新建 Home 只计划空 Relay bootstrap，不制造 state DB。不存在普通切换的全库正文扫描或全局数据库复制。一次性迁移、合并修复、手工 Full 和 restore safety 另走各自显式安全门；旧 hard-delete 只存在于测试 fixture，不属于生产计划。
4. 应用：已有全局 SQLite 时先 checkpoint WAL，再对 source `state_5.sqlite` 做 Online Backup、在 staging 中只归一 provider、验证 provider-normalized logical digest 后原子替换 inactive state view；全新 Home 只发布版本化视图状态和受管空目录。随后使用同目录完整临时文件、flush/sync 和原子替换写 live `config.toml`。Relay 路由写入受管 `experimental_bearer_token`、目标感知 `requires_openai_auth`、`supports_websockets = true` 和 sibling `sqlite_home`；Account 路由删除整个受管 `openai_custom` 表并恢复原 SQLite home。
5. 验证：request switch 写后重新读取 `auth.json` 并与关闭后快照比较 byte-exact 或同为缺失，再通过 `detect_active_runtime` 按目标验证认证策略 + 路由 exact。配置写后的失败只在再次证明 ChatGPT/Codex 未恢复活动后回滚原始 `config.toml` 字节或删除本次新建 config，并再次验证 auth 状态未变；bootstrap 同时清理本次视图状态/空目录。备份/恢复、Skill 与会话操作继续执行各自 hash、SQLite 和路径验证。
6. 回执与终态：`RuntimeSwitchResult` 只保留 route changed、目标 runtime、warning、typed session-view receipt、`routeProvenance`、进程状态修复与 ChatGPT launch，不再携带恒空 backup/toShared/fromShared/checkpoint 字段。失败 progress 携带稳定 `RuntimeSwitchFailureReason`，UI 用它提供目标感知指导；后端原始 message 仅为次级细节。route epoch 只记录 operation/time/runtime/provider/account generation/configured model，不含路径、正文或凭据；原生 `turn_context` 的 timestamp/turn_id/实际 model 与最新有效 epoch 组合成每轮来源视图。成功切换只在来源账本和 durable terminal 都可验证后受控激活 ChatGPT；启动结果携带 `ChatGptLaunchFailureReason`，仅 activation/verification 失败提供同一 overlay 重试，target missing/ambiguous 要求先建立可信身份；来源失败则保持关闭且不提供误导性启动重试。
7. 诊断旁路：每个用户主动 mutation 都在 lock/spawn/可能早退之前从 command boundary 创建随机 attempt；worker/join/早退和后置步骤写真实 phase/安全 branch，durable ledger 发布成功时只绑定 operation ID，cleanup/mobile/launch/block/rollback 完成后才按 typed 语义写唯一 terminal。失败 Tauri rejection 使用固定、严格限长的信封携带 durable ID 或本次 attempt ID，前端统一解包且兼容裸字符串，因此失败导出不猜 latest。任何诊断失败都 fail-open，不改变原业务 `Result`。event 先经 allowlist、限长、路径/秘密 sanitizer，再 append + `sync_data` 到独立 segment；command/lifecycle recorder 与 panic 拿不到 root named mutex 都零等待放弃。年龄 prune 逐事件验证，只删 clean 且全部过期的段；容量仍可淘汰 clean/valid 最旧段，dirty tail、未知 schema、内部损坏和祖先 reparse fail closed。exporter 严格只读 operation ledger/health，在受控内存先完成固定五文件脱敏、hash 与 ZIP 自检，再解析目标并同目录 staging/no-clobber 发布；只有 destination failure 返回短期 opaque retry ID，Downloads 重试和用户主动 APPDATA fallback 复用同一 prepared bytes/hash/selection，不重新采集。
8. 前台存储 mutation：迁移、冲突切换、离线 GC、待恢复导入和降级再升级都先创建 `session-storage-v1/operations/<operationId>/ledger.json`，绑定 canonical root、源路径的脱敏身份、大小/hash、数据库副本、预期主版本和回滚步骤。每一阶段先持久化再写；正式切换前复检 writer-closed 与 inventory identity，写后执行内容、SQLite 和真实 Codex runtime 验证。失败先回滚数据库，再只删除本次创建且 hash 未变/全局零引用的残留；无法证明的文件保留并报告。

## 5. 模块与边界

当前模块边界：

- `codex_paths`：统一解析 Codex home、`sqlite_home`、会话正文目录和 `session_index.jsonl`，封装用户真实路径与共享池本地路径的差异。
- `codex_home`：路径发现、文件探测、运行态检测。
- `runtime_store`：固定账号/relay 双槽位、损坏 metadata fail-closed、DPAPI 认证存储、无碰撞覆盖历史和三文件失败回滚验证；不保存无关 live 全局配置。
- `crypto`：Windows DPAPI protect / unprotect。
- `process_control`：ToolHelp PID/PPID 进程树检测；只管理 `ChatGPT.exe` / `OpenAI.Codex.exe` 受管根，独立识别但绝不结束 standalone `codex.exe` CLI；关闭前从受管根读取并验证唯一 AppUserModelID，将可信 allowlist 身份 best-effort 持久化；无运行根时只使用仍注册的可信记录或唯一注册目标。切换后通过 `IApplicationActivationManager` 原生激活并重新验证同一身份，目标缺失/歧义、进程库存、激活、验证分别返回 typed failure。
- `request_route_switcher`：冻结可选 auth/config 状态并执行目标感知认证门，联合请求路由和会话视图 plan；目标视图准备成功后才原子写 `config.toml`，验证 auth byte-exact/absence 与 route exact；配置写后失败只补偿配置到原字节或原缺失状态，并返回稳定 typed route reason。
- `runtime_session_view`：持久化 Account configured/effective SQLite home、同卷 managed Relay sibling 和可空共同 logical state digest；已有数据时只 snapshot/归一 `state_5.sqlite`，三个全局库用 hard-link file identity + WAL checkpoint 共享；全新 Home bootstrap 不伪造数据库。inactive digest drift、独立目标文件或主目录漂移时不覆盖并 fail closed。
- `runtime_switcher`：保留切换 typed phase/receipt/launch/provenance 类型和 `RuntimeSwitchFailureReason`；不再负责 auth/config 切换或日常会话正文物化。
- `config_patch`：TOML 解析、保护字段、账号覆盖、diff。
- `session_scan`：按解析后的 `sqlite_home` 做 SQLite 只读扫描、dry-run 去重，并扫描 Codex home 下的 JSONL 正文。
- `session_incremental`：旧 v0.2 增量基线与兼容测试实现；v0.3 日常 request-route 不再调用它扫描/复制会话正文，迁移前合并修复直接 blocked。
- `session_sync`：保留 v0.2.x 升级迁移、显式降级导出/再升级和用户显式单会话兼容所需的 JSONL-first/no-clobber/provider metadata 逻辑；不从 v0.3 普通 route 或合并修复调用。历史 provider slot 的归属仍由 marker/created-prefix 证明，最终删除统一交给 `session_storage::offline_gc` 的全局引用门。
- `session_manager`：production 只提供 current/shared 合并视图、active/archived 会话发现和 current-only 可见恢复；v0.2.x hard-delete 的 Immediate 事务与双根 Full 补偿封装仅供 test fixture，未注册为 v0.3 command，也没有生产 UI 入口。
- `backup`：BackupManifest v4 scoped payload、自动点 `operationId + role` binding、RuntimeState process-state、SQLite Online Backup、按卷组合容量、active/archived Full/Sessions 范围及强校验/精确恢复。临时清理仅接受与唯一 terminal 全量匹配的 bound v4；未绑定 v2/v3 自动点及失败、孤儿、drift、unclassified 保留。v2/v3/v4 Full 仍可显式管理。
- `file_ops`：同目录临时文件、同步、平台原子替换，以及 hard-link no-clobber `atomic_create` 的统一实现。
- `operation_log`：业务语义 append-only、物理整文件原子发布的结构化终态记录；普通历史读取容忍旧的最后一条截断，严格 cleanup reader 与新 terminal 发布拒绝损坏日志/重复 ID，发布失败保持旧文件 byte-exact。已终结且不再承担回滚/恢复引用的脱敏记录由独立 privacy retention 在 7 天后回收，不能由诊断面板清除或 provider GC 开关控制。
- `diagnostics`：独立支持诊断域。`event` 定义 schema、Unix epoch 毫秒时间、OS CSPRNG/`CoCreateGuid` fail-open 的 session-attempt ID 与 typed terminal；`mod` 管理 operation correlation 和 terminal 后封闭状态机；`sanitize` 在落盘前限制字段、深度、数组和字符串并脱敏 secrets/URL/path；`store` 用 Windows root named mutex、segment 创建时间轮转、event timestamp 7 天读取窗口、10 MiB/512 KiB 容量、dirty-tail 封存、schema/reparse/internal-corruption 防护；`lifecycle` 负责 PID+creation-stamp start/ready/exit/previous-unclean/panic best-effort；`export` 负责固定五文件、更严格二次脱敏、hash/ZIP 自检和同目录 no-clobber 发布；`platform` 负责 Known Folder Downloads、本地时区、路径 containment 与 Explorer 打开边界。
- `diagnostic_commands`：诊断 Tauri 薄边界；返回 store status，按 retained window 或从 durable bind 反查真实 attempt 的完整事件自动选择，严格只读并脱敏 operation 子集，先注册有界 prepared context，再生成 typed receipt/failure。只有 destination failure 携带 retry ID，成功后一次性消费；只接受 opaque export ID 打开刚生成的包。health best-effort 汇总应用/平台、存储、route/auth mode、受管/standalone 进程计数和四个受管 SQLite 的 present/readable/bytes/只读 `schema_version`，失败逐项 unavailable。
- `skill_manager`：固定 Skill 包、manifest/hash/漂移状态、路径/reparse 校验、同卷 stage/备份/原子激活、URL 归一化和 DPAPI 配置存储。
- `update_check`：固定 GitHub latest-release 读取、stable SemVer 验证、响应大小/超时/重定向边界、并发检查 guard 和限长说明。
- `update_install`：固定 Release EXE 资产选择、GitHub digest/大小/URL 校验、30 秒连接/10 分钟总下载超时、CSPRNG staging、受限 DACL、目录句柄、canonical target lease、阶段 journal、`RunEvent::Ready` ACK、helper kill + wait、cleanup retry、失败回滚和 elevated 无参数恢复发现 fail-closed。
- `commands`：Tauri command 薄边界，负责固定路径/槽位、本地 Relay 配置校验、会话视图、Shadow/control、迁移、冲突、离线 GC、旧备份待恢复、降级、排查任务、进程门禁、AUMID capture/launch、typed receipt、终态日志和 Full 管理；不注册 Relay 网络探测或会话 hard-delete command。每个高风险存储命令在 blocking worker 内复用 mutation guard 与 operation ledger；`request_app_exit` 复用同一 guard，`launch_chatgpt` 只消费受控缓存身份，不接受前端路径/命令。

## 6. 输出文件与状态流

工具自身输出目录：`%APPDATA%\codex-switch`。

- `runtimes/plus/runtime.json`：ChatGPT 账号态元数据，不含敏感明文；目录名 `plus` 是内部兼容 ID。
- `runtimes/plus/auth.enc`：保存账号配置时产生的 ChatGPT 官方 `auth.json` 加密快照；请求端切换不读取它作为 live 写回源。
- `runtimes/plus/config.toml`：账号态 runtime overlay，只含切换需要的模型/provider 绑定。
- `runtimes/relay/runtime.json`：单个 API 中转站元数据，不含 API Key。
- `runtimes/relay/auth.enc`：加密后的中转站 API Key 登录态。
- `runtimes/relay/config.toml`：中转站 runtime overlay，包含 `model_provider = "openai_custom"`、`supports_websockets = true` 与 provider 连接参数，不含无关 live 全局配置、明文 API Key、`env_key` 或 `goal`；激活时才把解密 Key 投影到 live config 的 `experimental_bearer_token`。旧槽位中的缺失值或 `false` 会在下一次 Relay 切换时被权威纠正为 `true`。
- `launch-target-v1.json`：位于 `%APPDATA%\codex-switch` 的版本化受管 ChatGPT/Codex Windows AUMID 记录，只允许内置可信 allowlist、受限大小 regular file 且每次使用都确认仍已注册；损坏、篡改或失效时 fail closed，不包含路径、命令或凭据。
- `runtimes/<slot>/history/<timestamp>[-<sequence>]/`：覆盖前的完整槽位三件套；同毫秒归档使用递增后缀避免碰撞。
- `shared-sessions/state_5.sqlite`、`shared-sessions/sessions/`、`shared-sessions/archived_sessions/` 与 `shared-sessions/session_index.jsonl`：v0.2.x legacy 会话池与迁移库存。v0.3 canonical 就绪后 Shared 只保留数据库视图/引用，不再作为永久正文镜像增长；旧正文由一次性迁移按语义关系处理。
- `mobile-continuity-v1.json`：手机连续性 cutover 与持久队列；保存 thread ID、source fingerprint、typed 状态、尝试次数/下次重试和脱敏失败分类，不保存正文或凭据。损坏/版本漂移 fail closed，不自动重建并上传历史。
- `session-sync-state-v1.json`：v0.2.x legacy current/shared 对账基线；不进入 v0.3 普通请求端切换、合并修复或 canonical 日常写入。
- OpenAI 主目录 `sessions/YYYY/MM/DD/rollout-YYYY-MM-DDTHH-MM-SS-<uuid>.jsonl`：canonical 活动正文。普通 provider/account 切换持续引用同一逻辑 threadId/正文；只有真实分叉、7 天恢复、显式降级和操作期临时适配允许额外正文。
- OpenAI 主目录 `sessions/**/.<rollout-name>.codex-switch-slot-v1.json`：历史 Switch provider slot marker，记录 thread/provider、最初 origin、`createdBytes` 与 created-prefix SHA-256。它不是正文或备份；v0.3 只把 marker 完整有效、确定 canonical peer、Equal/Prefix、全局零引用且连续稳定的 slot 作为离线 GC 候选。
- `session-storage-v1/latest-shadow-report.json` / `hash-cache.json` / `route-epochs.jsonl` / `storage-state-v1.json`：最近脱敏扫描、可信缓存、逐轮来源 epoch 和 canonical/自动清理 control state；都不包含正文或完整用户路径。自动清理 control 只决定安全窗口是否执行 provider 副本离线 GC，不控制隐私 retention。
- `session-storage-v1/operations/<operationId>/`：迁移/GC/冲突/降级/恢复的 ledger、preflight/plan 和临时 staging；操作 ID 与 canonical root/inventory identity 绑定，7 天 privacy retention 只回收已经终结且无被引用 recovery 的受管内容，未终结账本不得为满足 TTL 被删除。
- `session-storage-v1/conflict-recovery/`、`pending-recovery/`、`restore-import-recovery/`、`offline-gc-recovery/`：有明确 manifest/hash/到期时间的恢复区；恢复包与 manifest 保留 7 天，到期删除仍需复核全局零引用。用户选择目录中的降级包寿命跟随用户包，不纳入自动 7 天删除。
- `backups/<timestamp>-<pid>-<counter>-<reason>/manifest.json`：当前 v4 记录 scope、tracked databases、自动点 binding、来源、大小与 SHA-256；current RuntimeState 可额外跟踪 process-state。未绑定 v2/v3 自动点保留；经强校验的 v2/v3/v4 Full 可进入最多 256 项的按需列表并显式管理。
- `logs/operations.jsonl`：脱敏终态记录；逻辑追加、物理同目录临时文件同步后原子整文件发布，不保存凭据、请求/响应正文或自由文本错误。已终结且无恢复引用的记录按 7 天隐私生命周期回收；该 TTL 与 provider 副本自动清理开关独立。
- `logs/diagnostics/events-<timestamp>-<pid>-<session>-<sequence>.jsonl`：独立的脱敏诊断 segment；每次启动使用不可稳定关联的随机 session ID，单段默认 512 KiB，同一 Windows 诊断根以 named mutex 跨进程维持 10 MiB 容量。segment 文件名创建时间负责轮转/prune，读取再按 event `timestamp` 精确过滤最近 7 天，容量可能使窗口更短；每段唯一未完成尾记录可丢弃且该 dirty 段不再追加，内部损坏/未知 schema 明确报错。自由文本业务 UUID 和未知绝对路径在落盘前脱敏；结构化诊断关联 ID 保持。清除入口只删除这类受管 regular/non-reparse segment，不触碰 `operations.jsonl` 或业务数据。
- Windows Known Folder Downloads 下的 `ChatGPT-Switch-Diagnostics-<YYYYMMDD-HHMMSS-mmm>[-N].zip`：用户主动导出的支持包，固定包含 `README.txt`、`manifest.json`、`diagnostics.jsonl`、`operations.jsonl`、`health.json`；manifest 以 `timestampUnit = unixEpochMilliseconds` 声明 event `timestamp` 的 Unix epoch 毫秒单位。retained window 使用实际事件 timestamp 的 min/max，相关 operation 以规范化时间区间求交，因果排序与墙钟回拨不会让 manifest 窗口漏掉已打包事件。同名使用数字后缀且不覆盖；ZIP 不由 APPDATA retention 自动删除。
- `%APPDATA%\codex-switch\diagnostic-exports\ChatGPT-Switch-Diagnostics-<YYYYMMDD-HHMMSS-mmm>[-N].zip`：Downloads 导出失败后由用户主动选择的固定备用支持包；使用相同五文件、同目录 staging/self-check/no-clobber 合同，不属于 event retention，也不会被“清除诊断日志”删除。
- `skills/image2/config.json`、`skills/grok-search/config.json`：位于 `%APPDATA%\codex-switch` 下的非敏感 URL、固定模型和 credential 路径；不含 Key。
- `skills/<skill>/credential.enc`：当前 Windows 用户的 DPAPI 密文，经 Base64 供安装后的 PowerShell helper 解密；UI 只返回配置布尔状态。
- 当前 `CODEX_HOME\skills\newapi-image2-client` / `grok-search`：受管 Skill 文件和 `.codex-switch-package.json`；不含用户配置或 Key。
- 当前 `CODEX_HOME\.codex-switch\skill-backups\<operation-id>`：覆盖前的完整旧 Skill 目录；当前不自动清理。
- 当前 `CODEX_HOME\.codex-switch\skill-transactions\<skill>.json`：目录 swap 的原子恢复 journal；成功后删除，异常残留由下一次该 Skill 安装先恢复。

## 7. 常见故障归因

- 请求端切换后不生效：先查任务执行器停在哪个真实阶段、ChatGPT 受管进程树、操作 ID/终态、live `config.toml` 目标 provider 下的 `supports_websockets = true` 等受管 route 字段，以及 `auth.json` hash/修改时间是否保持不变；应用不会预先探测中转站网络。缺失或非 `chatgpt` auth 会在写入前 fail closed；mode-only 需要重新应用。
- 手机连续性提示“需手动处理”：查看 `mobile-continuity-v1.json` 对应 item 的 typed status 与脱敏 failure category，再看 `IncrementalSync` 操作记录。旧会话、归档/删除、source 超过 8 MiB、路径/metadata 不一致、状态损坏或无法证明安全的分支按设计保留；不得因此重跑请求端切换或自动执行全量同步。
- 会话列表缺失：先查 live `sqlite_home/state_5.sqlite`、validated Account/Relay view state 与 `threads.rollout_path` 是否指向 OpenAI 主目录中 ID 一致的 canonical JSONL，再核对 route epoch 和 provider-normalized state digest。不要要求 JSONL 首行 provider 永远等于当前请求路由，也不要为修复列表手工生成 provider 槽位；迁移前缺失正文或索引问题只报告并走“交给 Codex 排查”。
- 归档会话没有同步回来：这是预期行为；归档会话只在会话管理页可见并可“恢复可见”，不会被自动同步或清理。v0.3 会话页没有永久删除入口。
- 会话重复或历史突然变短：查 `SessionReferenceGraph` 中该 threadId 的全部 runtime/backup 引用与 Equal/EqualExceptProvider/Prefix/Divergent/Unknown 分类，再核对当前 canonical path、有效消息数、工具配对和最后有效消息时间。单纯 provider 差异不构成冲突；Prefix 只保留完整延续；两个不同尾部必须保留并默认不覆盖。禁止按文件名、mtime、大小或当前单库零引用直接删副本。
- 迁移/GC 被阻止：先看 operation ledger 的阶段、安全 blocker、writer inventory、preflight identity、备份 `runtimeVerified`、空间和最近两次稳定 hash。在线扫描的零引用只用于报告；必须关闭全部 Desktop/CLI writer，并在最终写入/删除前重新取得全局引用、hash/marker、句柄和未变化证明。
- SQLite 写入失败：查 `sqlite_home` 严格解析结果、source/target `PRAGMA table_info(threads)`、2 秒 busy timeout、事务/quick_check、操作回执和两根备份。若出现 `NOT NULL constraint failed: threads.<column>`，先核对该目标列是否是 source 尚无但带 default 的 schema 新列；同步应省略缺失列让 SQLite 执行 default，而不是显式绑定 `NULL`。
- 配置损坏：无效 live TOML 会阻断路径解析/写操作；查解析错误、操作记录和最近已验证备份，不允许静默回落到默认数据库。
- Windows 原子替换报 `os error 3`：不一定是 live 目录丢失。完整加密备份会在深层 payload 目录生成更长的同目录临时文件；旧实现把超过传统 260 字符限制的普通路径直接传给 `MoveFileExW` 时也会报“找不到路径”。先核对备份目标与临时路径长度、应用版本和父目录是否可解析，不要直接重建或删除 Codex Home；修复后的实现使用规范化 verbatim 父路径并兼容目录联接。
- 操作成功但页面提示错误：mutation、runtime 刷新和 unknown 终态使用不同页面状态。成功后“正在确认当前运行态”期间再次切换会被禁用；刷新失败不改写 mutation 成功。未收到可信终态时先不要重新打开 ChatGPT，按 operation ID 与日志确认，不要重复点击。
- 诊断导出失败：先看诊断面板的可用性、Known Folder Downloads 是否可解析/可写、诊断目录是否为 regular directory，以及指定 operation/attempt 是否真有事件。页面不会静默改存：可重试 Downloads，或由用户主动点击“改存应用诊断目录”写入固定 `%APPDATA%\codex-switch\diagnostic-exports`；两处都失败时保留页面内错误，不允许指定任意目录。导出失败不会反转原业务结果，也不会递归记录自身错误形成日志风暴。
- 上一会话显示 `previousSessionUnclean`：它只说明最近一次已记录 `sessionStarted` 的随机诊断 session 缺少 `sessionEnded`，可能来自 panic、强制结束、断电、硬崩溃或诊断写入失败，不能单靠该事件确定根因。继续结合最后已持久 phase、typed terminal 和 durable operation record；硬卡死、访问冲突、断电、Windows WER 等可能完全没有诊断事件。
- C 盘备份目录持续增长：先确认使用版本。v0.2.0 的 changed switch 每次永久创建 current `Runtime` + shared `Sessions` 全会话 checkpoint，是按次下降的直接原因；当前请求端切换不创建任何 checkpoint，也不写会话/provider rollout。2026-07-26 受控 UI 清理实证为 `21` 目录/`6,327,089,609` bytes → `17` 目录/`2,693,977,957` bytes，剩余项是 Full/孤儿/失败/hash drift/无法分类等安全保留项。若空间继续增长，应归因到用户单独执行的同步、备份、恢复、管理、日志或 updater staging，不能归因到 request switch。
- 切换成功但 ChatGPT 未打开：运行态成功不应被回滚。先看 task overlay 的 `ChatGptLaunchResult.reason`；`launchTargetMissing` / `launchTargetAmbiguous` 表示当前没有可信唯一 AUMID，需要先从一次真实受管运行根建立身份，UI 不应提供重复无效重试；`activationFailed` / `verificationFailed` 才提供“重新打开 ChatGPT”原位重试。任何情况都不应从 PATH 手工执行不明同名程序。
- Skill 已安装但不可用：先检查页面是否显示“API Key 已加密配置”，再确认重启过 ChatGPT / Codex CLI；Image2 URL 必须以 `/v1` 结尾，Grok URL 在保存时会移除末尾 `/v1` 并由脚本调用 `/v1/responses`。不要读取 credential 文件排查明文。
- Skill 显示“本地已修改 / 非受管目录 / 安装异常”：工具不会静默覆盖；确认目录确实可被接管后使用覆盖安装，并保留回执中的旧目录备份。

## 8. 测试与验证

当前真实可用验证命令：

- `npm test -- --run`：Vitest 前端 Dashboard、会话与技能页测试。
- `npm run typecheck`：TypeScript 静态检查。
- `npm run build`：Vite 前端生产构建。
- `cargo fmt --manifest-path src-tauri/Cargo.toml -- --check`：Rust 格式门禁。
- `cargo clippy --locked --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings`：Rust lint 门禁。
- `cargo test --locked --manifest-path src-tauri/Cargo.toml`：Rust 后端单元测试。
- `cargo test --manifest-path src-tauri/Cargo.toml --test skill_manager_contract`：临时 `CODEX_HOME` Skill 安装、漂移、备份、URL、DPAPI 与 Windows PowerShell 兼容合同。
- `cargo test --manifest-path src-tauri/Cargo.toml --test synthetic_large_home`：隔离的多文件 provider 切换回归，验证内容等价 JSONL 的文件数、bytes、SHA-256 与 mtime 不变；ignored benchmark 可用 `CODEX_SWITCH_SYNTHETIC_SESSION_COUNT` / `CODEX_SWITCH_SYNTHETIC_SESSION_KIB` 放大。
- diagnostics Rust/前端回归属于 `cargo test --locked --manifest-path src-tauri/Cargo.toml` 与 `npm test -- --run` 的发布门禁；必须覆盖所有用户主动 mutation 的 begin-before-lock/spawn/早退、真实 phase/安全 branch、attempt→durable 只绑定、worker join/后置步骤、唯一 typed terminal、无 durable record lifecycle、后台只记录失败和 diagnostics fail-open；还要覆盖 schema/sanitizer、路径 tokenization、每段 partial tail/dirty-tail 封存/internal corruption、segment 创建时间轮转、event timestamp 7 天精确过滤、10 MiB/512 KiB、Windows root named mutex 并发/panic 零等待/clear/post-prune、operation ledger byte-exact 分离、operation terminal 封闭、lifecycle PID+creation stamp、health 只读字段/逐项 unavailable、固定五文件/hash/ZIP self-check、Known Folder/用户显式备用目录/no-clobber/冲突/失败、staging 正常清理与硬崩溃残余边界、顶部面板/失败导出/焦点与 390px 可达。
- `npm run tauri -- build --no-bundle`：不生成安装包的正式桌面 UI/E2E 候选；仍会执行 `beforeBuildCommand`、加载 `frontendDist` 并使用生产 custom protocol。裸 `cargo build --release` 只证明 Rust 编译，可能继续请求 `devUrl`，不得用它代替桌面 UI 实测。
- `npm run tauri -- build`：完整 Tauri 桌面应用 release 编译与安装包构建，当前输出 `src-tauri\target\release\codex-switch.exe`。
- `npm run check:release`：校验五处版本、tag（如适用）、PE 格式/大小及构建机路径与常见 token marker。
- `.\scripts\pack-windows-release.ps1 -UpxPath "<verified-upx.exe>"`：固定验证官方 UPX 5.2.0 与 `upx.exe` SHA-256 `F4C0CC7ACA0F1FF0D0B750E966B44139F2FA1A2DB7281F48FC52194400712E1D`，冻结并验证 raw hash/contract，只复制到 staging 后执行 `--ultra-brute --lzma`，再执行 `upx -t`、packed contract、PE32+ x64/双版本与 3,000,000 bytes 硬门禁；raw 不原地修改。v0.3 禁用 Tauri 运行时 Brotli asset feature，由最终固定 UPX 层统一压缩嵌入资源，生产 custom protocol 仍需实际启动验证。

Cargo release profile 固定 `opt-level = "z"`、`lto = true`、`codegen-units = 1`、`panic = "abort"`、`strip = "symbols"`。`.github/workflows/ci.yml` 在 `windows-latest` / Node 22 / 固定 Rust 1.94.1 上执行前端/Rust 门禁并构建 raw Tauri EXE；随后从官方固定 URL 下载 UPX 5.2.0 ZIP，验证 ZIP SHA-256 `B471EBF1B7F20F4A89150264ED9A008A2A5BFD247F3C6D1184A75BB59CA08F5D` 与 EXE SHA-256 `F4C0CC7ACA0F1FF0D0B750E966B44139F2FA1A2DB7281F48FC52194400712E1D`，调用统一脚本，并只上传 packed 的 `release/codex-switch.exe`。本地临时候选 raw `5,955,584` bytes → packed `2,228,224` bytes（SHA-256 `4DDC…CED7A`）仅作为历史候选；v0.2.1 tag-CI/Release 的正式资产为 `2,214,400` bytes、SHA-256 `8F6EA219A53BB3395F039327A3CD3827B53EE67B8DAF4B130E60235940A3020C`。

## 9. Release 发布链路

当前公开 release 采用 GitHub Releases + Windows 单文件可执行产物：

1. 同步版本号：`package.json`、`package-lock.json`、`src-tauri/Cargo.toml`、`src-tauri/Cargo.lock`、`src-tauri/tauri.conf.json`。
2. 同步公开文档：`README.md` release badge / 功能说明、`CHANGELOG.md`、根目录长期文档。
3. 运行完整验证：`npm test -- --run`、`npm run typecheck`、`npm run build`、Rust fmt/clippy/test、必要的临时 `CODEX_HOME` E2E/故障注入/恢复演练、敏感扫描、乱码检查和 `npm run tauri -- build`；对 raw EXE 运行 release contract。最终 Trellis `check.md` 必须先满足 PASS 门，但任务此时保持 active，不得提前 finish/archive 或生成完成式 receipt。
4. 使用固定官方 UPX 5.2.0 调用 `scripts/pack-windows-release.ps1`，证明 raw hash 未变、`upx -t` 成功、packed 仍为 PE32+ x64、双版本一致、release contract 通过且不超过 3,000,000 bytes。
5. 提交并推送版本提交。
6. 创建与 manifest 一致的版本 tag，例如 `v0.2.1`，并推送 tag。
7. 从对应 tag commit 的成功 CI run 下载唯一 packed artifact，用 `gh release create` 或 `gh release edit` 发布；不得上传 raw EXE，也不得另行上传未经相同门禁验证的本地产物。
8. 用 `gh release view <tag>` 和 `git ls-remote --tags origin <tag>` 验证远端 release/tag，重下载唯一资产并复核大小、SHA-256、PE32+ x64 与双版本，再用 `scripts/v030-updater-e2e.mjs --old-tag <previous> --new-tag <tag>` 从同一路径正式旧版执行 updater 成功与 replacement-lock 回滚 E2E。该 gate 只操作隔离安装目录中的 ChatGPT Switch，并通过正常退出/Ready ACK 收口，不终止本机 ChatGPT/Codex。
9. 只有公开资产回下载和 updater 成功/回滚证据都绑定到精确发布 commit 后，才执行 `task.py finish`、`task.py archive <task> --no-commit`，最后生成并校验 schema v2 completion receipt。该 receipt 不能在 public evidence 前宣称任务完成。

应用内检查与一键更新依赖该链路的正式 release/tag/资产版本一致性。公开 UI/窗口标题使用 ChatGPT Switch，但 Release 必须包含唯一 packed `codex-switch.exe` 资产和 GitHub 生成的 SHA-256 digest；v0.1.9 updater 固定校验该名称，不能改为 `chatgpt-switch.exe`。不得只推 tag 不建 Release，不得上传 raw/packed 两份资产，也不得让 Release tag、Rust crate、Tauri config 和 npm 版本漂移。

v0.2.1 已完成整条发布链：PR #5 从工作提交 `702dc37` 合并为 `3b4f440`，PR CI `30194264349` / `30194276794`、main CI `30194772843` 与 tag CI `30195207004` 均通过；annotated tag `v0.2.1` 指向 `3b4f440`。[正式 Release](https://github.com/mingisrookie/codex-switch/releases/tag/v0.2.1) 为 latest stable、非 draft、单资产；tag artifact 与 Release 回下载的 `codex-switch.exe` 均为 `2,214,400` bytes、上述同一 SHA-256、PE32+ x64、`FileVersion/ProductVersion = 0.2.1`，并通过 `upx -t`。两个 ignored live GitHub 合同测试各 `1 passed`。

正式 v0.2.0（SHA-256 `42012…A65A`）已在隔离 UIA 中真实点击“立即更新”：约 `2.086s` 出现并点击，旧进程约 `89.103s` 后 exit `0`，`105.1s` 内自动替换并重启 ChatGPT Switch；目标文件与正式 v0.2.1 Release 的 bytes/hash/version 一致，staging/install leftovers 均为 `0`，没有手工替换。本机 Microsoft Defender Product/Feature disabled，相关命令无法形成扫描通过证据，不能用上述合同或更新闭环替代 Defender 结论。

v0.2.7 已完成整条发布链：门禁修复提交 `647b42e` 经 PR #9 合并为 `74832037710520e8ecb0d43c0bb163f32d46ca20`，PR CI `31328121903` / `31328135186`、main CI `31328794302` 与 tag CI `31329458760` 均通过；annotated tag `v0.2.7`（tag object `8e47e3d8d2ba24905689f5b704bc229743c1aee6`）指向该合并提交。[正式 Release](https://github.com/mingisrookie/codex-switch/releases/tag/v0.2.7) 为 latest stable、非 draft、单资产；tag-CI artifact 与公开回下载的 `codex-switch.exe` 均为 `2,411,008` bytes、SHA-256 `E8905135EAB0C5D76117BAA25770371E3D8BFDAF07495805463C96A823A5B78E`、PE32+ x64、`FileVersion/ProductVersion = 0.2.7`，并通过 UPX 5.2.0 完整性检查、两个 ignored live GitHub 合同测试和正式 `v0.2.5 -> v0.2.7` 一键更新。`v0.2.6` 只有 annotated tag；它在 GitHub Release 前被最终独立隐私审查拦截，未成为 Release/Latest，旧 tag 未移动。

## 10. 发布门禁与剩余边界

- 必须从干净工作区实际运行本节列出的前端/Rust门禁；不能只依赖历史结果或 CI 配置。
- 临时 `CODEX_HOME` E2E 至少覆盖账号态 → Relay → 账号态的 canonical 视图、一次性迁移幂等、完整备份真实恢复、冲突、离线 GC、降级再升级；不得用真实 `.codex` 作为首轮写入目标。
- 故障注入至少覆盖每个迁移阶段、运行态文件写入后失败、数据库/JSONL 部分提交、磁盘满、文件占用、SQLite 写失败、恢复包损坏和回滚中断。
- 敏感扫描必须覆盖 UI 构建产物、操作记录、备份 manifest 和错误文本；不得出现 token/API Key/响应正文。
- 中文乱码检查必须覆盖本轮修改的 Markdown、TSX 和 Rust 文案。
- 已知产品残余：v0.3 只自动回收严格受管、终结、到期且零引用的冲突/恢复/操作包；用户显式 Full/降级包、失败、孤儿、来源不明和证据不足目录不会自动删除。容量预检与“可证明回收”不能被误述为所有旧备份都安全。
- 已知更新安全残余：helper 已有受限 staging、目录句柄和持久化阶段 journal，但 GitHub digest 与 Release 元数据仍来自同一控制面，尚无独立固定公钥/Authenticode 信任根。引入发布签名基础设施前不得宣称能抵抗仓库发布权限失陷。
- 已知一致性残余：SQLite、JSONL 和外部 Codex runtime 没有共同尊重的单一跨介质事务。v0.3 通过数据库副本、完整 no-clobber 文件、持久 ledger、最终 writer/inventory 复检、真实 runtime 验证和可恢复原子切换缩窄窗口；无法证明时回滚或保留残留，不把“尽力而为”误述成原生全局事务。
- 已知 legacy provider 副本残余：升级前副本、marker 漂移、数据库仍引用、活动句柄、真实分叉或来源不明文件会安全保留，因此迁移后可能仍有有理由的第二份正文；但 v0.3 日常请求路由永久 provider slot 新增数必须为 0，临时适配文件操作后必须归零。
- 已知 source TOCTOU 残余：执行期对 source ID、语义尾、长度、SHA-256、file identity 和全局引用做有界复检；外部 writer 可随时让计划失效。该情况必须 fail closed 并保留正文，只有 provenance、稳定性、零引用和终态证据充分时才能回收，不能逆向猜测删除。
- 首跳更新证据：v0.2.1 已把下载连接/总超时扩展到 30 秒/10 分钟；已发布 v0.2.0 的下载器虽仍是 120 秒，但真实 `v0.2.0 -> v0.2.1` 一键更新已在 105.1 秒内自动替换并重启，无 staging/install 残留。
- v0.2.1 发布资产证据：`5,955,584` / `2,228,224` bytes 与 `4DDC…CED7A` 只保留为本地历史候选；最终 tag-CI 与 Release 回下载均为 `2,214,400` bytes、SHA-256 `8F6EA219A53BB3395F039327A3CD3827B53EE67B8DAF4B130E60235940A3020C`。
- v0.2.7 发布资产证据：唯一 tag-CI artifact 与 Release 回下载均为 `2,411,008` bytes、SHA-256 `E8905135EAB0C5D76117BAA25770371E3D8BFDAF07495805463C96A823A5B78E`；版本、PE32+ x64、UPX 5.2.0、Latest、live 合同测试和 updater 首跳均已闭环。
- 已知 updater 多实例残余：target-keyed OS lease 已覆盖 helper 的替换、ACK 与回滚窗口，并由启动清理再次取得；应用本身仍未实现 singleton，因此多个普通实例仍可同时打开，但同一路径的更新 helper 会 fail closed，不能并发替换同一 EXE。
- 已知诊断残余：diagnostics 是 fail-open 支持旁路，不是业务事务或完整崩溃取证系统；panic hook 在 `panic = "abort"` 前只尽力写最小事件，硬卡死、访问冲突、强制结束、断电和 Windows WER 不保证捕获。Windows named mutex 在同一 `Local\` 登录会话、同一词法 root 标识内协调；跨登录会话或同目录不同路径别名不保证共享锁，7 天窗口也会受系统墙钟大幅回拨影响。exporter 正常 unwind 会尽力删除同目录 staging，但最终发布前硬崩溃仍可能留下 `.chatgpt-switch-diagnostics.<pid>.<sequence>.tmp`，clear 不处理它。health 只提供 best-effort 结构化摘要与计数，不包含完整 route/config/auth、进程命令行、SQLite `quick_check`、磁盘自由空间或 WER；任一 collector 失败会逐项 unavailable。备用导出只允许用户显式选择固定应用诊断目录，不是任意位置选择；v0.2.7 已用最终组合工作树、真实 packed EXE 和公开 Release 完成本轮验收，这些产品边界仍继续保留。

<!-- DXM-TRELLIS:START -->

## DXM 大开发工作流链路

中大型开发、新模块、跨多文件重构、需求不清楚的任务，默认走：

1. 先用 `project-grill` 问清楚；有代码/文档时用 `grill-with-docs`，空项目用 `new-project-grill`，小脚本/demo 用 `lightweight-grill`。
2. 提问前先从代码和文档自行判断，能确定的是否题、偏好题、低风险取舍题作为推荐假设推进。
3. 把结论写入 `.trellis/tasks/<task>/prd.md`，不能只停留在聊天上下文里。
4. 用 `.trellis/scripts/task.py start <task>` 进入 Trellis active task。
5. 按 Trellis 的 implement/check/update-spec/finish 节奏开发、验证、沉淀规范。
6. 任务完成后同步 DXM 长期文档；不能只更新 `.trellis/` 内部状态。

<!-- DXM-TRELLIS:END -->

<!-- DXM-PROJECT-BASELINE:START -->

## DXM 项目基线

- Schema version: `1`
- Governance profile: `standard`
- Project root: local canonical path stored in `.dxm/project.json`; not embedded in shared Markdown.
- Goal: Maintain a safe Windows desktop control surface for ChatGPT official-account and Relay request routing, explicit session management, encrypted recovery, Skills, and self-update.

### Primary users / callers

- Windows users who operate ChatGPT/Codex locally and need auditable request-route switching and recovery

### Deliverables

- A Tauri desktop application with a typed React and Rust command boundary
- Official-login-preserving Account and Relay request-route switching
- Explicit session synchronization, encrypted backup and restore, Skill management, and verified self-update
- Executable tests, release gates, and synchronized long-term project documentation

### Non-goals

- Replacing the ChatGPT official login flow
- Sharing user credentials or publishing a hosted relay service
- Silently rewriting existing conversation files or terminating unrelated standalone Codex CLI processes

### Runtime entry points

- npm run tauri -- dev
- npm run tauri -- build --no-bundle
- src-tauri\target\release\codex-switch.exe

### Runtime facts

- The desktop UI is React/TypeScript and the native backend is Rust/Tauri on Windows.
- The live Codex root resolves from CODEX_HOME and otherwise defaults to the current user .codex directory.
- Official auth.json is sensitive and request-route switching treats it as read-only authority.
- Relay credentials are DPAPI encrypted at rest and projected only into the active managed config.toml provider.
- Request-route switching reaches a durable config-only terminal before optional bounded indexed session closure; manual complete sync remains an explicit closed-state mutation.

### Acceptance criteria

| ID | Criterion | Required evidence |
| --- | --- | --- |
| desktop-native-flow | The Tauri production artifact renders the dashboard and native window actions complete without a development server. | runtime, screenshot, test |
| auth-preserving-routing | Account and Relay routing preserve official login state, apply only the managed request configuration, and fail closed before mutation on invalid prerequisites. | runtime, test, source |
| storage-and-recovery-safety | Session, backup, restore, and update mutations remain bounded, encrypted where required, compensating, and independently testable. | test, source |
| quality-and-release-gates | Frontend and Rust tests, type checks, lint, production builds, release contracts, encoding checks, and secret checks are explicit and reproducible. | command, artifact |
| documentation-governance | Architecture, file ownership, development workflow, Git boundaries, and observable behavior stay synchronized in the governed project documents. | audit, source |

### Validation commands

- `npm test -- --run`
- `npm run typecheck`
- `npm run build`
- `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check`
- `cargo clippy --manifest-path src-tauri/Cargo.toml --locked --all-targets -- -D warnings`
- `cargo test --manifest-path src-tauri/Cargo.toml --locked --all-targets`
- `npm run tauri -- build --no-bundle`
- `npm run check:release`
- `git diff --check`

### Assumptions

- The maintained target is Windows and current-user DPAPI is available.
- ChatGPT owns official login refreshes; this application does not mint or replace official credentials.
- Git stage, commit, push, PR, tag, and release remain explicit user-authorized actions.

<!-- DXM-PROJECT-BASELINE:END -->

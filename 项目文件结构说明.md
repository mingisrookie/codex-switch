# 项目文件结构说明

> 项目：`codex-switch`
> 根目录：`<repo-root>`
> 初始化日期：`2026-06-23`

本文档列出当前项目维护视角下的文件结构与职责。新增、删除、重命名源码、配置、长期文档后，必须同步更新本文。

<!-- DXM-DOC-RULES:START -->

## DXM 文档维护规则

- 本块由 DXM 管理；`--refresh-blocks` 只刷新本块，保留下方文件职责和人工补充。
- 新增、删除、重命名文件或改变目录职责后，直接更新下方对应条目，不堆叠临时增量说明。
- 运行态、缓存、账号、密钥、环境变量和构建产物只说明用途与保护边界，不回显真实内容。
- 当前根目录快照是初始化种子；首次 `/dxm` 后需要根据真实子目录和模块职责继续补充。

<!-- DXM-DOC-RULES:END -->

## 维护原则

- 文件职责发生明显变化时，必须更新对应说明。
- 运行态数据文件可以记录用途，但不得写入真实 token、密码、API Key、账号明细或会话内容。
- 本文档不保留“增量补充”堆叠章节；结构变化时，直接更新对应文件条目与职责说明。

## 根目录长期文档

- `AGENTS.md`：目录级 AI 执行规则，定义 `/dxm` 触发与本目录强制遵守事项。
- `项目开发规范（AI协作）.md`：AI/开发者协作规范、架构边界、测试规范、文档同步和乱码检查要求。
- `项目完整链路说明.md`：项目从配置、输入、执行到输出的完整运行链路。
- `项目文件结构说明.md`：项目文件结构与职责边界。
- `开发者AI开发与PR提交流程.md`：分支、PR、GitHub CLI、合并授权等协作流程说明。

## 当前根目录快照

- `.agents/skills/`：Trellis 生成的本项目工作流 skill 入口。
- `.github/workflows/ci.yml`：Windows GitHub Actions 发布质量门禁；执行前端/Rust 门禁与 raw Tauri release contract，再从官方固定 URL 下载 UPX 5.2.0，验证 ZIP/EXE 双 SHA-256，调用统一脚本打包，只上传 packed 的 `release/codex-switch.exe`。v0.2.1 的 PR/main/tag CI 均通过；正式 tag-CI 与 Release 回下载的唯一资产均为 `2,214,400` bytes、SHA-256 `8F6EA219A53BB3395F039327A3CD3827B53EE67B8DAF4B130E60235940A3020C`。本地 raw `5,955,584` → packed `2,228,224` bytes（`4DDC…CED7A`）只保留为历史候选，真实 `v0.2.0 -> v0.2.1` 一键更新也已闭环。
- `.gitignore`：忽略依赖、构建产物、本地运行态和 Trellis runtime/workspace。
- `.trellis/`：Trellis 任务、规范、脚本和当前 MVP task 状态。
- `AGENTS.md`：项目文件；首次 /dxm 生成后需要补充职责说明。
- `docs/`：项目设计、证据和后续实现文档目录。
- `docs/design/codex-switch-image2-runtime-redesign-no-plus-2026-06-23.png`：Image2 生成的运行态切换新版 UI 参考图，文案使用“Codex 账号态”而非套餐名。
- `docs/evidence/2026-06-23-local-codex-storage-scan.md`：本机 Codex 存储结构只读扫描证据，不含敏感明文。
- `docs/prd/2026-06-23-codex-switch-prd.md`：Codex 运行态切换与本地会话同步 PRD；保留初始调研历史，并以 2026-07-13 release-hardening 合同收敛到固定双槽位。
- `docs/superpowers/plans/2026-06-23-codex-switch-mvp.md`：MVP 实现计划。
- `docs/superpowers/plans/2026-07-13-release-hardening.md`：全栈发布硬化实施计划，覆盖备份、运行态、同步/删除、前端真实状态、relay 验证和质量门禁。
- `docs/superpowers/plans/2026-07-14-skill-bundles.md`：用户已确认的 Image2/Grok 内置 Skill 安装、DPAPI 配置、原子覆盖与跨层测试设计。
- `docs/architecture/platform-readiness-2026-07-14.md`：当前 Windows x64 产品边界与未来 macOS/Linux 的路径、密钥库、进程控制、锁、Skill runtime、CI 迁移矩阵。
- `docs/reviews/2026-07-26-product-reliability-review.md`：v0.2.0 产品、客户、可靠性、安全、性能与可访问性审查，以及远端 G11 发布闭环、v0.2.1 二次审计和最终发布 addendum；记录 PR #2 的不直接合并结论、PR #5/CI/tag/Release/真实首跳证据、最终 cleanup 终态、Defender 不可用边界和仍需保留的产品风险。
- `docs/reviews/2026-07-26-cc-switch-session-sync-review.md`：CC-Switch 会话迁移实现的源码级对照审查；记录可借鉴的一次性迁移机制，以及不应搬进每次运行态切换的整文件 migration。
- `scripts/tauri.mjs`：统一 Tauri CLI 入口；保留调用参数并为 release/dev Rust 编译注入工作区和用户目录路径重映射，避免发布 EXE 泄漏构建机绝对路径。
- `scripts/pack-windows-release.ps1`：Windows release 的 copy-only 打包入口；调用方显式传入 UPX 路径，脚本固定官方 UPX 5.2.0 和 EXE SHA-256，先校验并冻结 raw，再对 staging 副本执行 `--best --lzma`、`upx -t`、packed release contract、PE32+ x64/双版本与 3,000,000 bytes 硬门禁，最后生成 `release/codex-switch.exe`，不原地修改 raw。
- `README.md`：ChatGPT Switch 的 GitHub 仓库首页和公开使用说明，说明展示品牌与 Codex Home/EXE/wire contract 兼容命名的边界，并覆盖下载、运行态切换、会话热同步、会话管理、安全和开发说明。
- `CHANGELOG.md`：公开版本更新日志，记录每个 release 的用户可见变化、修复点、风险语义和验证命令。
- `index.html`：Vite 前端 HTML 入口。
- `public/favicon.ico`：浏览器预览与 WebView 使用的 ChatGPT Switch 站点图标；不替代 Tauri Windows EXE 图标。
- `package.json` / `package-lock.json`：前端、Tauri CLI、测试和构建依赖配置，同时维护 npm/前端版本号，release 前需与 Rust/Tauri 版本同步。
- `src/`：React 前端源码。
- `src/App.tsx`：ChatGPT Switch 前端编排入口；挂载时预注册关闭门禁，未 ready 时切换 fail closed；切换后用独立 runtime refresh pending 禁止重复切换，并把 session/backup 标记 stale。备份扫描的新刷新请求排队补跑；用户按需加载后展示后端返回的最多 256 个强校验 Full，逐份提供恢复/删除，不在前端再截成 5 项。检查点清理只以 `failedCount > 0` 判为 partial，纯保留 warning 显示“完成（有保留说明）”；页面内管理账号覆盖、同步/删除确认、手工 Full、真实进度与单一错误面，不使用系统弹窗。
- `src/RelayRuntimeDialog.tsx`：页面内可访问的中转站配置 `<section>`；打开后聚焦标题，保存成功或取消后恢复触发点；API Key 使用 password 输入且不回填，保存失败保留本次输入，保存成功或取消后销毁；校验 URL scheme/凭据/query/fragment，只有 origin 不变时才允许空 Key 保留旧密文。
- `src/RuntimeSwitchProgressPanel.tsx`：运行态切换任务执行器；按后端 `Channel` 事件显示 `planningSessions` 到 `cleaningCheckpoints` 的真实阶段和 typed 终态，不伪造百分比；未知/缺失终态使用保守文案，禁止把它表述为“数据未变化”。
- `src/RuntimeSwitchProgressPanel.test.tsx`：切换阶段（含 `planningSessions` / `cleaningCheckpoints`）、可选关闭阶段、成功、写入前失败、已回滚、回滚失败以及 live region 计时降噪测试。
- `src/OperationResultPanel.tsx`：统一展示 mutation 的操作 ID、备份数、计数、回滚终态与警告。
- `src/SessionManagementPage.tsx`：会话管理独立页面组件；提供来源/归档筛选、搜索、排序、每页 50 条、跨页 selection reconciliation、页面内硬删除确认和恢复可见；表格横向滚动区可聚焦且有可访问名称，确认区聚焦标题，取消或成功后恢复触发点；shared-only 会话不执行无效恢复，不提供排除同步按钮。
- `src/SkillsManagementPage.tsx`：Image2/Grok 独立懒加载页面；展示包版本、安装/漂移、URL 和 Key 配置布尔状态，在页面内执行安装/覆盖确认、受控 password 配置、typed receipt 和后台 best-effort 状态刷新；离开技能页立即卸载确认/配置区并销毁局部 Key，取消或成功后恢复触发点。
- `src/App.test.tsx`：覆盖 runtime-only 首屏、按需域、stale/queued rerun、关闭门禁预注册、runtime refresh pending、单一错误面、页面内确认、切换进度、手工 Full、恢复点删除、检查点释放、备份恢复、更新、并发 guard 与 unknown 终态；显式锁定“有保留 warning 但 `failedCount = 0` 为成功”和“真实删除失败即使无 warning 仍为 partial”。
- `src/SessionManagementPage.test.tsx`：会话筛选、排序、分页、跨页选择/indeterminate、可访问滚动区、删除确认焦点流、空选择禁用、成功后选择清理和 shared-only 恢复门禁测试。
- `src/SkillsManagementPage.test.tsx`：技能页懒加载、安装关闭预检、确认/配置焦点流、typed receipt、Key 失败重试保留及成功/取消/页面失活清空测试。
- `src/api.ts`：Tauri command typed wrapper；拆分 runtime/session/backup 数据域并串行备份列表与持 guard 的空间检查；用 `Channel<RuntimeSwitchProgress>` 传递真实切换阶段，同时封装固定双槽位、会话、手工 Full/恢复、已验证恢复点删除、检查点释放及 Skill 命令。
- `src/api.test.ts`：验证分域加载、legacy 列表先于 checkpoint inspect、inspect/cleanup/delete wire contract、错误隔离、切换 `Channel` 和 operation command，不把失败替换成空数组或零数据。
- `src/types.ts`：前后端 wire contract；包含分域 Dashboard、运行态 phase/outcome、会话/备份/检查点空间、`BackupDeleteReceipt`、带 `attemptedCount` / `failedCount` 的清理 Summary/Receipt 与 Skill typed receipt。
- `src/main.tsx`：React 挂载入口。
- `src/styles.css`：ChatGPT Switch 响应式工作台视觉系统；覆盖 Lucide 图标、运行态轨道、页面内表单/确认、切换时间线、检查点空间/任务流、会话表格和移动端布局。
- `src/vite-env.d.ts`：Vite/TS 资源声明。
- `src-tauri/`：Tauri Rust 后端工程。
- `src-tauri/Cargo.lock`：Tauri/Rust 后端依赖锁定文件，由 Cargo 生成维护，release 前随 `Cargo.toml` 版本更新。
- `src-tauri/Cargo.toml`：Rust 包、Tauri 和后端依赖配置，同时维护 Rust crate 版本号；`windows-sys` 只作为 Windows target 依赖。release profile 固定 `opt-level = "z"`、LTO、单 codegen unit、`panic = "abort"` 和 strip symbols。
- `rust-toolchain.toml`：声明本地 Rust stable toolchain 与 `clippy` / `rustfmt` 组件；CI 另外固定到已验证的具体 Rust 版本。
- `src-tauri/tauri.conf.json`：Tauri 应用配置，`version` 必须与公开 release 版本一致；生产 CSP 仅允许 self/Tauri IPC，dev CSP 只为本机 Vite HMR 与开发样式开放额外权限。
- `src-tauri/build.rs`：Tauri build script。
- `src-tauri/capabilities/default.json`：Tauri 2 command 权限能力配置。
- `src-tauri/resources/skills/newapi-image2-client/`：从用户提供 ZIP 固定 vendoring 的 Image2 Skill、来源哈希、参考文档和 DPAPI-backed PowerShell generate/edit helper；不含用户 Key。
- `src-tauri/resources/skills/grok-search/`：可分发 Grok Web/X Skill、便携 PowerShell 入口/模块、来源说明和许可证；不含本机 config 或 credential。
- `src-tauri/icons/icon.ico`：Windows build 占位图标。
- `src-tauri/src/lib.rs`：Tauri builder 和 command 注册入口。
- `src-tauri/src/main.rs`：桌面应用主入口；release 下启用 Windows GUI subsystem，避免启动时显示控制台窗口。
- `src-tauri/src/commands.rs`：Tauri command 薄边界；switch/sync 使用 async wrapper + `spawn_blocking` 和真实 `Channel` 阶段。终态日志后释放成功/完整回滚、typed prewrite 与恢复可见临时点；`create_full_backup` 创建持久双根 Full，`list_backups` 按需返回最多 256 个强校验恢复点，`delete_backup` 复用同一 managed-full 校验并审计显式删除；inspect/cleanup 在 mutation guard 内执行，cleanup plan 与 execute 两阶段都重新验证完整 payload SHA-256。显式 cleanup 把 `attemptedCount` / `failedCount` 写入 Receipt 和操作日志，terminal 只取决于真实失败数而不是 warnings。所有 mutation 共用进程内/跨进程 guard。
- `src-tauri/src/codex_paths.rs`：严格解析 Codex home、`sqlite_home`、四个受管数据库、`sessions/`、`archived_sessions/` 和 `session_index.jsonl`；无效 `config.toml` fail closed，共享池固定使用本地路径。
- `src-tauri/src/codex_home.rs`：Codex home 文件状态扫描、解析后 SQLite home 展示与 `auth.json` 结构摘要。
- `src-tauri/src/config_patch.rs`：基于 live `config.toml` 的运行态 overlay 计划；只替换运行态模型/service tier/provider 绑定，保护用户全局配置。
- `src-tauri/src/session_scan.rs`：按 `codex_paths` 解析的 SQLite 与 `sessions/**/*.jsonl` 做只读 active 会话扫描和 dry-run 去重；`archived_sessions/` 的管理视图由 `session_manager` 合并，不进入普通同步。
- `src-tauri/src/backup.rs`：manifest v3 scoped 快照、容量/root 预检、候选回填与精确恢复；Full/Sessions 范围覆盖 active/archived 正文，runtime/state 不扩大。`list_recent_backups` 与 `delete_verified_full_backup` 共用 `verify_managed_full_backup`，要求 managed root 直接子目录、Full scope、manifest、精确文件集合/大小和 payload SHA-256；extra file 或 hash drift 候选不显示 verified 且保留。模块在 cleanup plan 与 execute 两阶段重新强校验，仅清理严格成功/完整回滚、typed prewrite 一根/两根和恢复可见单根临时点；legacy v2 只兼容旧成功双根。`CheckpointCleanupSummary/Receipt` 分开记录 attempted/failed/reclaimed/retained，只有计划项执行期 revalidate/remove 失败进入 failed。持久 Full 只显式恢复/删除，不按年龄/数量/mtime prune。
- `src-tauri/src/crypto.rs`：Windows DPAPI 加密/解密封装，用于运行态、备份和 Skill credential；非 Windows 未接安全密钥库时明确拒绝，不提供可逆占位。
- `src-tauri/src/file_ops.rs`：统一原子写、原子复制、流式原子重写和 hard-link no-clobber `atomic_create`；临时文件与目标同目录并同步，Windows 先把已存在的父目录规范化为绝对 verbatim 路径并解析目录联接，再使用 `MoveFileExW` write-through replace，兼容超过传统 `MAX_PATH` 的备份临时路径。`atomic_create` 在目标已抢先出现时返回未发布，不覆盖 live 文件。
- `src-tauri/src/operation_log.rs`：追加式结构化操作记录；只保存 action/phase/status、操作 ID、时间、备份目录和计数。普通历史读取容忍最后一条截断，检查点回收使用全文件严格解析，并支持 `cleanupCheckpoints` 与 `deleteBackup` action；新 cleanup 计数含 `attemptedCount` / `failedCount`，历史记录不重写。
- `src-tauri/src/process_control.rs`：Windows ToolHelp PID/PPID 枚举、受管进程树关闭与 standalone `codex.exe` CLI 检测；只把 `ChatGPT.exe` / `OpenAI.Codex.exe` 识别为关闭根，独立 CLI 只作为 fail-closed 信号。先温和关闭并等待约 8 秒，超时才对重新枚举后仍可证明身份的受管进程使用 `taskkill /F`；子进程均以隐藏窗口启动。
- `src-tauri/src/relay_verify.rs`：使用 rustls 客户端请求 `<base>/models`；Bearer 认证、10 秒超时、禁止重定向、4 MiB 响应上限，要求非空列表包含配置的精确 model ID，并脱敏错误；前端 mutation 管道只消费一次失败，避免 Relay 验证错误重复显示。
- `src-tauri/src/runtime_store.rs`：固定 `plus` / `relay` 两槽位；损坏 metadata fail-closed，DPAPI 加密认证，覆盖前归档，三文件写失败时恢复并验证旧快照；relay 仅允许 HTTPS 或 loopback HTTP，空 Key 保留旧凭据，exact 判定包含影响请求路由的 provider Base URL。
- `src-tauri/src/runtime_switcher.rs`：执行 `preflight -> frozen policy -> RuntimeState/StateOnly backup -> process recheck ->双向 sync -> auth/config overlay -> exact verify`；changed switch 始终使用窄状态点。关闭态双向和 hot current→shared 使用 `SelectMostComplete`；只有 hot shared→current 使用 `PreserveExisting`，其既有 thread 由 `session_sync` 在 target candidate 文件处理前直接跳过。无文件写用 `Deny`，需要合并用零 in-place 的 `Allow`。失败恢复 config/SQLite，不逆向删除实际已完整发布的 JSONL。
- `src-tauri/src/session_manager.rs`：合并 current/shared active/archived 会话视图；硬删除要求确认并创建覆盖四库、`sessions/`、`archived_sessions/`、index 的双根 Full，再用事务/文件操作同步删除。恢复可见使用单根 `StateOnly`，成功落盘后自动释放。
- `src-tauri/src/session_sync.rs`：JSONL-first 双向同步；source 对完整尾行/session ID/len/hash 做执行期稳定验证与有界重试。JSONL 生产路径零 in-place：Create atomic no-clobber，允许替换时的严格扩展与 Divergent 按稳定 source hash 发布完整 imported JSONL，旧目标 bytes/hash/mtime 不变。`SessionSyncPolicy.existing_rollout_path = ExistingRolloutPathPolicy::PreserveExisting` 仅用于 hot shared→current：检测到既有 current thread 后，在 `existing_thread_rollout_path` 与 `copy_rollout_file` 前直接 duplicate+continue，target candidate 不读取、不写入、不发布，零 orphan、`copied_session_files = 0`；SQLite rollout/provider/title 和旧 writer 可见性保留，重复同步不会为该分支增加 C 盘会话文件。新 thread 仍事务插入并使用 source row 可用字段/current provider。current→shared 与关闭态使用 `ExistingRolloutPathPolicy::SelectMostComplete`，完整文件发布后才可切换既有引用。hot current index `Skip`，closed/app-owned shared index 以完整 merged bytes 同目录 `atomic_write`，`Deny` 零写且 `Unchanged` 终前复检。
- `src-tauri/src/skill_manager.rs`：两个固定 Skill 的编译期文件 allowlist、manifest/SHA-256 状态检测、绝对路径/reparse 防护、同卷 stage/旧目录备份/原子激活、带 phase 的崩溃 journal 恢复与 DPAPI 配置保存；备份尚未创建时恢复不得删除用户旧目录。
- `src-tauri/src/update_check.rs`：固定请求本仓库最新正式 GitHub Release；限制响应大小、禁止重定向、校验 stable SemVer、比较当前版本、截断更新说明，并提供受控资产元数据给安装模块。
- `src-tauri/src/update_install.rs`：Windows x64 单文件自更新；固定 EXE 资产 URL/大小/SHA-256 校验，30 秒连接/10 分钟总下载超时，CSPRNG staging、受限 DACL、目录句柄和 target lease。helper 使用阶段 journal 与 Ready ACK；超时执行 kill + wait，清理有界重试；elevated 无参数恢复发现与非 Windows安装均 fail closed。
- `scripts/check-release-contract.mjs`：校验五处版本一致、tag 合同、PE32+ x64、`ProductVersion` / `FileVersion`、基本大小、构建机路径和常见 GitHub token marker；打包脚本与 CI 对 raw 和 packed EXE 都执行，packed 另有 3,000,000 bytes 硬门禁。
- `src-tauri/tests/skill_manager_contract.rs`：临时 `CODEX_HOME` 下的安装、漂移/接管、旧目录备份、URL 校验、Key 密文保留及 Rust→Windows PowerShell DPAPI 兼容测试。
- `src-tauri/tests/synthetic_large_home.rs`：隔离合成大 Home provider 切换回归；固定用多文件验证内容等价 JSONL 的文件数/bytes/SHA-256/mtime 不变、零 imported 副本与 SQLite provider 更新，并提供环境变量可调的 ignored benchmark。
- `tsconfig.json` / `tsconfig.node.json`：TypeScript 编译配置。
- `vite.config.ts`：Vite/Vitest/React 配置。
- `开发者AI开发与PR提交流程.md`：项目文件；定义 Git/PR 协作约束。
- `项目完整链路说明.md`：项目文件；首次 /dxm 生成后需要补充职责说明。
- `项目开发规范（AI协作）.md`：项目文件；首次 /dxm 生成后需要补充职责说明。
- `项目文件结构说明.md`：项目文件；维护文件结构和职责说明。

> 上方快照为自动生成的顶层种子，只能作为起点；维护者应继续补充关键子目录、入口文件和运行态目录的职责。

## 文件维护边界

首次 `/dxm` 生成后，应根据真实项目补充：

- 主入口文件负责什么，哪些逻辑不应继续放入主入口。
- 主要源码目录、测试目录、脚本目录、配置目录分别负责什么。
- 运行态数据、缓存、构建产物、依赖目录如何保护或忽略。
- 修改中文日志、注释或文档后如何做乱码检查。

<!-- DXM-TRELLIS:START -->

## DXM 大开发工作流目录

本项目启用 Trellis/Codex 大开发工作流时，下列目录属于项目级 AI 协作基础设施：

- `.trellis/`：Trellis 项目工作流状态、任务 PRD、spec、workspace journal 和脚本。
- `.trellis/tasks/`：每个开发任务的 `task.json`、`prd.md`、实现上下文和检查上下文。
- `.trellis/spec/`：可复用项目规范；完成任务后应把稳定经验沉淀回这里。
- `.codex/`：项目级 Codex agents、hooks 和配置。
- `.agents/skills/`：跨 agent 共享的 Trellis skill 入口。

维护要求：修改这些目录的事实结构或工作流含义时，同步更新 `AGENTS.md`、`项目开发规范（AI协作）.md` 和本文档。

<!-- DXM-TRELLIS:END -->

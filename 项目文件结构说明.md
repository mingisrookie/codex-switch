# 项目文件结构说明

> 项目：`codex-switch`
> 根目录：`<repo-root>`
> 初始化日期：`2026-06-23`

本文档列出当前项目维护视角下的文件结构与职责。新增、删除、重命名源码、配置、长期文档后，必须同步更新本文。

<!-- DXM-DOC-RULES:START -->

<!-- DXM-CONTRACT:1 -->

## DXM 文档维护规则

- 本块由 DXM 管理；`--refresh-blocks` 只刷新本块，保留下方文件职责和人工补充。
- 新增、删除、重命名文件或改变目录职责后，直接更新下方对应条目，不堆叠临时增量说明。
- 运行态、缓存、账号、密钥、环境变量和构建产物只说明用途与保护边界，不回显真实内容。
- 当前根目录快照是初始化种子；首次 `/dxm` 后需要根据真实子目录和模块职责继续补充。

<!-- DXM-DOC-RULES:END -->

## 维护原则

- 文件职责发生明显变化时，必须更新对应说明。
- 运行态数据文件可以记录用途，但不得写入真实 token、密码、API Key、账号明细或会话内容。
- 本文档不保留“增量补充”堆叠章节；结构变化时，直接更新对应文件条目与职责说明。

## 根目录长期文档

- `AGENTS.md`：目录级 AI 执行规则，定义 `/dxm` 触发与本目录强制遵守事项。
- `项目开发规范（AI协作）.md`：AI/开发者协作规范、架构边界、测试规范、文档同步和乱码检查要求。
- `项目完整链路说明.md`：项目从配置、输入、执行到输出的完整运行链路。
- `项目文件结构说明.md`：项目文件结构与职责边界。
- `开发者AI开发与PR提交流程.md`：分支、PR、GitHub CLI、合并授权等协作流程说明。

## 当前根目录快照

- `.agents/skills/`：Trellis 生成的本项目工作流 skill 入口。
- `.dxm/project.json`：DXM schema v1 项目基线；绑定规范化根目录、目标、交付边界、运行入口、验收 ID/证据类型、验证命令与 Git 授权假设，不保存凭据。
- `.github/workflows/ci.yml`：Windows GitHub Actions 发布质量门禁；执行前端/Rust 门禁与 raw Tauri release contract，再从官方固定 URL 下载 UPX 5.2.0，验证 ZIP/EXE 双 SHA-256，调用统一脚本打包，只上传 packed 的 `release/codex-switch.exe`。v0.2.1 的 PR/main/tag CI 均通过；正式 tag-CI 与 Release 回下载的唯一资产均为 `2,214,400` bytes、SHA-256 `8F6EA219A53BB3395F039327A3CD3827B53EE67B8DAF4B130E60235940A3020C`。本地 raw `5,955,584` → packed `2,228,224` bytes（`4DDC…CED7A`）只保留为历史候选，真实 `v0.2.0 -> v0.2.1` 一键更新也已闭环。
- `.gitignore`：忽略依赖、构建产物、本地运行态和 Trellis runtime/workspace。
- `.trellis/`：Trellis 任务、规范、脚本和当前 MVP task 状态。
- `AGENTS.md`：DXM always-read 项目执行入口；维护 root/mode/scope lock、Trellis/Git 授权边界、证据矩阵、完成门与文档同步红线。
- `docs/`：项目设计、证据和后续实现文档目录。
- `docs/USER_GUIDE.md`：面向普通用户的安装、模式切换、会话、备份、诊断和更新操作指南；与 GitHub 首页互补，不堆放实现细节。
- `docs/SECURITY.md`：面向用户的官方登录、API Key 运行期、会话、备份、诊断和第三方 API 数据边界说明。
- `docs/SKILLS.md`：Image2 与 Grok 搜索等可选技能的独立说明，明确其不属于核心模式切换前置条件。
- `docs/RELEASE_VERIFICATION.md`：Release 页面、应用内更新和维护者发布验证入口；不在 README 固化历史资产校验数据。
- `docs/design/codex-switch-image2-runtime-redesign-no-plus-2026-06-23.png`：Image2 生成的运行态切换新版 UI 参考图，文案使用“Codex 账号态”而非套餐名。
- `docs/evidence/2026-06-23-local-codex-storage-scan.md`：本机 Codex 存储结构只读扫描证据，不含敏感明文。
- `docs/prd/2026-06-23-codex-switch-prd.md`：Codex 运行态切换与本地会话同步 PRD；保留初始调研历史，并以 2026-07-13 release-hardening 合同收敛到固定双槽位。
- `docs/superpowers/plans/2026-06-23-codex-switch-mvp.md`：MVP 实现计划。
- `docs/superpowers/plans/2026-07-13-release-hardening.md`：全栈发布硬化实施计划，覆盖备份、运行态、同步/删除、前端真实状态、relay 验证和质量门禁。
- `docs/superpowers/plans/2026-07-14-skill-bundles.md`：用户已确认的 Image2/Grok 内置 Skill 安装、DPAPI 配置、原子覆盖与跨层测试设计。
- `docs/architecture/platform-readiness-2026-07-14.md`：当前 Windows x64 产品边界与未来 macOS/Linux 的路径、密钥库、进程控制、锁、Skill runtime、CI 迁移矩阵。
- `docs/reviews/2026-07-26-product-reliability-review.md`：v0.2.0 产品、客户、可靠性、安全、性能与可访问性审查，以及 v0.2.1 发布闭环和当前 Unreleased addendum；记录 v0.2.0 全会话 checkpoint 的 GB 级 C 盘增长、PR #4 不直接合并原因、Remote 安全替代、单一 task overlay/受控 ChatGPT 启动、剩余 17 个安全保留目录和仍需保留的产品风险。
- `docs/reviews/2026-07-26-cc-switch-session-sync-review.md`：CC-Switch 会话迁移实现的源码级对照审查；记录可借鉴的显式 opt-in、选择性更新、Online Backup、事务与恢复账本理念，不直接移植高频整文件 migration，并明确 `archived_sessions`、source TOCTOU 和跨介质非单一事务等当前残余风险。
- `scripts/tauri.mjs`：统一 Tauri CLI 入口；保留调用参数并为 release/dev Rust 编译注入工作区和用户目录路径重映射，避免发布 EXE 泄漏构建机绝对路径。
- `scripts/pack-windows-release.ps1`：Windows release 的 copy-only 打包入口；调用方显式传入 UPX 路径，脚本固定官方 UPX 5.2.0 和 EXE SHA-256，先校验并冻结 raw，再对 staging 副本执行 `--best --lzma`、`upx -t`、packed release contract、PE32+ x64/双版本与 3,000,000 bytes 硬门禁，最后生成 `release/codex-switch.exe`，不原地修改 raw。
- `README.md`：ChatGPT Switch 的 GitHub 产品首页；优先说明 Windows 下载、官方模式/API 模式、快速开始、用户数据边界、限制和 FAQ，并把深入操作、安全、可选技能与 Release 验证链接到 `docs/`。
- `CHANGELOG.md`：公开版本更新日志，记录每个 release 的用户可见变化、修复点、风险语义和验证命令；已记录 v0.2.7 的 CI、Release、公开回下载与 updater 闭环，并保留 v0.2.6 只有 tag、未成为 GitHub Release 的真实门禁记录。
- `index.html`：Vite 前端 HTML 入口。
- `public/favicon.ico`：浏览器预览与 WebView 使用的 ChatGPT Switch 站点图标；不替代 Tauri Windows EXE 图标。
- `package.json` / `package-lock.json`：前端、Tauri CLI、测试和构建依赖配置，同时维护 npm/前端版本号，release 前需与 Rust/Tauri 版本同步。
- `src/`：React 前端源码。
- `src/App.tsx`：ChatGPT Switch 前端编排入口；挂载时预注册关闭监听，所有原生 close event 都先阻止，再通过 typed 后端退出命令取得 mutation lock 后安全退出。mutation 期间的关闭请求进入 `closePending`；手机连续性发布阶段明确提供“继续等待 / 仍然退出”。切换期间挂载唯一 task overlay，Relay 首次/配置变化后显示验证或直接切换选择，直接切换成功提供一键返回 Account；首页维护手机连续性一次性通知、开关和 typed 总览。顶部工具栏挂载页面内诊断面板，并把 `window.error` / `unhandledrejection` 收敛为固定安全分类，不传原始错误/stack；业务失败面有真实关联 ID 时导出本次诊断，否则导出最近诊断。页面内管理账号覆盖、完全同步/删除确认、手工 Full、真实进度与单一错误面，不使用系统弹窗。
- `src/RelayRuntimeDialog.tsx`：页面内可访问的中转站配置 `<section>`；打开后聚焦标题，保存成功或取消后恢复触发点；API Key 使用 password 输入且不回填，保存失败保留本次输入，保存成功或取消后销毁；校验 URL scheme/凭据/query/fragment，只有 origin 不变时才允许空 Key 保留旧密文。
- `src/RuntimeSwitchProgressPanel.tsx`：运行态切换单一 modal/task overlay；保留后端原始 phase/timestamp，但聚合为 Account 最多 6 步、Relay 最多 7 步，逐步显示耗时并在终态标出最慢步骤，不伪造百分比；完成态展示官方登录态、typed incremental 状态与 ChatGPT 启动回执，不再渲染恒为零的会话同步字段。mutation 期间收到退出请求时显示安全排队回执，启动 warning 提供原位重试；失败或启动 warning 复用带真实 operation ID 的诊断导出。
- `src/RuntimeSwitchProgressPanel.test.tsx`：切换分组细阶段、逐步耗时/最慢步骤、成功、写入前失败、已回滚、回滚失败、ChatGPT launched/already-running/failed+retry、关联诊断导出、关闭排队回执、焦点流和 live region 计时降噪测试。
- `src/DiagnosticPanel.tsx`：顶部工具栏打开的页面内“诊断与支持”面板及共用 `DiagnosticExportAction`；显示 event count/bytes/14 天与 10 MiB 配置上限，提供 retained-window 或 operation-scoped 导出、opaque export ID 打开所在位置、打开日志目录和只清理诊断 segment 的页面内确认。Downloads 失败后保留错误并提供重试或用户主动“改存应用诊断目录”，不接受任意路径；固定展示脱敏说明并维护焦点/busy/error/success。
- `src/DiagnosticPanel.test.tsx`：诊断面板不展示原始事件、状态/导出/打开/清除、真实 operation ID 与无 ID recent fallback、Downloads 重试/显式备用诊断目录、隐私文案、失败保留、确认焦点及 trigger 恢复测试。
- `src/OperationResultPanel.tsx`：统一展示 mutation 的操作 ID、备份数、计数、回滚终态与警告；rolled-back receipt 使用真实 operation ID 提供“导出本次诊断”。
- `src/OperationResultPanel.test.tsx`：rolled-back receipt 的 operation-scoped 诊断入口和成功回执不额外展示诊断操作测试。
- `src/SessionManagementPage.tsx`：会话管理独立页面组件；提供来源/归档筛选、搜索、排序、每页 50 条、跨页 selection reconciliation、Remote typed 状态、旧 Relay 会话“同步此会话”、页面内硬删除确认和恢复可见；表格横向滚动区可聚焦且有可访问名称，确认区聚焦标题，取消或成功后恢复触发点；shared-only 会话不执行无效恢复。
- `src/SkillsManagementPage.tsx`：Image2/Grok 独立懒加载页面；展示包版本、安装/漂移、URL 和 Key 配置布尔状态，在页面内执行安装/覆盖确认、受控 password 配置、typed receipt 和后台 best-effort 状态刷新；安装/配置失败复用可选 operation ID 的诊断导出；离开技能页立即卸载确认/配置区并销毁局部 Key，取消或成功后恢复触发点。
- `src/App.test.tsx`：覆盖 runtime-only 首屏、按需域、stale/queued rerun、关闭门禁预注册、runtime refresh pending、单一错误面、页面内确认、切换进度、手工 Full、恢复点删除、检查点释放、备份恢复、更新、并发 guard 与 unknown 终态；同时覆盖顶部诊断入口和固定前端异常 payload 不泄露原始 secret/path，显式锁定“有保留 warning 但 `failedCount = 0` 为成功”和“真实删除失败即使无 warning 仍为 partial”。
- `src/SessionManagementPage.test.tsx`：会话筛选、排序、分页、跨页选择/indeterminate、可访问滚动区、删除确认焦点流、空选择禁用、成功后选择清理和 shared-only 恢复门禁测试。
- `src/SkillsManagementPage.test.tsx`：技能页懒加载、安装关闭预检、确认/配置焦点流、typed receipt、Key 失败重试保留及成功/取消/页面失活清空测试。
- `src/api.ts`：Tauri command typed wrapper；拆分 runtime/session/backup 数据域并串行备份列表与持 guard 的空间检查；用 `Channel<RuntimeSwitchProgress>` 和 `Channel<SessionSyncProgress>` 传递真实切换/完全同步阶段，同时封装 Relay switch preference、手机连续性状态/开关/通知/单会话发布、零参数 `request_app_exit`、typed `launch_chatgpt` 重试、固定双槽位、会话、手工 Full/恢复、已验证恢复点删除、检查点释放及 Skill 命令。诊断命令把默认 Downloads 与固定应用诊断目录导出拆成两个零路径命令，只接受可选 operation ID、后端生成的 opaque export ID 或固定前端异常分类，不暴露任意文件路径/原始错误。
- `src/api.test.ts`：验证分域加载、legacy 列表先于 checkpoint inspect、inspect/cleanup/delete wire contract、错误隔离、切换 `Channel`、`launch_chatgpt` 和 operation command；同时锁定 diagnostics status/Downloads 与固定备用导出/open/clear/frontend wire contract 不传任意路径或原始 secret，不把失败替换成空数组或零数据。
- `src/types.ts`：前后端 wire contract；包含分域 Dashboard、运行态/完全同步 phase、Relay validation status、手机连续性 source fingerprint 与 `queued/publishing/remotePublished/partial/conflict/retrying/needsManual/paused` 状态、`AppExitRequestResult` / `ChatGptLaunchResult`、会话/备份/检查点空间、Skill typed receipt，以及 `DiagnosticStatus` / `DiagnosticExportReceipt` / 固定前端异常 input。`RuntimeSwitchResult` 不再带恒空的 backup/toShared/fromShared/checkpoint 字段。
- `src/main.tsx`：React 挂载入口。
- `src/styles.css`：ChatGPT Switch 响应式工作台视觉系统；覆盖 Lucide 图标、唯一任务 overlay、运行态轨道、页面内表单/确认、切换时间线、诊断面板/导出回执/清除确认、检查点空间/任务流、会话表格、reduced-motion 和 1200×820 / 900×640 / 390×844 多视口布局。
- `src/vite-env.d.ts`：Vite/TS 资源声明。
- `src-tauri/`：Tauri Rust 后端工程。
- `src-tauri/Cargo.lock`：Tauri/Rust 后端依赖锁定文件，由 Cargo 生成维护，release 前随 `Cargo.toml` 版本更新。
- `src-tauri/Cargo.toml`：Rust 包、Tauri 和后端依赖配置，同时维护 Rust crate 版本号；诊断 ZIP 使用关闭 default features 的 `zip` 依赖，Known Folder/本地时间使用收敛后的 Windows feature；`windows-sys` 只作为 Windows target 依赖。release profile 固定 `opt-level = "z"`、LTO、单 codegen unit、`panic = "abort"` 和 strip symbols。
- `rust-toolchain.toml`：声明本地 Rust stable toolchain 与 `clippy` / `rustfmt` 组件；CI 另外固定到已验证的具体 Rust 版本。
- `src-tauri/tauri.conf.json`：Tauri 应用配置，`version` 必须与公开 release 版本一致；生产 CSP 仅允许 self/Tauri IPC，dev CSP 只为本机 Vite HMR 与开发样式开放额外权限。
- `src-tauri/build.rs`：Tauri build script。
- `src-tauri/capabilities/default.json`：Tauri 2 command 权限能力配置。
- `src-tauri/resources/skills/newapi-image2-client/`：从用户提供 ZIP 固定 vendoring 的 Image2 Skill、来源哈希、参考文档和 DPAPI-backed PowerShell generate/edit helper；不含用户 Key。
- `src-tauri/resources/skills/grok-search/`：可分发 Grok Web/X Skill、便携 PowerShell 入口/模块、来源说明和许可证；不含本机 config 或 credential。
- `src-tauri/icons/icon.ico`：Windows build 占位图标。
- `src-tauri/src/lib.rs`：Tauri builder 和 command 注册入口；注册 diagnostics status/export/open/log-directory/clear/frontend-error 命令，并在 `RunEvent::Ready` / `ExitRequested` / `CloseRequested` / `Exit` 将生命周期事件送入 best-effort recorder，不改变既有退出门禁。
- `src-tauri/src/main.rs`：桌面应用主入口；release 下启用 Windows GUI subsystem，避免启动时显示控制台窗口。在 updater 启动参数处理前尽早从绝对 APPDATA 初始化随机诊断 session/store 并安装 panic hook；update helper 早退也尽力写 clean session terminal。
- `src-tauri/src/commands.rs`：Tauri command 薄边界；switch/完全同步使用 async wrapper + `spawn_blocking`。请求路由先校验官方登录态，按用户记忆选择验证或直接切换 Relay，关闭受管 ChatGPT 后调用 route/session-view switcher；不调用 session 全量 scan/sync、capacity、checkpoint 或 provider GC。手动单会话/完全同步各自负责关闭、官方 auth bytes 复核、终态与受控重启。所有用户主动 mutation 在 lock/spawn/早退前创建 diagnostic attempt，记录真实 phase/安全 branch，并在 durable operation ID 可用时只做 binding；cleanup/mobile/launch/block/rollback 等后置语义完成后再写唯一 typed terminal。无 durable record 的 update/process/continuity/exit 也有完整 lifecycle，后台 query 只记失败；任何诊断失败都 fail-open，不得改变原 `Result`。`request_app_exit` 调度后 2 秒仍存活会释放 shutdown reservation。
- `src-tauri/src/diagnostic_commands.rs`：诊断 Tauri command 薄边界；读取 store status，按 retained window 或 operation/attempt 选择事件与前置 10 分钟上下文，严格只读/脱敏 operation 子集，生成 typed ZIP receipt；status/retained window 使用事件 timestamp 的 min/max，operation overlap 规范化 started/completed 区间以容忍墙钟回拨。health best-effort 汇总应用/平台、存储、route/auth mode、受管/standalone 进程计数和四个受管 SQLite 的 present/readable/bytes/只读 `schema_version`，失败逐项 unavailable。默认目标使用 Known Folder Downloads，另提供零路径参数的固定 `%APPDATA%\codex-switch\diagnostic-exports` 命令；只用进程内登记的 opaque export ID 打开 containment 复核后的包，并把前端未处理异常限制为固定分类/安全文案。
- `src-tauri/src/diagnostics/mod.rs`：诊断域聚合与 recorder/operation correlation；管理随机 attempt、durable operation ID 绑定、phase/branch/terminal，只有 terminal 真正持久化后才封闭状态并拒绝迟到 bind/phase/branch/重复 terminal；固定诊断根为 `%APPDATA%\codex-switch\logs\diagnostics`。
- `src-tauri/src/diagnostics/event.rs`：diagnostic schema v1、level/event kind/typed terminal/safe context、`u128` Unix epoch 毫秒 `timestamp`，以及随机、不可跨启动稳定关联的 session/attempt ID；Windows ID 依次使用 OS CSPRNG、`CoCreateGuid` 和进程局部 fail-open fallback，导出 manifest 用 `timestampUnit = unixEpochMilliseconds` 固定说明单位。
- `src-tauri/src/diagnostics/sanitize.rs`：落盘前 sanitizer；限制 message/string/context/JSON 深度与数量，按 key/assignment/token/URL/path/自由文本业务 UUID 形态脱敏，同时保留结构化诊断 correlation ID。已知根只在路径边界上映射为 `%CODEX_HOME%` / `%APPDATA%` / `%USERPROFILE%`；盘符、UNC（含正斜杠）、设备和通用未知绝对路径不原样保存。
- `src-tauri/src/diagnostics/store.rs`：独立分段 JSONL store；Windows 同一登录会话、同一绝对/词法规范化诊断根使用 root-scoped `Local\` named mutex 跨进程协调 append/read/status/prune/clear，普通等待有界、panic 路径零等待，不取得 mutation guard；路径别名/跨登录会话不保证共享锁。默认单段 512 KiB、同根总量 10 MiB；segment 按文件名创建时间轮转/prune，read 按 event timestamp 精确过滤最近 14 天。append/read 都在对应 sanitizer 前验证 schema，并提供 append + `sync_data`、dirty-tail 封存、每段唯一截断尾容错、内部损坏/祖先 reparse/非 regular segment 防护和仅删除受管 event segment 的 clear；durable append 后的 post-prune 失败不反报写入失败。
- `src-tauri/src/diagnostics/lifecycle.rs`：全局诊断 runtime、带 PID/timestamp unit 与 best-effort process creation stamp 的 `sessionStarted`、`appReady` / `exitRequested` / `sessionEnded` / `previousSessionUnclean` 与 panic hook；liveness 字段可用时用 PID + creation stamp 排除仍存活实例和 PID reuse。panic 记录使用 root/state try-lock best-effort，继续调用原 hook，不捕获或恢复 panic。
- `src-tauri/src/diagnostics/export.rs`：固定 `README.txt` / `manifest.json` / `diagnostics.jsonl` / `operations.jsonl` / `health.json` ZIP；导出时二次脱敏、删除 forbidden keys、扫描 secrets/identity/自由文本业务 UUID/绝对路径并保留受控结构化 correlation ID，计算负载 bytes/SHA-256，在选定目标的同一目录 staging、自检并 no-clobber 发布，发布后再次复核 hash/内容；正常返回/错误 unwind 由 Drop 尽力删除 `.chatgpt-switch-diagnostics.<pid>.<sequence>.tmp`，硬崩溃残留不由 clear 清理。
- `src-tauri/src/diagnostics/platform.rs`：Windows `FOLDERID_Downloads` Known Folder、本地时间/时区、诊断文件名、Downloads/export containment 和 Explorer 打开边界；不拼接用户目录、不接受任意路径，非 Windows 明确拒绝导出/打开。
- `src-tauri/src/codex_paths.rs`：严格解析 Codex home、`sqlite_home`、四个受管数据库、`sessions/`、`archived_sessions/` 和 `session_index.jsonl`；无效 `config.toml` fail closed，共享池固定使用本地路径。
- `src-tauri/src/codex_home.rs`：Codex home 文件状态扫描、解析后 SQLite home 展示与 `auth.json` 结构摘要。
- `src-tauri/src/config_patch.rs`：基于 live `config.toml` 的请求路由 overlay 计划；受管 Relay provider 固定为共享常量 `openai_custom`，其他名称 fail closed。Relay 路由注入 `experimental_bearer_token`，并权威写入 `requires_openai_auth = true` 与 `supports_websockets = true`；Account 路由删除同一受管表，并对保存 overlay 中缺失的 model/service tier 执行权威删除，保护其他用户全局配置。
- `src-tauri/src/session_scan.rs`：按 `codex_paths` 解析的 SQLite 与 `sessions/**/*.jsonl` 做只读 active 会话扫描和 dry-run 去重；`archived_sessions/` 的管理视图由 `session_manager` 合并，不进入普通同步。
- `src-tauri/src/backup.rs`：当前写入 BackupManifest v4；继承 scoped payload，并以非空 `operationId + role` 绑定会创建自动点的操作。请求路由 mutation不调用本模块；`IncrementalSync` 与手动 `SyncSessions` 分别使用精确 reason/role 的双 `StateOnly`，成功、已回滚或 Backup 前失败只有与唯一 terminal 全量匹配才释放。Full/Sessions 范围覆盖 active/archived 正文，runtime/state 不扩大；未绑定 v2/v3 自动点 fail closed 保留。
- `src-tauri/src/chat_process_state.rs`：固定管理 `process_manager/chat_processes.json`；16 MiB 有界读取、regular-file/reparse/canonical containment、独占快照与 file identity 复检。任何合法 JSON byte-preserving；请求端切换关闭 ChatGPT 后只修复明确的空/全 NUL 损坏，缺失不创建，未知无效格式保留并 fail closed。
- `src-tauri/src/crypto.rs`：Windows DPAPI 加密/解密封装，用于运行态、备份和 Skill credential；非 Windows 未接安全密钥库时明确拒绝，不提供可逆占位。
- `src-tauri/src/file_ops.rs`：统一原子写、原子复制、流式原子重写和 hard-link no-clobber `atomic_create`；临时文件与目标同目录并同步，Windows 先把已存在的父目录规范化为绝对 verbatim 路径并解析目录联接，再使用 `MoveFileExW` write-through replace，兼容超过传统 `MAX_PATH` 的备份临时路径。`atomic_create` 在目标已抢先出现时返回未发布，不覆盖 live 文件；`atomic_publish_new` 要求 source/target 同父目录并用不带 replace flag 的 write-through rename，使诊断 ZIP 文件名冲突可安全选择下一后缀。
- `src-tauri/src/operation_log.rs`：逻辑 append-only、物理整文件原子发布的结构化操作记录；锁内严格解析旧文件、拒绝损坏历史和重复 operation ID，内存追加后通过 `atomic_write` 发布，失败时旧文件 byte-exact。普通历史读取可容忍旧的最后一条截断，严格 cleanup reader 与后续发布拒绝损坏日志；支持 `cleanupCheckpoints` 与 `deleteBackup` action。diagnostics 只严格读取并生成脱敏子集，不修改本 schema 或 cleanup 证据。
- `src-tauri/src/process_control.rs`：Windows ToolHelp PID/PPID 枚举、受管进程树关闭、standalone `codex.exe` CLI 检测和受控 ChatGPT 重启。只把 `ChatGPT.exe` / `OpenAI.Codex.exe` 识别为受管根；关闭前用进程句柄读取且统一验证唯一 AppUserModelID，切换后通过 `IApplicationActivationManager` 原生激活并轮询同一身份。目标缺失/冲突/不可验证返回 typed failure，不执行 PATH 中同名程序；独立 CLI 只作为 fail-closed 信号。
- `src-tauri/src/relay_verify.rs`：使用 rustls 客户端请求 `<base>/models`；Bearer 认证、10 秒超时、禁止重定向，只以 2xx 判断地址、网络和鉴权可用，不读取/判断模型列表且不记录响应正文或凭据。
- `src-tauri/src/runtime_store.rs`：固定 `plus` / `relay` 两槽位；损坏 metadata fail-closed，槽位凭据使用 DPAPI，覆盖前归档，三文件写失败时恢复并验证旧快照；relay 仅允许 HTTPS 或 loopback HTTP，空 Key 保留旧凭据。Relay 模板与 exact 指纹都把 `supports_websockets = true` 作为权威路由合同。按 Relay 配置保存 `validate/direct` 切换选择，地址变化或重新输入 Key 时失效。live exact 判定要求官方 `chatgpt` auth，并比较所有影响请求路由的 provider 字段与解密后的 bearer。
- `src-tauri/src/request_route_switcher.rs`：实现 `1A / 2A / 3A` 请求端切换。preflight 与关闭后 replan 都要求官方 `auth.json`；联合生成 route/session-view plan，会话视图准备成功后才原子写 live `config.toml`，写后验证 auth byte-exact 与目标路由 exact，配置写后失败只回滚原配置。该模块不做全量 JSONL scan、checkpoint 或 provider GC。
- `src-tauri/src/runtime_session_view.rs`：隔离 Relay/Account 会话索引。持久保存原 Account configured/effective SQLite home；Relay 目标用 SQLite Online Backup 复制四个受管库并只在副本归一 provider，Account 目标最多 4 批/30 秒驱动 cutover 队列发布，仍 deferred 则在 route write 前 fail closed。
- `src-tauri/src/runtime_switcher.rs`：保留运行态切换的 typed receipt/phase/launch 类型，以及用户单独触发的 current ↔ shared 活跃会话完全同步编排；不再负责账号/Relay route apply 或 auth 写入。
- `src-tauri/src/mobile_continuity.rs`：手机连续性 cutover、版本化持久队列、source fingerprint、候选识别、8 thread/8 MiB 领取上限和 typed status。只处理 cutover 后全新且未归档的受管 Relay 会话；发布后复核受管路径、官方 Remote 文件名、SQLite/首条 JSONL provider。local file/attachment 在 Remote successor 中用不含本地细节的占位替换，原 source bytes 不变；分叉返回 conflict。
- `src-tauri/src/session_incremental.rs`：保留 v1 current/shared 索引与 planner，供既有手动完全同步维护基线和合同测试；不再由普通请求端切换调用。
- `src-tauri/src/session_manager.rs`：合并 current/shared active/archived 会话视图；硬删除要求确认并创建覆盖四库、`sessions/`、`archived_sessions/`、index 的双根 Full，再用事务/文件操作同步删除。恢复可见使用单根 `StateOnly`，成功落盘后自动释放。
- `src-tauri/src/session_sync.rs`：JSONL-first 双向同步、provider successor 与 storage planning；source 对完整尾行/session ID/len/hash 做执行期稳定验证与有界重试。所有既有 JSONL 都是 immutable/no-clobber；手机发布专用 policy 只在 successor 中重写首条 provider，并递归替换 `local_file/file_path/attachment` 值为“部分内容仅本机”。provider 候选分配最多 32 个，分叉保留多个 successor 并把 thread ID 回传为 conflict；SQLite provider/path 与活动 JSONL provider 一致，transaction 后执行 `PRAGMA quick_check`。
- `src-tauri/src/skill_manager.rs`：两个固定 Skill 的编译期文件 allowlist、manifest/SHA-256 状态检测、绝对路径/reparse 防护、同卷 stage/旧目录备份/原子激活、带 phase 的崩溃 journal 恢复与 DPAPI 配置保存；备份尚未创建时恢复不得删除用户旧目录。
- `src-tauri/src/update_check.rs`：固定请求本仓库最新正式 GitHub Release；限制响应大小、禁止重定向、校验 stable SemVer、比较当前版本、截断更新说明，并提供受控资产元数据给安装模块。
- `src-tauri/src/update_install.rs`：Windows x64 单文件自更新；固定 EXE 资产 URL/大小/SHA-256 校验，30 秒连接/10 分钟总下载超时，CSPRNG staging、受限 DACL、目录句柄和 target lease。helper 使用阶段 journal 与 Ready ACK；超时执行 kill + wait，清理有界重试；elevated 无参数恢复发现与非 Windows安装均 fail closed。
- `scripts/check-release-contract.mjs`：校验五处版本一致、tag 合同、PE32+ x64、`ProductVersion` / `FileVersion`、基本大小、构建机路径和常见 GitHub token marker；打包脚本与 CI 对 raw 和 packed EXE 都执行，packed 另有 3,000,000 bytes 硬门禁。
- `src-tauri/tests/skill_manager_contract.rs`：临时 `CODEX_HOME` 下的安装、漂移/接管、旧目录备份、URL 校验、Key 密文保留及 Rust→Windows PowerShell DPAPI 兼容测试。
- `src-tauri/tests/synthetic_large_home.rs`：隔离合成大 Home provider 切换回归；固定用多文件验证内容等价 JSONL 的文件数/bytes/SHA-256/mtime 不变、零 imported 副本与 SQLite provider 更新，并提供环境变量可调的 ignored benchmark。
- `tsconfig.json` / `tsconfig.node.json`：TypeScript 编译配置。
- `vite.config.ts`：Vite/Vitest/React 配置。
- `开发者AI开发与PR提交流程.md`：项目文件；定义 Git/PR 协作约束。
- `项目完整链路说明.md`：项目文件；首次 /dxm 生成后需要补充职责说明。
- `项目开发规范（AI协作）.md`：项目文件；首次 /dxm 生成后需要补充职责说明。
- `项目文件结构说明.md`：项目文件；维护文件结构和职责说明。

> 上方快照为自动生成的顶层种子，只能作为起点；维护者应继续补充关键子目录、入口文件和运行态目录的职责。

## 文件维护边界

首次 `/dxm` 生成后，应根据真实项目补充：

- 主入口文件负责什么，哪些逻辑不应继续放入主入口。
- 主要源码目录、测试目录、脚本目录、配置目录分别负责什么。
- 运行态数据、缓存、构建产物、依赖目录如何保护或忽略。
- 修改中文日志、注释或文档后如何做乱码检查。

<!-- DXM-TRELLIS:START -->

## DXM 大开发工作流目录

本项目启用 Trellis/Codex 大开发工作流时，下列目录属于项目级 AI 协作基础设施：

- `.trellis/`：Trellis 项目工作流状态、任务 PRD、spec、workspace journal 和脚本。
- `.trellis/tasks/`：每个开发任务的 `task.json`、`prd.md`、实现上下文和检查上下文。
- `.trellis/spec/`：可复用项目规范；完成任务后应把稳定经验沉淀回这里。
- `.codex/`：项目级 Codex agents、hooks 和配置。
- `.agents/skills/`：跨 agent 共享的 Trellis skill 入口。

维护要求：修改这些目录的事实结构或工作流含义时，同步更新 `AGENTS.md`、`项目开发规范（AI协作）.md` 和本文档。

<!-- DXM-TRELLIS:END -->
